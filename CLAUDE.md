# سهل (Sahl) — ملف ذاكرة المشروع

> اقرا الملف ده بالكامل قبل أي شغل. ده السياق الدائم للمشروع.

---

## 🚦 وقفنا فين — آخر تحديث 24 سبتمبر 2026

> **البلوك ده أول حاجة تتقرا، وبيتحدّث آخر كل جلسة.** لو تاريخه بعيد عن
> النهاردة، غالباً إنت على فرع قديم — شوف «أول حاجة تعملها» تحت.

| البند | الحالة |
|---|---|
| **آخر نسخة اتسلّمت** | **v60** — 24 سبتمبر (**تكلفة J&T النهائية في التفاصيل · بحث المحادثات القديمة · الردود المحفوظة زراير بضغطة + نافذة إدارة وترتيب**) · ⏳ **لسه ماترفعتش**. قبلها v59 (تحديث الحالات لحظياً) **اترفعت واتأكدت** 24 سبتمبر |
| **فرع الشغل** | `claude/ecstatic-hamilton-olzaa8` (سيشن J&T — مرفوع على origin بقرار المالك 20 سبتمبر مساءً) |
| `check.sh` | ✅ أخضر (14 فحص) |
| الهارنسات | **29 هارنس** · `test-jt-sign.mjs` 57 فحص · `test-jt-awb.mjs` 5 · `test-jt-ship.mjs` 33 · `test-realtime-status.mjs` 16 · **`test-jt-fee.mjs` 10 · `test-wa-search.mjs` 18 · `test-wa-qr.mjs` 47 (اتكتب من جديد)** |
| آخر migration | `wa_quick_reply_title` (بعد `jt_fee_sync_cron` · `jt_replay_after_clock_fix` · `jt_trace_order_kind_not_clock` · `jt_final_shipping_fee` · `jt_status_map_return_signed_13`) — **مطبّقين على الحي**. EFs: `jt-lookup` **v5** · `jt-status` **v4** · pg_cron `sahl-jt-fee-sync` كل 15 دقيقة |

**🟢 J&T اشتغلت على الإنتاج — 5 شحنات حقيقية (21 سبتمبر، 15:09–15:46 بتوقيت القاهرة):**
الصلاحية اتفتحت، والمالك شحن بنفسه. **الدليل مقيس من الداتابيز ومن J&T مش من كلام:**

| الأوردر | البوليصة | كود الفرز | العنوان |
|---|---|---|---|
| `17172` | `JEG000538937803` | `20,B02-02,` | الإسكندرية / المنتزه / ميامي |
| `W-12` | `JEG000543299171` | `20,G01-04,009` | الدقهلية / دكرنس / المحمودية |
| `W-13` | `JEG000531296658` | `20,A01-03,021` | القاهرة / حلوان / 15 مايو |
| `17199` | `JEG000541472500` | `20,B02-02,001` | الإسكندرية / المنتزه / المنزه ثاني |
| `17200` | `JEG000538241985` | `20,A01-39,013` | القاهرة / القاهرة الجديدة / التجمع الخامس |

- **5 من 5 موجودين عند J&T** (`getOrders command:2`): `orderStatus 101` · `lastCenterName
  10thRamadanCityHub` · المرسل **الحقيقي** من البوابة (مش مرسل Postman التجريبي) ·
  `itemsValue` = الـCOD · `remark` = المنتجات. **صفر تكرار** (5 أرقام متميزة لـ5 أوردرات).
- سجل الحالة `confirmed → BOSTA AUTO` بـ`by: "J&T API · shekoz"` / `"· ebrahim"` — يعني
  موظفين اتنين استخدموها، والطباعة حصلت (`awb_print_count` 1–2 لكل أوردر).
- 🔴 **رقم البوليصة في الإنتاج بيبدأ بـ`JEG` مش `UEG`** (الـ`UEG` كان الـSandbox).
  أي كود أو توثيق بيفرّق J&T عن بوسطة بالبادئة لازم يقبل الاتنين.
- ⚠️ **`shipping_fee_estimated` فاضية على الخمسة** — مفيش تقدير اتسجّل (`getComCost`
  مقفول)، والتكلفة المؤكدة هتيجي من `settlementReturn`. ده **صح** مش نقص.
- **الـcallbacks — حتتين مختلفتين، متخلطوش:**
  1. **الاشتراك في البوليصة** (`trace/subscribe`) — ✅ **اتعمل**: الـ5 بوالص اتسجّلوا
     (`isSuccess:true` للخمسة)، و**`jt-ship` v5** بقت بتعمله أوتوماتيك بعد كل شحنة،
     وفشله بيتسجّل في `orders.jt_subscribe_error` (migration `jt_subscribe_error`)
     **بعد** تسجيل البوليصة عن قصد — الشحنة اتعملت وفلوسها راحت، فمينفعش فشل
     الاشتراك يرجّع خطأ ويخلّي الموظف يضغط تاني.
  2. **تسجيل الـURLs** — 🟢 **اتعمل من البوابة بإيد المالك (21 سبتمبر ~17:25)،
     مش إجراء إداري زي ما كنت كاتب.** ⚠️ **تصحيح لقياس غلط مني**: قلت «مفيش
     باراميتر `url` في أي endpoint فده إجراء عندهم» — ده **صح في الـAPI وغلط
     كاستنتاج**: البوابة نفسها فيها `Interface Mgt.` → `Application` → خانة URL
     → `Save`، وقبلها `Sandbox Mgt.` → `Register the category` → `Debugging`.
     التوثيق مش المكان الوحيد اللي فيه الإجابة — **الواجهة كمان** (درس 48).
     الحالة دلوقتي **`Applying`** = مستني مراجعة 1–2 يوم عمل.

**🟢 وأول callbacks حقيقية من J&T وصلت (21 سبتمبر 17:25–17:26) — التوقيع عدّى:**

| الوقت (القاهرة) | النوع | البوليصة | `digest_ok` |
|---|---|---|---|
| 17:25:20 | `trace` | `UT0001359848461` | ✅ **true** |
| 17:26:07 | `order` | `JT4000073746531` | ✅ **true** |

**ده قفل أكبر مخاطرة كانت فاضلة:** `jt-status` بترفض أي نداء مش موقّع بـ`401`،
وماكانش فيه أي دليل إن J&T بتوقّع الـcallbacks بمفتاحنا فعلاً. دلوقتي اتأكد.
و`apply_note = order_not_found` **صح** — دي بوالص تجريبية مش بتاعتنا.
⚠️ وظهر صف واحد لكل نوع رغم إن الضغط اتكرر: **حارس التكرار** (نفس الحمولة =
نفس الهاش) بيرد `code 1` من غير ما يكرر الصف — فالمحاولات نجحت عندهم واتسجّلت
مرة عندنا. ده مقصود.

🔴 **بس دي بيانات المثال من التوثيق بالحرف** (`JT4000073746531` · `test0040` ·
南京雨花台板桥网点 — منسوخة من `requestCode` في صفحة التوثيق). يعني بتثبت إن
**الأنبوبة شغالة**، **مابتثبتش** إن الحمولة المصرية الحقيقية شكلها كده (درس 26).

✅ **والموافقة اتأكدت سلوكياً (21 سبتمبر 22:58 → مستمرة):** الـcallbacks الحقيقية
بقت بتوصل. **787 حدث · `digest_ok=true` على الكل · صفر مرفوض** (آخر قياس 24 سبتمبر
09:50 — كانوا 716 عند بناء الخريطة الصبح، والعدد بيزيد لوحده).
(وقت التسجيل كان `logistics/trace` بيرجّع `details: []` للخمسة — يعني الصمت
ساعتها كان معناه «مفيش مسح اتعمل لسه» مش «التسجيل فشل». الفرق ده اتقاس مش اتفترض.)

## 🟢 `jt_status_map` اتبنت من داتا حقيقية (24 سبتمبر) — والجدول الفاضي كان القرار الصح

الجدول فضل **فاضي عمداً** لحد أول حمولة مصرية (درس 44)، وأول ما وصلت طلع
إن كل اللي كنا هنبني عليه **غلط**:

🔴 **1) `scanTypeCode` الحقيقي مش الأرقام 1–15 اللي في التوثيق.** الأرقام دي
`traceNode` (عُقد **الاشتراك** اللي إحنا بنطلبها)، واللي بيرجع في الـcallback
حاجة تانية خالص: **10 · 50 · 92 · 94 · 100 · 110 · 172**. لو الخريطة اتبنت
على أرقام التوثيق كانت هتطلع غلط بالكامل **وفي صمت**.

🔴 **2) نوعين بيوصلوا بلا `scanTypeCode` خالص** — `Enter branch scan` (70 حدث)
و`Holding scan` (19) — رغم إن التوثيق كاتب الحقل **إجباري (`Y`)**. والدالة
كانت بتطابق بالكود لوحده (`where scan_type_code = p_scan_code`)، يعني **89 حدث
كانوا هيتسقّطوا في صمت للأبد**. الدالة بقت: الكود أولاً، ولو فاضي مفتاح بالاسم
(`name:<scan_type>`) بتحسبه هي — فمفيش تعديل في الـEF.

🔴 **3) `scanType` بييجي إنجليزي** — مش صيني زي حمولات الـdebugging.

| `scanType` | كود | عدد | → حالتنا |
|---|---|---|---|
| Sending scan | 50 | 161 | `In transit between Hubs` |
| DC arrival & Station arrival | 92 | 148 | `Received at warehouse` |
| Enter branch scan | **—** | 70 | `Received at warehouse` |
| Pickup scan | 10 | 70 | `Picking up from consignee` |
| Delivery scan | 94 | 54 | `Out for delivery` |
| Abnormal parcels scan | 110 | 27 | `Exception` |
| Signing scan | 100 | 21 | **`Delivered`** |
| Holding scan | **—** | 19 | `Exception` |
| Returned parcel scan | 172 | 6 | **`Returned to business`** |

⚠️ **كل القيم المستهدفة موجودة أصلاً في `constants.js`** — صفر قيمة جديدة، فكروت
البضاعة ومتوقع التحصيل ونسبة التسليم بتشتغل زي ما هي. أي قيمة جديدة هنا = أوردر
بيختفي من الحسابات من غير أي خطأ (الخطر الموثّق في أول القسم).

✅ **اتأكدت حيّ خلال 20 دقيقة من التطبيق**: 4 أوردرات اتحرّكوا لوحدهم من
`BOSTA AUTO` لـ`Received at warehouse` بـ`apply_note='status_set'` من callbacks
حقيقية — مش من إعادة تشغيل ولا محاكاة.

**واتجرّب بترانزاكشن راجعة قبل التطبيق** (قاعدة 4) بإعادة تشغيل آخر مسح لكل
بوليصة: `unmapped = 0` · **التلجرام صفر** (اتأكد من الكود كمان: `tg_log_status_change`
بتبعت بس لما `by` فيها «واتساب» وحالة `confirmed/cancelled`، وبتاعنا `J&T API`) ·
`upsell` من غير تغيير · و`inventory_cost_snapshot` كان مقفول على الـ75 قبلها.

### ✅ والـbackfill اتعمل (24 سبتمبر 10:10 — بموافقة المالك)

الخريطة بتشتغل **للأمام** بس، واللي اتسلّموا خلاص مش هيجيلهم مسح تاني —
فكانوا هيفضلوا واقفين على `BOSTA AUTO` للأبد. إعادة تشغيل **آخر مسح لكل
بوليصة** حرّكت **26 أوردر** (`status_set` للـ26، صفر مرفوض):
**21 `Delivered`** · 2 `Exception` · 2 `In transit between Hubs` ·
1 `Returned to business`.

**اتقاس بترانزاكشن راجعة الأول** (قاعدة 4) مرتين — واحدة للنتيجة وواحدة
للآثار الجانبية: **التلجرام صفر** · **العمولات من غير أي تغيير**
(`void 9 / earned 8 / pending 14` قبل وبعد) · و🔴 **`inventory_cost_snapshot`
اتحرّك 7,040 → 22,855 على نفس الـ109 صف** — ده تريجر
`lock_cogs_on_delivery` بيجمّد تكلفة البضاعة لحظة التسليم، **وده مقصود
ومطلوب** عشان أرباح الـ21 أوردر تتحسب صح. اكتشفته من القياس مش من الذاكرة.

🔴 **العدد بينقص لوحده** كل ما مسح جديد يوصل لأوردر لسه في السكة، فأي
backfill جاي **يتقاس وقت التنفيذ مش يتقرا من هنا** (القياس 05:30 قال 72 ·
09:50 قال 32 · والتنفيذ 10:10 لقى 26). الاستعلام اللي بيفرز «ليه مسح سابق»
من «لسه جديد»: `jsonb_array_elements(payload->'details')` + `row_number()`
على `scanTime` لكل `bill_code` ثم `join` على `jt_status_map` بنفس مفتاح
الدالة (`coalesce(code, 'name:'||lower(scan_type))`).

الحالة بعد التنفيذ (109 أوردر J&T): 38 `BOSTA AUTO` (**شحنات جديدة لسه
مامسحتش — طبيعي مش عطل**) · 29 `Delivered` · 22 `Out for delivery` ·
11 `Exception` · 5 `In transit` · 3 `Returned to business` · 1 `cancelled`.

## 🟢 الحالات بتتحدث في اللوحة لحظة بلحظة (24 سبتمبر — طلب المالك)

الريل-تايم على `orders` كان **شغّال من زمان** (`orders` في `supabase_realtime`
والقناة بتفلتر بـ`tenant_id`) — بس `handleRealtimeChange` كانت بتحدّث **`all`
بس**، و`all` هو مخزن الماليات الكسول اللي بيفضل **فاضي** طول ما الموظف في
صفحة الأوردرات. مصدر الجدول هو **`fil`**، فالصف المعروض مكانش بيتغيّر غير
لما `fetchOrdersPage` ترجع من السيرفر.

- **`fil` بتتحدث من الحدث نفسه** → شارة الحالة بتتغيّر **فوراً** من غير أي جلب.
- 🔴 **`REPLICA IDENTITY` على `orders` = `default`**، يعني `payload.old` فيه
  **المفتاح بس** — فالحالة القديمة بتتقرا من الصف اللي في الذاكرة مش من الحدث.
  أي كود يقارن بـ`payload.old.status` هيشوف `undefined` دايماً.
- **`toast` باسم الأوردر والحالة الجديدة**، مجمّع في نفس نافذة الـ800ms بتاعة
  الأوردرات الجديدة (رشقة = toast واحد).
- 🔴 **الموظف اللي غيّر الحالة بنفسه مايتبعتلوش toast** — الفرق من آخر مدخل
  في `status_log` (`by`) مش من نوع الحدث. ⚠️ والسجل ممكن يبقى متخزّن
  **كـstring** (نود n8n بتعمل `stringify` على عمود jsonb) فبيتفكّ بـ
  `parseStatusLog` — من غير كده كل تغيير من n8n كان هيتحسب «مش من الموظف».
- **نافذة التفاصيل المفتوحة بتتحدث معاه** — قبل كده كانت `ordersSetSelected`
  بتتحط من غير إعادة رسم، فالجدول يتغيّر والنافذة تفضل على القديم.
- 🔴 **شبكة أمان**: `postgres_changes` **مافيهوش replay** — أي تحديث بيوصل
  والـsocket مقفول (تاب نايمة · نت قطع) بيضيع للأبد والنقطة تفضل خضرا.
  فالتيكر (30ث) بقى بيراجع كمان **شحنات J&T اللي لسه في السكة على الصفحة
  المعروضة**، ورجوع الاتصال بعد قطع بيجرّ جلب تعويضي (`rtEverOff`).
- `tools/test-realtime-status.mjs` — **16 فحص + 3 معايرات**: شيل تحديث `fil` ·
  شيل فلتر `by` · شيل `renderDetail`.
  ⚠️ **ودرس قياس**: أول نسخة من فحص النافذة كانت بتقرا نص `#dcnt` كله وتوقع
  وهي صح — **التايم لاين بيعرض «من ← إلى» فالحالة القديمة بتفضل ظاهرة فيه**،
  والمعايرة عدّت وهي غلط (درس 47). المرساة الصح `#dsel` (قيمة الـselect).
  وفحص الـtoast كان بيقرا بعد 60ms والـtoast بيخرج بعد نافذة التجميع (800ms).

ℹ️ **والـ`order` callbacks بتحمل الوزن** (140 حدث للـ70 بوليصة) — بس J&T بترجّع
**نفس الوزن اللي بعتناه** (`1.0`) مش وزن معاد قياسه. ✅ **والتكلفة الحقيقية
بقت بتتسجّل من غير ما نستنى `settlementReturn`** — القسم الجاي.

## 🟢 تكلفة شحن J&T النهائية على الأوردر (24 سبتمبر — طلب المالك)

«J&T تسجل سعر شحن الأوردر النهائي عند تم التسليم ومرتجع، ويكون السعر الحقيقي ب الـCOD Fees».

**المصدر مقيس (70 بوليصة): `waybill/getWaybillInfo`** — كان مسموح لينا من الأول.
بيرجّع `totalFreight` (= `freight`) · `packageChargeWeight` (الوزن اللي J&T حاسبت
عليه — 4.43 مش الـ1 اللي بعتناه) · `isSign`.
- `totalFreight` = تعريفة × **1.254** بالمنطقة (A/B/C) والوزن — طابقت **67/67**.
- 🔴 **مافيهوش رسوم COD**: نفس الشريحة بـCOD مختلف = نفس الرقم بالحرف (1450/1395/1285
  → 57.68). فالرسوم **بنحسبها إحنا من العقد**: `max(COD × 1%, 5)` — في
  `platform_settings.jt_cod_fee` (`pct` · `min` · `on_returns`).
- 🔴 **`isSign=2` مش موثّقة** = مرتجع اتقفل (3 بوالص آخر مسح `13 Returned Signed`)،
  ورقمها **50% من الذهاب** (28.84 = 57.68 / 2). قبل ما يتقفل بيعرض رسوم الذهاب كاملة.
- `settlementReturn` لسه **صفر** — ولما يوصل بيكسب (فاتورة) ومابيتكتبش فوقه.

**القاعدة («متسجّلش تقديري كأنه مؤكد»):** `real_shipping_fee` (اللي الماليات بتقراه)
بيتكتب **لما الرقم يبقى نهائي بس**: `Delivered + isSign 1` · `مرتجع + isSign 2` ·
أو فاتورة. قبل كده بيتخزن في `jt_freight` بـ`jt_fee_final=false` ومابيدخلش الماليات —
والبانر «X أوردر من غير تكلفة شحن» بيعدّه زي ما هو.
- رسوم COD على المرتجع **صفر افتراضياً** (`on_returns=false` — مفيش COD اتحصّل).
  ⚠️ **ده افتراض لحد ما أول فاتورة J&T تأكده أو تنفيه** — لو طلعت بتتحسب، سطر
  واحد في الإعداد.

**الشكل:** migration `jt_final_shipping_fee` — أعمدة `jt_freight` · `jt_cod_fee` ·
`jt_charge_weight_kg` · `jt_is_sign` · `jt_fee_final` · `jt_fee_source` ·
`jt_fee_for_status` · `jt_fee_synced_at` + `app.jt_apply_fee` (والتسوية بقت بتعدّي
عليها — وبطّلت تدوس `shipping_weight_kg` اللي إحنا بعتناه) + `jt_fee_candidates`.
`jt-lookup` **v5** action `fee_sync` (diag/service بس) + **pg_cron `sahl-jt-fee-sync`
كل 15 دقيقة** (التوكن بيتقرا من `platform_settings` وقت التنفيذ؛ لو مفيش مرشّحين
مفيش نداء أصلاً). نافذة التفاصيل: سطر «تكلفة الشحن (J&T)» للأدمن بالتفصيل
(`jtFeeRow` — `test-jt-fee.mjs` 10 فحص + 2 معايرة).

**النتيجة الحية (أول تشغيل):** **37 من 37 مسلّم** بقى ليهم رقم نهائي (متوسط 77.19 ج =
63.82 شحن + 13.37 رسوم تحصيل) · **3 مرتجعات مقفولة** 28.84 · **8 مرتجعات لسه مفتوحة**
عند J&T (`isSign 0`) — الجدولة هتلقطهم. والجدولة **اتأكدت بتشتغل** (21:15 UTC: مرشّحين 2 · مبدئي 2).
⚠️ `merchant_finance_summary` / `ai.finance_summary` على الداتابيز لسه فيهم
`COALESCE(real_shipping_fee, 85)` — **الفرونت مابيقراهمش** (اتقاس)، فمالهمش أثر.

### 🔴 وتلات باجات J&T اتكشفوا من القياس في نفس الشغل

1. **وقت J&T = UTC+2 ثابت — مش توقيت مصر بالصيفي.** `order` callbacks بتوصل بعد
   وقتها بـ**1.9ث** (وسيط 137) لو اتقرا +2، وعمر ما واحد وصل قبله. `parseJtTime`
   كانت بتطبّق DST (+3) فكل الأوقات المتخزّنة كانت **متأخرة ساعة** → `jt-status` **v4**
   + تصحيح +1 ساعة لـ103 صف (الصفوف اللي جالها حدث بعد النشر اتكتبت صح فاتسابت).
2. 🔴 **`order` callbacks كانت بتقدّم الساعة وتخلّي المسح الحقيقي «stale»** — J&T
   بتبعت «已揽收» بعد `Sending scan` بدقيقة-اتنين، فحارس stale كان بيرمي المسح.
   **32 أوردر فضلوا `BOSTA AUTO` وهم في السكة فعلاً.** الإصلاح (migration
   `jt_trace_order_kind_not_clock`): `order:*` مابتلمسش `carrier_status_at` أبداً،
   وبتكتب الخام بس لو مفيش مسح تتبع لسه.
3. **رحلة المرتجع (`isRefund=1`) بنفس أكواد الذهاب** (50/92/94/Holding) فكانت
   بترجّع أوردر راجع لـ«In transit»/«Out for delivery» (17264 · 17266 فضلوا In transit
   برغم مسح 172). `jt-status` بقت بتبعتها بـ`refund:` والدالة بتسجّلها خام بس. 13 و172
   بيعدّوا عادي (هما اللي بيحسموا). + خريطة `13 Returned Signed → Returned to business`.

**والتصحيح (migration `jt_replay_after_clock_fix` — اتجرّب بترانزاكشن راجعة الأول):**
34 أوردر اتحركوا (32 → `In transit between Hubs` · 17264/17266 → `Returned to business`) ·
**التلجرام صفر**. فيه 3 عقود جديدة في `check-functions.py` (UTC+2 · `refund:` ·
`fee_sync` مش للموظف) متعايرة بحقن كل واحد.

## 🟢 بحث المحادثات بيلاقي الشات القديم (24 سبتمبر — طلب المالك)

«لما بنبحث عن أي رقم عدى عليه وقت حتى لو بسيط مش بيظهر خالص الشات بتاعه».
السبب: `waConvMatches` بتفلتر **أحدث 200 محادثة المحمّلة بس** (نفس عمى فلتر التصنيفات —
19 سبتمبر). الإصلاح بنفس شكل `waAdExtra`/`waLabelExtra`: `waFetchSearchConvos` بتستعلم
السيرفر (`or` على الاسم + `wa_id` + `customer_phone`، 300ms debounce، جيل للردود
المتأخرة، سقف 100) والنتيجة في `waSearchExtra` **منفصلة** مش مدموجة.
- الرقم بيتطبّع (`normalizePhone`) ≥ 3 أرقام؛ الاسم بيتطبّع (الهمزات/التاء/الياء → `_`).
- 🔴 **تهريب PostgREST**: `,().:` بيكسروا صيغة `or` — بيتشالوا من النص قبل الاستعلام.
- `tools/test-wa-search.mjs` — **18 فحص + 5 معايرات** (فيكستشر 260 محادثة والهدف بره الـ200).
  والستب بقى بيطبّق `.ilike` جوّه `.or()` فعلاً.

## 🟢 الردود المحفوظة: زراير بضغطة + نافذة إدارة (24 سبتمبر — طلب المالك)

- **زرار «⚡ إضافة و تعديل رد محفوظ»** في آخر شريط الفلاتر (الشمال) — بيتولد من
  `waBuildFilters` (الشريط بيتمسح كله) و**مش `.wa-filter`** عمداً (test-wa-labels بيعد 12).
  على الموبايل مخفي (كان هيعمل صف تالت) و«⚙️ إدارة الردود» في آخر صف الزراير بيفتح نفس النافذة.
- **النافذة** (`#wa-qrm` — نفس غلاف `.jt-bd`): قايمة بالترتيب · ▲▼ · ✏️ · 🗑 بتأكيد ·
  «＋ رد جديد». الفورم: **اسم مختصر إجباري** (≤40 — CHECK `wa_qr_title_len`) · نص ·
  صور لحد 5 (الموجودة بتتشال/تفضل، والجديدة بتترفع وقت الحفظ بمسار المتجر).
- **الترتيب** في عمود `sort` الموجود (محدش كان بيكتبه — الـ4 كلهم 0): تبديل ثم إعادة
  ترقيم 0..n-1 والكتابة للي اتغيّر بس. فشل = رجوع لنسخة السيرفر.
- 🔴 **الزراير تحت بتبعت بضغطة** (بيلغي قرار 16 سبتمبر — شوف «القرارات المحسومة»):
  قفل `waQrSending` · الزراير بتختفي مع النافذة المقفولة · **المحادثة بتتلقط لحظة الضغط
  وكل الأجزاء بتروحلها حتى لو الموظف بدّل الشات** (النسخة القديمة كانت بتقف في النص =
  نص رد عند العميل) · خانة الكتابة مابتتلمسش · الاقتباس (لو فيه) على أول رسالة.
- ⚡ بقى **طيّ/فتح** للصف (مفتوح افتراضياً — `sahl_qr_collapsed`).
- ⚠️ **الـ4 ردود اللي على الحي مالهمش اسم** — الزرار بيعرض أول كلامهم والنافذة بتعلّمهم
  «بدون اسم». **المالك يسمّيهم** (مفيش تخمين).
- ⚠️ حذف رد/صورة **مابيمسحش الملف من الـStorage** — مفيش سياسة DELETE، **وده صح**:
  رسايل اتبعتت ممكن تكون بتعرض نفس الصورة في الشات.
- `tools/test-wa-qr.mjs` اتكتب من جديد — **47 فحص + 7 معايرات** (كلها بحارس المرساة —
  درس 47). والستب بقى بيطبّق `update`/`delete` على `__QR` فعلاً.

**🟢 وقايمة العناوين الكاملة وصلت من J&T IT (21 سبتمبر — `JT_Addresses_with_codes_JT.xlsx`):**
27 محافظة · **267 مدينة** · **6,464 منطقة** وكلهم بأكواد J&T — ده اللي `online/pca` كان
هيديه، فالـendpoint المقفول بقى بلا لزمة.
- migration `jt_pca_cities_from_jt_it`: `jt_pca` بقت **267 صف مدينة** (`area=''`) بدل
  الـ237 بتوع قالب 2023، والأكواد في `raw`. **31 مدينة جديدة** اتفتحت منها **الزقازيق**
  (اللي كانت الفجوة الموثّقة) · أبو حماد · أبو كبير · الإبراهيمية · القرين · ههيا ·
  العاصمة الجديدة · مدينة المستقبل · البروج · مون سيتي · و**شمال سيناء** (المحافظة الـ27).
- 🔴 **وأهم اكتشاف: J&T مسجّلة اسم كل محافظة كـ«مدينة» جوّاها** (القاهرة/القاهرة ·
  الشرقية/الشرقية…) بأكواد `vip*` — وده بالظبط اللي بيخلّي اقتراح اللاندنج يشتغل،
  لأن عمود `orders.city` **بيحمل اسم المحافظة مش المدينة** (765 أوردر «القاهره» ·
  173 «الجيزه» · 135 «الاسكندريه» في 30 يوم).
- ⚠️ وفيه صف `Hqbr` بكود `999999` تحت القاهرة — بيانات زبالة عند J&T. **اتساب زي ما
  هو عمداً** (مانخمّنش في قايمتهم) وهيبان أول خيار في مدن القاهرة. لو المالك عايزه
  يتشال دي سطر `delete` واحد.
- 🔴 **والمنطقة فضلت نص حر — والملف ده أثبت القرار مش نقضه**: 4 من الـ5 شحنات اتعملت
  بمناطق **مش في الملف** (ميامي · المنزه ثاني · التجمع الخامس · المحمودية) وJ&T قبلت
  الخمسة، و«شمال سيناء» نفسها جت من J&T **بلا أي منطقة خالص**. فالـ6,464 منطقة بقت
  **اقتراحات** في `app/data/jt-areas.json` (152KB، بيتحمّل بالتوازي ومابيوقفش الشحن لو
  وقع) مش صفوف في `jt_pca`: الحارس في `jt-ship` بيتحقق من `prov+city` بس، و6.4k صف في
  الجدول كانوا هيبقوا 7 نداءات PostgREST كل جلسة بلا فايدة. المرجع الكامل بالأكواد في
  `tools/jt/docs/jt-addresses-2026-09-21.json`.

**آخر شغل اتعمل (21 سبتمبر الصبح — المنطقة نص حر + `jt_pca` من قالب البوابة)
— ⚠️ القالب ده اتشال مساءً واتستبدل بقايمة J&T الرسمية (شوف البلوك اللي فوق):**
- المالك بعت `Download_template.xls` (قالب الرفع الجماعي من بوابة J&T): **26 محافظة و237
  مدينة** بأسماء J&T، ومفيش قايمة للمنطقة. **قرار المالك: «الخانة التالتة مطلوبة بس
  عادي نكتب فيها أي حاجة، ملهاش اسطمبة أصلاً»** → المنطقة بقت **نص حر مطلوب** (≤ 60
  حرف من التوثيق)، والمحافظة/المدينة اختيار من `jt_pca`.
- ✅ migration `jt_pca_area_free_text` (اتجرّبت بترانزاكشن راجعة): `area` default `''`
  + 237 صف من القالب (`raw.source=portal_template_2023`) + شيل الصف اليدوي بتاع
  المرسل. ⚠️ القالب من أكتوبر 2023 وناقص («الزقازيق» مش فيه) — مدينة ناقصة = رفض صريح.
- ✅ `jt-ship` **v4** (فحص `jt_pca` على المحافظة/المدينة بس + `area_too_long`) —
  **اتأكد حيّ** بنداءين diag على `17170`: مدينة وهمية → `422 address_not_in_pca` ·
  `الجيزة` / `مدينة السادس من أكتوبر` / «الحي السابع» → عدّى كل الحراسات ووقف عند
  `423 production_disabled`. والقيم دي **اتحفظت على الأوردر** (`ship_*`) فالنافذة هتفتح متعبّية.
- ✅ **v57**: نافذة الشحن — المنطقة `<input>` + `datalist` (اقتراحات من صفوف
  `jt_pca` اللي فيها `area` بس) · فاضية = رفض قبل الإرسال · `test-jt-ship.mjs` بقى 27.
- ✅ أسرار الـSandbox في الـVault (`jt_sbx_*`) — `query` شغّال، و`online/pca`
  مقفول على الـSandbox كمان (`145003012`). الأسماء عند J&T **عربي** (اتأكد من رد الـSandbox).
- 🔴 **أول ضغطة حقيقية اترفضت من J&T (21 سبتمبر 01:23 UTC):** v57 اترفعت، المفتاح
  اتفتح، المالك داس «🚚 شحن J&T» على `17170` → `145003012 API account has no interface
  permissions` على `order/addOrder`. **مفيش شحنة اتعملت** والمفتاح رجع `false` فوراً.
  سكرين شوت «Interface Mgt.» كان بيقول Create Order مفعّل — **الشاشة مش دليل، الرد هو
  الدليل** (درس 24). القياس بعدها: `getOrders` · `trace` · `subscribe` · `getWaybillInfo` ·
  `printOrder` كلهم شغّالين على نفس الحساب، و`addOrder` بس اللي مرفوض.
  ✅ **والضابط:** نفس الحمولة بالحرف على الـSandbox عدّت (`UEG088902635684` · كود فرز
  `88,A01-67,`) — يعني المنطقة الحرة والأسماء والحقول كلها مقبولة عند J&T، والحاجز
  الوحيد صلاحية `order/addOrder` على حساب الإنتاج.
- ✅ **والصلاحية اتفتحت بعدها بساعات (21 سبتمبر ~12:45 UTC)** بعد ما المالك بعت
  الـpayload وكود الخطأ للـIT. **اتأكدت من غير ما نعمل شحنة** بـ`jt-lookup` v4
  action **`perm_probe`**: نداء `addOrder` بحمولة **ناقصة عمداً ومحفورة في الكود**
  (`txlogisticId` بس). المنطق: `145003012` بيرجع **قبل** أي تحقق من الحمولة، فأي كود
  تاني = اتفتحت. الرد بقى **`0 Operation type cannot be empty`** = خطأ تحقق، والاستعلام
  `getOrders command:1` على مرجع الفحص رجّع «مش موجود» فمفيش شحنة اتعملت.
  🔴 **ده الشكل المعتمد لأي فحص صلاحية بعد كده — مايتعملش بشحنة حقيقية على أوردر عميل.**
  ⚠️ `online/pca` · `cover` · `getComCost` · `getLocation` **لسه مقفولين** — تحسينات مش حواجز.
- ✅ **~~الفاضل اختيار الأوردر + الضغطة~~ — اتعمل: 5 شحنات حقيقية** (الجدول فوق).
  `jt_production_enabled='true'` **لسه مفتوح** — ده الوضع الصح دلوقتي (الشحن شغّال يدوي
  من اللوحة)، وقفله معناه إيقاف الشحن. ⚠️ `17170` مااتشحنش خالص (خرج لـ`bosta_assigned`
  من غير بوليصة) والمالك شحن أوردرات تانية بدله.

**آخر شغل اتعمل (20 سبتمبر ليلاً — go-live J&T، قرار المالك بتغيير النطاق):**
- المالك غيّر القيود: **مسموح النشر والـpush وتغيير الداتابيز** — الهدف شحنة J&T
  حقيقية واحدة من سهل + طباعة القالب المعتمد + استقبال التحديثات.
- ✅ **اتنشر على الحي**: migration `jt_shipments` (+ أغلفة RPC) · EFs `jt-ship` (v2) ·
  `jt-status` (v1) · `jt-lookup` (v2). **الفرونت (v56) في `app/` ولسه ماترفعش**.
- ✅ **بيانات المالك اتسجّلت (20 سبتمبر ليلاً):** حقول `addOrder` الخمسة من Postman
  (`EZ` · `04` · `ITN1` · `1` · `PP_PM`) في `platform_settings.jt_addorder_fields` —
  `serviceType` مكانش في الطلب الناجح فبقى **اختياري** (v2). المرسل على `tenants`:
  الاسم والتليفون والشارع بس — **`sender_prov/city/area` فاضيين عمداً** لحد
  `pca_sync` (مفيش تخمين لأسماء J&T)، و`jt-ship` بترفض `sender_incomplete` لحد ما يتملوا.
- ✅ **أسرار J&T اتسجّلت في Supabase Vault** (المالك بعتها في الشات — سكرين شوت
  «User configuration» + الباسورد) وبتتقرا عبر RPC `jt_secrets_v1` (service_role بس،
  migration `jt_vault_rpc`). الـEFs بقت `jt-ship` v3 · `jt-lookup` v3 · `jt-status` v2
  (البيئة أولاً ثم الـVault). `config` → `creds_production=true`.
  **الباسورد اتأكدت بضابط** على إنتاج J&T: توقيع غلط عمداً = `145003031`، كود عميل
  غلط = `145003080`، وتوقيعنا عدّى. وcallback موقّع بمفتاحنا على `jt-status` → `code 1`
  و`digest_ok=true` (صف تجريبي `UEG000000000001` في `jt_events`).
- 🔴 **حساب الإنتاج مالوش صلاحية على `online/pca` / `cover` / `getLocation` /
  `getComCost`** (`145003012`) — واتأكد من سكرين شوت «Interface Mgt.»: المفعّل هو
  Delivery Time Inquiry · Logistics track query · **Create Order** · Query Order ·
  Logistics track subscription · Waybill Model Query بس. ~~فـ`jt_pca` بيتملى يدوي~~ —
  **اتملى 21 سبتمبر** — ومساءً اتستبدل بالقايمة الرسمية الكاملة من J&T IT
  (267 مدينة بأكواد)، فـ`online/pca` بقى بلا لزمة أصلاً.
- ✅ **المرسل كامل** من سكرين شوت «Sender Info» في البوابة: `القاهرة` / `السلام` /
  `موقف بلبيس` (+ صف يدوي مطابق في `jt_pca`) · `shipping_provider='jt'` لعتبة ·
  **أوردر التجربة `17170`** — ⚠️ مدينته «القاهره» بس عنوانه **6 أكتوبر الحي السابع**
  (الجيزة عند J&T)، فمستني أسماء J&T للمستلم من البوابة قبل الشحن.
- ✅ **`dry_run` على `17170` عدّى** (المفتاح اتفتح لثواني واتقفل، والعنوان المؤقت
  اترجّع فاضي): الحمولة كاملة وموقّعة، ومفيش نداء راح لـJ&T.
- 🔒 **مفيش أي شحنة اتعملت**: `jt_production_enabled=false` — بيتفتح لحظة الشحنة الأولى.
- 📄 **كل المطلوب من المالك وخطوات التشغيل والرجوع في `tools/jt/GO-LIVE.md`** —
  ده المرجع للسيشن الجاية، مش الذاكرة.

**آخر شغل اتعمل (20 سبتمبر مساءً — سيشن J&T الأولى):**
- عميل J&T (`supabase/functions/_shared/jt.ts`) + أداة تحقق محلية `tools/jt/jt-cli.mjs` —
  مبني ومتعاير، **من غير نشر ولا push**. التفاصيل والقياسات الحية في قسم
  «سيشن J&T الأولى» جوّه قسم الانتقال.
- ✅ **`Switch1` في `Whatsapp_WEBHOOK` مفصول عمداً** (قرار المالك — بوسطة اتلغت):
  تأكيد الواتساب مابيعملش شحنة دلوقتي، **وهيرجع يشحن أوتوماتيك مع J&T**.
- ✅ **توثيق J&T المصري اتقرا كامل من هنا** (مضمّن في JS البوابة — من غير لوجين)
  ومحفوظ في `tools/jt/docs/` مع خلاصة العقود في `CONTRACTS.md`.
- ⚠️ **تصحيح لقياس غلط في نفس اليوم:** `BOSTA_WEBHOOK` **شغّال** (23,317 سطر
  بـ`Bosta API` آخرهم 16 سبتمبر) — استعلامي الأول كان بيتجاهل السجلات
  المتخزّنة كـstring. التفاصيل في قسم سيشن J&T.

**آخر شغل اتعمل (19–20 سبتمبر):**
- 4 تصنيفات محادثات جديدة + إصلاح فلتر كان أعمى عن أي محادثة أقدم من الـ200 المحمّلة.
- الأوردر اليدوي بينزل **مؤكد** — ✅ **اتأكد حيّ** (`W-10` · `W-11`).
- إلغاء المحاسبة: الواجهة اتقفلت والماكينة سابت (متشابكة في الـRLS).
- شريط فلاتر المحادثات بره العمود + شيل عنوان الصفحة.
- الرد الجاهز بصور: ✅ شغّال في الإنتاج (50 إرسالة بصورتين في 7 أيام).

**⏳ مستني تجربة حية واحدة بس:** تصنيف على محادثة **أقدم من يومين**
وتظهر في فلترها — دي الحالة اللي كانت مكسورة بالظبط.

**🔜 الشغل الكبير الجاي:** الانتقال من بوسطة لـ**J&T** — **بدأ 20 سبتمبر**
(الدفعة الأولى: العميل والتوقيع). الدفعة التانية (migration + `jt-ship`)
مستنية إجابات المالك المكتوبة آخر قسم «سيشن J&T الأولى». يبدأ من هناك.

---

## اللغة

**التواصل بالعربي المصري العامي.** المصطلحات التقنية والمعرّفات بالإنجليزي زي ما هي.

---

## إيه هو المشروع

منصة SaaS متعددة المستأجرين للتجار المصريين اللي بيبيعوا بالدفع عند الاستلام (COD).

بتأتمت: استقبال الأوردر → تأكيده بالواتساب بأزرار → بوليصة شحن تلقائية → تتبع → مخزون → أرباح → إشعارات تلجرام → صندوق محادثات واتساب.

**الوضع:** live وشغّالة بأوردرات حقيقية.

## 🔴 السيستم بقى خاص بـ3ataba وحدها — قرار المالك (19 سبتمبر)

> «السيستم كله خاص بينا احنا بس كـعتبة دوت كوم، ومش هيكون فيه **أبداً**
> دلوقتي أي تجار تانيين، ولا هنعلن عنه لأي شخص. ونظام المحاسبة على
> الأوردر المؤكد ده كمان خلاص اتلغى.»

**مفيش SaaS ومفيش محاسبة ومفيش تسويق.** والداتا بتأكد ده من قبل القرار
(اتقاس 19 سبتمبر):

| المتجر | أوردرات | آخر أوردر | محادثات |
|---|---|---|---|
| **3ataba** | 4,151 | **النهاردة** | 2,052 |
| trendose | 97 | **من 42 يوم** | 0 |
| الباقي (5) | 0–3 | 1.5–4 شهور | 0 |

يعني السيستم **فعلياً متجر واحد من 6 أسابيع**.

### 🔴 بس ماكينة المحاسبة **ماتتشالش** — وده مقصود

`app.wallet_depleted()` متشابكة في سياسات الـRLS بتاعة `orders` و
`wa_conversations` و`wa_messages` (المجموعتين — `*_same_tenant` و
`phase2_*`). شيلها = لمس أخطر حتة في السيستم مقابل **صفر فايدة**:
رصيد 3ataba **99,999,999,606 ج** و`is_lifetime=true`، يعني قفل النفاد
محتاج **133 مليار أوردر** عشان يولّع. الماكينة بتخصم من رصيد لا نهائي =
حسابات على ورق مالهاش أي أثر.

**اللي اتعمل بدلها (قرار المالك): الواجهة اتقفلت والماكينة سابت.**
- شارة المحفظة من التوب بار · زرار «المحفظة» من القايمة · سلَج `/billing`
  من الراوتر (وملفه اتمسح) — كلهم **اتشالوا**.
- `showPage('billing')` بترجّع للأوردرات — حارس للينك المباشر.
- **`billing.js` نفسه فاضل محمّل** لأن `walletStateCache` بتغذّي قفل
  النفاد في 6 ملفات (`detail.js` · `orders.js` · `merge.js` ·
  `billing-summary.js` · `table.js` · `inbox.js`).
- فيه 6 فحوص ومعايرة في `test-routing.mjs` بيمنعوا رجوعها في صمت.

### ⚠️ وإيه اللي بقى مالوش معنى في الملف ده

الأقسام اللي تحت مكتوبة على أساس SaaS متعدد المستأجرين. **ماتتشالش**
(فيها تشخيص حقيقي ودروس مدفوعة) بس تتقرا بالسياق ده:
- **«المبدأ المعماري الحاكم — Central workflows»**: لسه **صح تقنياً**
  وهو اللي خلّى `tenant_id` متشدّد في كل حتة — ودي اللي بتحمي الداتا
  من الخلط، مش بتحمي تجار مش موجودين. **متتكسرش.**
- **«الحاجز أمام العميل التاني»**: بنودها (تسريب تلجرام · الرقم المشترك)
  **نايمة** طالما مفيش تاجر تاني. 🔴 والقاعدة فاضلة: متفتحش
  `has_telegram_ops` لأي متجر تاني قبل الإصلاح.
- **درس 10 («الاختبار على تاجر واحد مضلل»)**: لسه صح كمبدأ، بس مفيش
  تاجر تاني نجرّب عليه — فالبديل الوحيد هو التجربة الحية عند المالك.

---

## 🔴 المبدأ المعماري الحاكم

> **Central workflows — مش نسخ لكل تاجر.**

كل workflow بيقرا هوية التاجر **ديناميكياً** وبيجيب بياناته من الداتابيز وقت التنفيذ.

**تاجر جديد = صف جديد في `tenants`. مش workflow جديد ولا نسخة ولا شغل يدوي.**

أي اقتراح بيكسر ده **مرفوض** — بيحوّل المنتج لخدمة يدوية.

---

## 🔴 قواعد أمان لازمة

1. **ممنوع تعديل آلي على وركفلوهات n8n الإنتاجية.** شخّص، واكتب الخطوات، وسيب التنفيذ اليدوي للمالك. الوركفلوهات فيها بيانات اعتماد حية **وبتخلق شحنات بفلوس حقيقية**.

2. **ممنوع تشغيل أو اختبار وركفلو الأوردرات المركزي** عبر أي أداة — بيخلق أوردرات حقيقية ويبعت رسايل واتساب حقيقية.

3. **مفيش أسرار في الكود ولا في الملفات دي.** المفاتيح مكانها متغيرات البيئة أو جدول إعدادات المنصة أو الـ vault.

4. **أي تعديل على الداتابيز يتجرّب الأول** بترانزاكشن بترجع:
```sql
DO $$ BEGIN
  -- التعديل والاختبار
  RAISE EXCEPTION 'النتيجة: %', var;  -- بيرجّع كل حاجة
END$$;
```
ده بيمنع كمان إرسال رسايل تلجرام حقيقية، لأن طلبات الشبكة بتتحط في طابور جوّه الترانزاكشن.

---

## أسلوب الشغل (مهم جداً)

- **افهم قبل ما تصلح.** التشخيص وشرح السبب الجذري **قبل** أي حل. المالك عايز يفهم مش بس يتصلح.
- **اتأكد من الحالة الحية مش من الذاكرة.** اقرا الداتابيز/الوركفلو/الملف قبل ما تفترض. الورق ممكن يكون قديم.
- **ماتفترضش دلالة API — اتحقق.** (اتلسعنا: افترضنا إن نداء POST بيعمل شحنة والحقيقة إنه بحث. الافتراض ده أدى لقرار تشغيلي غلط.)
- **خطوة خطوة.** كل بند يتنفّذ ويتجرّب ويتأكد قبل اللي بعده. المالك بيجرّب كل تسليم بنفسه.
- **إصلاح للأمام بس.** البيانات التاريخية المصححة يدوي ماتتلمسش.
- **صفر حشو.** مفيش ملخصات ولا مقدمات ولا اعتذارات مطوّلة.
- **لما تغلط، قول غلطت بوضوح** واشرح السبب — من غير جلد ذات.

---

## هيكل الملفات

> اتفكّك في 29 يوليو 2026. لو لقيت أي وصف قديم بيقول "ملف واحد ضخم" — ده بقى غلط للوحة التاجر.

```
sahl/
├── CLAUDE.md          ← الملف ده — **المصدر الوحيد للحقيقة**
├── AGENTS.md          ← متولّد منه (Codex بيقراه) · tools/make-agents-md.py
├── check.sh           ← wrapper · المنطق في tools/check.py
├── tools/check.py     ← فاحص ما قبل النشر (مش بيتنشر)
├── tools/preview/     ← بنّاء معاينة أوفلاين بداتا وهمية للمالك (مش بيتنشر):
│                        `python3 tools/preview/build-preview.py <خرج>` بيجمّعها
│                        من app/ + overlay/ بمراسي بتفشل بصوت عالي لو الـmarkup
│                        اتغير. ⚠️ preview-stub.js لازم يتحدث مع أي عمود/جدول
│                        جديد بيدخل الواجهة (زي line_prices) — المعاينة بترندر
│                        منه مش من الداتابيز. تسليمات المالك زيبات مرقمة:
│                        sahl-app-vN (مطابقة لـapp/ بالبايت) + sahl-preview-vN.
│                        🔴 **آخر نسخة اتسلّمت: v60 (24 سبتمبر — تكلفة J&T + بحث + ردود محفوظة).**
│                        v59 (تحديث الحالات لحظياً) اترفعت 24 سبتمبر.
│                        حدّث الرقم ده مع كل تسليم — مفيش أي سجل تاني للأرقام
│                        في الريبو، وأنا خمّنت v45 (من آخر رقم مذكور في الملف)
│                        والحقيقة كانت v48، فالمالك صحّحني.
├── app/               ← لوحة التاجر → Cloudflare Pages
│   ├── index.html     ← markup بس (58KB)
│   ├── _headers       ← CSP مشدّدة
│   ├── js/            ← 50 موديول ES
│   │   ├── main.js    ← 466 سطر: initApp + موزّع data-act + قايمة التشغيل
│   │   ├── core/      ← dom · log · toast · constants · format · config · supabase · modal · veil · empty · skeleton · loaderr
│   │   ├── orders/    ← 15 موديول · state.js هو المالك الوحيد للحالة المشتركة
│   │   └── auth/ inbox/ stock/ finance/ billing/ analytics/ settings/ issues/ tour/ ui/
│   ├── css/           ← 23 ملف — الترتيب الرقمي ملزم (22-dark آخرهم عمداً: تجاوزات الليلي لازم تكسب)
│   ├── data/          ← بيانات ساكنة بتتحمّل بالـfetch (مش من الداتابيز):
│   │                    jt-areas.json — 6,464 منطقة J&T كاقتراحات datalist بس
│   └── fonts/ img/
├── admin/             ← لوحة السوبر أدمن (نطاق منفصل)
│   ├── index.html     ← لسه ملف واحد بجافاسكربت متضغوط — قرار متعمد
│   ├── _headers       ← CSP (script-src لسه فيها 'unsafe-inline')
│   └── img/
└── supabase/          ← مرآة للكود الحي على Supabase (مش بيتنشر من هنا)
    ├── functions/     ← مصدر الـEdge Functions
    └── migrations/    ← الـmigrations المطبّقة
```

⚠️ **`supabase/` مرآة مش مصدر.** النشر بيتم عبر MCP/CLI على المشروع الحي. الملفات هنا
عشان الكود يبقى متتبّع في git — **بس الحقيقة هي اللي على Supabase**. لو عدّلت هنا،
لازم تعمل `Deploy` كمان، والعكس.

✅ **المرآة اكتملت 14 سبتمبر — 9 دوال من 9.** اللي اتضاف: `wa-send` (v7) ·
`wa-followup` (v2) · `platform-admin` (v5) · `bosta-print-awb` (v2) ·
`wa-verify-number` (v1) · `tmp-kids-ref` (v2 — معطّلة بترد 410، تتمسح من الداشبورد).

🔴 **حد التحقق — يتقال صراحةً:** الملفات دي **منقولة** من المصدر الحي
المقروء عبر Supabase API — **مافيش مقارنة بالبايت** لأن مفيش `supabase` CLI
ولا توكن إدارة في بيئة الوكيل. اللي اتفحص فعلاً: الترميز والبنية والأسرار
والعقود الموثّقة (الفحص 14). **لقفل الفجوة نهائياً:**
`supabase functions download <slug>` من جهاز المالك ثم `git diff` — دقيقة شغل
وبتدي دليل بالبايت. **وقت أي تعديل على دالة، اقرا الحيّ الأول مش المرآة**
(نفس قاعدة «الوثيقة مش دليل» — درس 24).

**مفاتيح localStorage في لوحة التاجر:** `sb-auth` (جلسة Supabase) · `sb_ps` (حجم صفحة الجدول) · `sb_cols2` (مقاسات الأعمدة — `sb_cols` القديمة بتتطنش وتتمسح) · `sahl_setup_done_<tenantId>` (إقفال checklist الإعداد) · `sahl_side_collapsed` (طيّ السايدبار لأيقونات بس — ديسكتوب بس) · `sahl_dark` (الوضع الليلي — كلاس `dark` على `<html>` وتجاوزاته في `22-dark.css`، وألوان Chart.js بتتظبط من `applyDark` في main.js) · `sahl_tour_done_<uid>` (الجولة) · `sahl_qr_collapsed` (صف الردود المحفوظة مطوي بـ⚡ — الافتراضي مفتوح).

**شكل الشِل (1 أغسطس):** سايدبار يمين 236px فيه أقسام معنونة (التشغيل / الأعمال / النظام — الأخيرتين `admin-only`)، أيقونات SVG line icons بـ`stroke:currentColor` (مش إيموجي)، وكارت المستخدم مثبّت تحت (`sn-user` بيتعبّى في `auth.js` جنب `user-badge`). زرار الطيّ دايرة راكبة على حافة القايمة زي بوسطة — **أخو الـ`aside` في الـDOM مش جواه** (الـ`overflow-y:auto` كان هيقصّه) بـ`position:fixed` وبيتحرك مع الطيّ بمحدد `#sidenav.collapsed + .side-toggle`. شارة المستخدم في التوب بار مخفية على الديسكتوب ومرجوعة على الموبايل (≤768 السايدبار بيبقى شريط أفقي من غير عناوين ولا كارت ولا زرار طيّ).

### الفحص — قبل أي نشر، من غير استثناء

```bash
bash check.sh
```

بيفحص 14 حاجة. **الستة دول مايقدرش يمسكهم أي حاجة تانية** — لا `node --check` ولا المتصفح:
- **دوال محبوسة** جوه نطاق أضيق من اللي بتتنادى منه
- **أسماء حرة** — اسم مستخدم في موديول من غير تعريف ولا import
- **أسماء بره نطاقها** — اسم متعرّف كباراميتر في دالة وبيتنادى من دالة تانية.
  الفحص اللي فوق **أعمى عن دي** لأنه بيبني مجموعة تعريفات مسطّحة للملف كله.
- **إسناد لـbinding مستورد** — `import {x}; x = 1;` بيرمي TypeError وقت التشغيل
- **سكربتات CDN** — نسخة مثبّتة كاملة + integrity + crossorigin + مسار كامل في script-src
- **توازن وسوم الـmarkup** — قفلة `</div>` زيادة أو ناقصة. الـHTML parser
  بيصلّحها في صمت والتخطيط بيتفكّ من غير أي خطأ في أي مكان (درس 37)
- **صفحات الأقسام مطابقة لـ`index.html`** — `orders.html` وأخواتها نسخ بالبايت.
  لو `index.html` اتعدّلت من غير ما تتولّد تاني (`python3 tools/make-route-pages.py`)
  التاجر اللي داخل على `/chats` بيشوف **نسخة قديمة من اللوحة** من غير أي إشارة
- **`AGENTS.md` مطابق للمتولّد من `CLAUDE.md`** — Codex بيقرا `AGENTS.md`
  وClaude بيقرا `CLAUDE.md`، ونسختين بينحرفوا فيشتغل كل وكيل على حقيقة
  مختلفة (`python3 tools/make-agents-md.py`)
- **مرآة `supabase/functions/`** — ترميز سليم · أقواس متوازنة · **مفيش أسرار
  في الكود** (قاعدة أمان 3 — مكانتش متفحوصة في أي حتة قبل كده) · و**العقود
  الموثّقة موجودة** (15 عقد — `bad_reply_target`، `tenant_id` من الـJWT،
  الحمولة `order_id` بس...). بيمسك «نسخة قديمة اتحطت مكان الجديدة»
  وتشوّه الترميز (حصل في wa-inbox-ingest v7) — `tools/check-functions.py`

محتاج **Node** (linter محلي بس — مفيش build step) و**Python 3.8+**.

### هارنس المتصفح (اتبنى 4 أغسطس — لأي ميزة تفاعلية)

`check.sh` بيفحص الكود ساكن. أي ميزة **ضغط** أو **تغيير في مصدر البيانات** محتاجة تشغيل حقيقي كمان (دروس 15 · 31 · 33 · 35):

```bash
cd .. && python3 tools/spa-server.py 8899 app &   # ⚠️ مش http.server — اللوحة بقى
                                             # ليها لينك لكل قسم ومحتاجة SPA fallback
cd tools && npm i playwright                 # مرة واحدة
node test-day-click.mjs                       # ضغطة اليوم في الكالندر (17 فحص)
node test-cols.mjs                            # أعمدة ensureAllLoaded: مقارنة كل رقم قبل/بعد
node test-delta.mjs                           # نافذة مقارنة الفروق + تسميات الماليات
node test-mov-date.mjs                        # تاريخ حركات المخزون (علامات bidi)
node test-detail-popup.mjs                    # أقسام نافذة التفاصيل — نهاري وليلي
node test-carrier-naming.mjs                  # «بوسطة» مااتسابتش في أي نص حي
node test-staff.mjs                           # إضافة/تعطيل/حذف موظف (26 فحص)
node test-commission.mjs                      # عمولة الـupselling — الحساب على السيرفر
node test-settlement.mjs                      # التسويات + صفحة «عمولتي» + شارة 🔼
node test-ship.mjs                            # زرار الشحن: أوتوماتيك ويدوي (51 فحص)
node test-price.mjs                           # خانة سعر القطعة في محرر المنتجات
node test-variants.mjs                        # خصائص المنتجات: عيلات + توزيع + محررات (43 فحص)
node test-props.mjs                           # عرض props الويبهوك + النسخ + حارس «تكلفة المصنّع مش في الأرباح»
node test-routing.mjs                         # لينك لكل قسم + عنوان التاب + تاب المحادثات (38 فحص)
node test-wa-sender.mjs                       # اسم الموظف + الرد على رسالة (عرض وإرسال) + المصغّرة (48 فحص)
node test-wa-followup.mjs                     # قالب متابعة الأوردر: الاستبدال + المعاينة + الحمولة (18 فحص)
node test-wa-ctwa.mjs                         # شارة «جه من إعلان» + لينك الإعلان (30 فحص + 6 معايرات)
node test-wa-adfilter.mjs                     # فلتر «جه من إعلان» — استعلام سيرفر (20 فحص + 4 معايرات)
node test-wa-qr.mjs                           # الردود المحفوظة: زراير بضغطة + نافذة إدارة/ترتيب + حارس مسار الميديا (47 فحص + 7 معايرات)
node test-wa-neworder.mjs                     # «إنشاء طلب» يدوي من الشات (30 فحص + 3 معايرات)
node test-wa-start.mjs                        # «شات جديد» بقالب لرقم مكلّمناش (37 فحص + 4 معايرات)
node test-finance-shipping.mjs                # تكلفة الشحن بعد شيل الافتراضي 85 (10 فحص + معايرتين)
node test-jt-sign.mjs                         # عميل J&T: md5 + التوقيع التلاتي + الغلاف + حارس الإنتاج + تحقق الـcallback (57 فحص منهم 6 معايرات — من غير متصفح)
node test-jt-awb.mjs                          # Code 128 بتاع البوليصة = python-barcode بالحرف + فك zxing-cpp (5 فحوص — من غير متصفح)
node test-jt-ship.mjs                         # نافذة شحن J&T (المنطقة نص حر) + الطباعة المعتمدة 100×150 (27 فحص + معايرة)
node test-realtime-status.mjs                 # تحديث حالات الشحن في الجدول والنافذة لحظياً (16 فحص + 3 معايرات)
node test-jt-fee.mjs                          # سطر تكلفة J&T النهائية في التفاصيل — نهائي/مبدئي/فاتورة + للأدمن بس (10 فحص + 2 معايرة)
node test-wa-search.mjs                       # بحث المحادثات من السيرفر — الشات القديم بره الـ200 (18 فحص + 5 معايرات)
# ⚠️ `test-routing` لوحده محتاج **تلات سيرفرات**، وغير كده بيقع بأخطاء
#    مالهاش علاقة بالكود (اتلسعنا فيها):
#      python3 tools/spa-server.py 8901 app        ← الأساسي (مع fallback)
#      cd app && python3 -m http.server 8902       ← «استضافة مش داعمة» (مفيش
#                                                     تحويل .html — لازم http.server
#                                                     مش spa-server ولا حتى --strict)
# ⚠️ بعد أي تعديل على app/index.html:  python3 tools/make-route-pages.py
# ⚠️ وبعد أي تعديل على CLAUDE.md:        python3 tools/make-agents-md.py
```
الملفات في `tools/` (مش بتتنشر): `stub.js` + `test-*.mjs`.

**هوكات الستب** (كلها بتتقرا **وقت الاستعلام** مش وقت التحميل): `__MOVEMENTS` ·
`__CM_EVENTS` · `__CM_SETTLE` · `__CM_BAL` · `__UPSELL_IDS` · `__ROLE` · `__CM_ON` · `__FN` ·
`__STOCK` (منتجات المخزون — من غيره الصف الافتراضي القديم) · `__RPC_HOOK` (دالة بتحاكي
أثر RPC زي `transfer_stock` — **مش** `__RPC` لأن الاختبارات القديمة بتستخدمه كمصفوفة تسجيل) ·
`__PROPS` (خريطة `{order_id:{manufacturer_note, manufacturer_cost, var}}`) ·
`__WA_CONVOS`/`__WA_MSGS` (صندوق المحادثات) · `__MEDIA` (روابط التخزين الموقّعة) ·
`__TENANT` (إعدادات المتجر — بتتدمج في `v_my_tenant`، والافتراضي فاضي عشان الاختبارات القديمة) ·
`__QR` (الردود الجاهزة) · `__UPLOAD_OK` + `__UPLOADS` (رفع الملفات — الافتراضي فشل زي ما كان) ·
`__START_TPLS` (قوالب بدء الشات — الافتراضي فاضي فالزرار مايبانش زي التاجر اللي مالوش قالب).
والستب بقى **بيقطع `wa_conversations` زي PostgREST** (`or` + `order` + `limit`)
و**بيرجّع نسخ مش نفس المراجع** — من غير ده فلتر السيرفر وفلتر الذاكرة بيدّوا نفس
النتيجة، وأي خاصية الكود بيكتبها على صف بتظهر في نتيجة أي استعلام تاني (اتلسعنا:
علامة اتحطت على صف من جلب، فاختفت محادثتين من جلب تاني). و`insert().select()
.single()` بقى يرجّع **الصف المُدخل** مش `rows[0]` (صف قديم عشوائي من نفس الجدول).
والكتابات بتتسجل في `__calls` بحمولتها (`payload`) — الاختبار يقدر يتأكد من اللي اتبعت فعلاً.
الحقن على `DOMContentLoaded` **بيوصل متأخر** — موديولات ES بتتنفّذ قبله، فكل
الاستعلامات بتخرج والصفحة بترسم قبل ما الحدث يولّع (اتلسعنا فيها في شارة الـupsell).

- **الستب بيتحقن بـ`addInitScript` قبل أي سكربت** — بيدخل أدمن حقيقي عبر `getSession` من غير أي طلب شبكة، **وبيقطع الأعمدة زي PostgREST** (من غير ده تضييق أي `select` بيعدّي وهو ناقص).
- لازم يعلّم `sahl_tour_done_<uid>` وإلا كارت الجولة بيحجب كل الضغطات.
- المتصفح المثبّت: `executablePath: '/opt/pw-browsers/chromium'`.
- **وكل هارنس يتعاير بحقن الباج اللي اتعمل عشانه** قبل ما يتصدّق (درس 14).
⚠️ `python3` على ويندوز ستَب بتاع Microsoft Store — `check.sh` بيلاقي المفسّر الصح لوحده.

### قواعد ملزمة في الفرونت

1. **ترتيب ملفات CSS عقد.** البادئة الرقمية (`00-` … `21-`) مش تزويق: `.sc` معرّفة 4 مرات بخصوصية متساوية، `.fbar` مرتين، `.bulkbar` 3. ترتيب غلط = كروت الإحصاء تفقد شريطها والقايمة المنسدلة تظهر ورا الجدول — **من غير أي خطأ في الكونسول**.
2. **ترتيب السكربتات ملزم.** الـCDN الكلاسيكية (supabase-js · chart.js) **قبل** `<script type="module">`. أي `defer` أو نقل تحت = `sb` تفضل `null` والتاجر يشوف لوجين واقف.
3. **ممنوع أي inline script أو `onclick` في لوحة التاجر** — `script-src` مافيهاش `'unsafe-inline'`، المتصفح هيرفضه **في صمت**. الأحداث بـ`data-act` والموزّع في أول `main.js`.
4. **`style-src` لازم تفضل فيها `'unsafe-inline'`** — فيه ميّات `style="..."` متولّدة. خلطها مع `script-src` = الداشبورد يفقد التنسيق كله.
5. **كل حالة ليها كاتب واحد.** تحت ES modules الـbinding المستورد **للقراءة بس**. لو نقلت دالة لموديول، الحالة اللي بتكتب فيها تتنقل معاها. في `orders/` المالك هو `state.js` وأي كتابة من بره بتعدّي على setter.
6. **أي سكربت CDN لازم: نسخة مثبّتة كاملة + `integrity` + `crossorigin` + مساره الكامل في `script-src`.** من غير `crossorigin` المتصفح **بيتجاهل** الـ`integrity` في صمت. تغيير نسخة = تحديث الـhash في اللوحتين والـ`_headers` مع بعض.

---

## الأدوات (MCP)

| الأداة | الاستخدام |
|---|---|
| **Supabase** | التطوير والتشخيص الأساسي — استعلامات، migrations، لوجات، Edge Functions |
| **n8n** | **تشخيص بس** — اقرا الوركفلو والتنفيذات. ممنوع التعديل. |
| **Notion** | ذاكرة المشروع طويلة المدى — **يتحدّث آخر كل جلسة** |

معرّف مشروع Supabase موجود في ملفات الفرونت (مش سر — الـ anon key عام بطبيعته).

---

## Notion = مصدر الحقيقة

فيه صفحة واحدة فيها تاريخ المشروع كامل بالأقسام المرقّمة.

- **أول كل جلسة:** اقراها لو محتاج سياق تاريخي
- **آخر كل جلسة:** حدّثها بالشغل الجديد والقرارات

**التحديث بيتم بمراسي نصية مطابقة حرف بحرف** — اقرا الصفحة قبل ما تكتب.

---

## النشر

🔴 **لوحة التاجر على Cloudflare Worker (Static Assets) — مش Pages.** الملف ده كان
مكتوب فيه «Pages» لشهور وده **غلط** — اتكشف 3 سبتمبر من سكرين شوت المالك:
شاشة الرفع عنوانها «Upload static files to update your **Worker**» (درس 24: الوثيقة
مش دليل). الفرق مش شكلي:
- **`_headers` بيشتغل** على الاتنين — اتأكد حيّ: الـCSP نازلة فعلاً في هيدرز
  `app.sahlgedan.com`.
- ⚠️ **`_redirects` بإعادة كتابة `200` (SPA fallback) ميزة Pages** — على Workers
  التوثيق بيقول الـrewrites بغير أكواد التحويل **مش مدعومة**. الطريقة الرسمية
  على Workers هي `assets.not_found_handling: "single-page-application"` في
  إعدادات الـWorker/wrangler. **يتأكد سلوكياً بـ`curl /orders` بعد أي نشر** —
  الشكل مايثبتش (درس 26).
- **رافع الداشبورد بيقول صراحةً**: «This uploader currently only supports static
  assets (HTML, CSS, and JS files). We recommend using wrangler deploy» —
  والمشروع فيه `fonts/` و`img/`.
- 🔴 **الضغط على Deploy والملفات لسه بترفع = فشل صامت** (الزرار بيرمد ويرجع أزرق
  من غير أي رسالة). `js/` لوحده 517 KB. **استنى كل السطور تخلص السبنر الأول.**
  اتكشفت 3 سبتمبر: v35 اتضغط عليها Deploy وماوصلتش، وv34 فضلت على الهواء.

لوحة الأدمن مشروع منفصل.

**⚠️ الرفعة بتستبدل النشرة كلها.** مفيش رفع ملف واحد — بترفع الفولدر وهو بيبقى النشرة الجديدة.

**⚠️ `app/` بقى 80 ملف — كله أو مفيش.** لو الـHTML وصل والـ`js/` أو `css/` لأ، التاجر بيشوف صفحة لوجين شكلها سليم وزرار الدخول مايعملش حاجة، والخطأ يظهر في تاب الـNetwork بس.

**⚠️ الداتابيز والفرونت لازم يتنشروا مع بعض.** حصل قبل كده إن تعديل داتابيز اتنشر والفرونت لأ، فحاجات وقفت تشتغل من غير سبب واضح.

---

## دروس اتدفع تمنها (اقراها قبل ما تشتغل على n8n)

1. **n8n بيحفظ من غير ما ينشر.** وقعنا فيها مرتين في يوم واحد — التعديل يبان شغال في المحرر والعملاء على النسخة القديمة. **الحكم من `activeVersionId` مش من شكل النود.** بعد كل تعديل → Publish فوراً.

2. **إضافة نود في نص مسار شغّال بتكسر كل اللي بعدها** — لأنها بتغيّر `$json`. **الحل: نادي النودز بأسمائها الصريحة** `$('اسم النود').item.json...`

3. **إعادة تسمية نود = كسر صامت.** 14 نود بقت تنادي اسم مش موجود، ومسار التأكيد وقف في الإنتاج من غير ما حد ياخد باله.

4. **أي نود تقع بتقتل كل الفروع التانية من نفس التريجر** — حتى اللي مالهاش علاقة. والترتيب بيتحدد **بموقع النود على الكانفس**. ده أوقف صندوق المحادثات 3.5 ساعة.

5. **نود بترجّع فاضي = الفلو يقف صامت** والتنفيذ يتسجّل "ناجح". فعّل `Always Output Data` على نودز البحث.

6. **الفلترة والترتيب مرتبطين.** فيو بترتيب معكوس عن توقّع الـ `Limit` = النظام يمسك **العنصر الغلط في صمت**. مفيش خطأ، بس النتيجة غلط. **أخطر نوع باج.**

7. **`maybeSingle()` بترمي خطأ لو لقت صفين** والكود اللي بيقرا النتيجة كـ "مفيش" بيخرج ساكت. **الخروج الصامت خبّى مشكلة 3.5 ساعة.** فرّق دايماً بين "مفيش" و"أكتر من واحد".

8. **الباجات النايمة بتصحى من تغييرات إعدادات مش كود.** تغيير باقة تاجر فعّل باج كان نايم من شهور.

9. **أي تنبيه لازم يكون ليه طريقة إقفال** — وإلا بيتعوّد الناس يتجاهلوه ويفقد قيمته.

10. **الاختبار على تاجر واحد مضلل.** حاجة "شغالة" ممكن تكون شغالة بالصدفة لأن غلطتين بيلغوا بعض. **جرّب على تاجرين.**

11. **الـ JSON Body في نود HTTP ماينفعش فيه Enter حقيقي** — لازم `\n`. الخطأ: `Bad control character`.

12. **الفيوهات المعمولة بـ `SELECT *`** بتتفك لأعمدة ثابتة وقت الإنشاء. **أي عمود جديد على الجدول لازم يتبعه إعادة إنشاء الفيو.**

13. **الفيوهات من غير `security_invoker=on`** بتشتغل بصلاحيات المالك وبتتخطى RLS.
    🔴 **بس فيه استثناء مقرَّر — `v_my_tenant` وإخواتها اللي بتقرا من `tenants`:**
    دي **DEFINER عن قصد** بفلتر عزل داخلي صريح (`WHERE id = app.current_tenant_id()`)
    وتغطية أسرار بـ`CASE WHEN is_tenant_admin()`. السبب في درس 39 تحت —
    وحطّ `security_invoker` عليها **بيقفل دخول اللوحة على كل التجار**.

---

## دروس الفرونت (من جلسة التفكيك — 29 يوليو 2026)

14. **الفحص اللي مايتجرّبش = مفيش فحص.** `check.sh` فضل **ميت** فترة (exit 49 على `python3` ستَب) والتعديلات اتنشرت من غير أي فحص. **أي فحص جديد لازم يتعاير بحقن الباج اللي اتعمل عشانه** والتأكد إنه بيمسكه.

15. **فحص تحميل الصفحة مايثبتش حاجة.** أخطر باج في الجلسة (`tour.js` بيكتب في `tourActive` المستوردة) **عدّى من اختبار المتصفح** لأن `tourStart` بترجع بدري لما المستخدم مش أدمن. كان هيفشل بس عند التاجر أول ما يفتح الجولة.

16. **`typeof X === 'function'` بيخبّي الأعطال.** الحارس بيخلّي الدالة المفقودة تفشل في صمت — الزرار مايعملش حاجة ومفيش أثر في أي مكان. لقينا واحد بينادي دالة **مش موجودة في المشروع خالص**.

17. **التحليل بالمسافة البادئة خطر.** كتلة طباعة البوليصة كانت مزوّدة مستوى indentation (بقايا nesting قديم) فأداة اللفّ ابتلعتها وحبست 3 دوال. وعدّ الأقواس مش بديل — بينحرف مع الـregex والكومنتات.

18. **`grep` مابيفرقش بين الكود والسترينج.** فحص الـIDs القديم كان بيعدّ `id="x"` جوه سترينج جافاسكربت كأنه id حقيقي (302 بدل 258).

19. **الدورات بين موديولات ES قانونية** طالما مفيش كود بيتنفّذ وقت التقييم. ده اللي خلّى التقسيم ممكن — الجراف فيه دورات طبيعية (الجولة بتنادي كل حاجة، و`showPage` بتنادي كل loader وكلهم بينادوها).

20. **الـCSP بتفشل في صمت.** `img-src` مكانش فيها `blob:` فمعاينة صورة المرفق في الواتساب كانت **متحجوبة على الهواء** من غير أي رسالة. ولما تجرّب CSP، **لازم تجرّب أصل مرفوض كمان** — وإلا "مفيش انتهاكات" ممكن تكون معناها "السياسة مش شغالة".


21. **الفحص الأمني اللي الشبكة فيه واقعة = فحص فاضي.** أول نسخة من اختبار الـCSP قالت "المسار التاني اتحجب ✓" والحقيقة إن الـCDN مكانش واصل أصلاً (`ERR_CONNECTION_RESET`). **أي اختبار "اتحجب" لازم يبقى معاه ضابط بيثبت إن الطريق مفتوح** — هنا: نفس الهوست بيخدم المسار المسموح بنجاح، فالحجب يبقى من السياسة مش من الشبكة.

22. **الفاحص نفسه ممكن يبقى أعمى بنفس طريقة الكود.** `check_free_identifiers` بيبني مجموعة تعريفات **مسطّحة** للملف، فاسم موجود كباراميتر في أي دالة بيغطّي على نداء حر في دالة تانية. ده خبّى `val` (مش مستوردة خالص في `orders.js`) لشهور. **ولما اتعمل الفحص البديل، نسخته الأولى وقعت في نفس العمى بالظبط** — لأن سطر `function f(val){` عمقه صفر فالباراميتر اتجمّع في نطاق الموديول تاني. **المعايرة لازم تعيد إنتاج شكل الباج الأصلي بالحرف، مش تقريب ليه.**

23. **`swallow()` بيخبّي زي `typeof` بالظبط.** نداء `updateRevenueStats` الوحيد متلفوف `try{…}catch(e){swallow(…)}` — فـReferenceError دايم كان بيتبلع وكروت الإيرادات في الجولة تفضل فاضية. أي `swallow` حواليه نداء لدالة من موديول تاني = مرشّح لباج نايم.

    **الجرد اتعمل 31 يوليو — 42 موقع، صفر باج نايم.** الجرد الساكن لوحده مايكفيش (باج `val` كان هيعدّي منه — الدالة موجودة، الاستيراد هو اللي كان ناقص). الطريقة اللي اشتغلت: **نسخة متلبّسة من `core/log.js` بتسجّل كل نداء `swallow` بالخطأ الحقيقي**، وتشغيل اللوحة في Chromium بعميل Supabase مزيّف عبر `setSb()` ودخول أدمن حقيقي عبر `fetchProfileAndEnter` — لأن `tourStart` بترجع بدري لغير الأدمن (درس 15). اتمشى على: الجولة كاملة (21 خطوة + الاسترجاع) · الإعدادات · الجدول · تفاصيل الأوردر · الفلترة · الصفحات الثمانية. **صفر `swallow` ولّع وصفر خطأ غير متبلّع.** والهارنس اتعاير بإعادة حقن باج `val` ومسكه فوراً.

    **10 مواقع مااتغطتش** — محتاجة بيئة حية مش ستوب: `orders/awb.js` (2 — طباعة البوالص بتضرب Edge Function بفلوس حقيقية) · `inbox/inbox.js` (3 — إشعارات المتصفح ومرفقات الواتساب) · `auth/auth.js` (3 — الخروج والتعليق) · `billing/billing.js` (1) · `main.js` (1).

27. **الـinline style بيكسب أي stylesheet.** محاولتين إصلاح منطقيتين للتوب بار فشلوا بنفس الشكل قبل ما تشخيص خام بالهندسة المحسوبة يكشف `display:flex` inline على الحاوية بيبطّل أي قاعدة ريسبونسف. القاعدة: بعد تاني محاولة فاشلة، بطّل تخمين وهات الـcomputed styles الفعلية. (الحاوية بقت `.tleft` في 05-shell.)

28. **grep بيكدب مع العربي.** `grep '[٠-٩]'` بيمسك حروف عربية مش أرقام (نطاق بايتات مش نطاق حروف)، وجرد `تعذّر` الأول فوّت 5 مواقع. أي مسح نصي عربي يبقى ببايثون. والمسح الآلي بعد "الانتهاء" هو اللي بيمسك الفوّتات — 27 رسالة فصحى اتمصّرت والمسح النهائي لقى 5 زيادة.

29. **خط مونو على نص عربي = حروف مفككة متباعدة.** JetBrains Mono مافيهوش جليفات عربية، فالمتصفح بيرقّع حرف-حرف من fallback والوصلات بتتكسر ("الـمـتـكـا..."). حصلت مرتين: `.notrack` زمان، وكروت "أفضل منتج" في إحصائيات الأداء (`.perf-mini b` mono وجواها أسماء منتجات). أي عنصر ممكن يعرض نص عربي لازم Cairo — المونو للأرقام والمعرّفات بس.

30. **"الأرقام مش تحت العناوين" — `text-align:start` بيتحسب من اتجاه الخلية نفسها مش الجدول.** `td.mn` عندها `direction:ltr` (عشان الأرقام والتواريخ تترسم صح)، فالـ`start` الافتراضي بقى **شمال** بينما العناوين `text-align:right` — الرقم رسى بعيد عن عنوانه 200px في الجداول العريضة، من غير أي خطأ في أي مكان. الحل: `text-align:right` صريح جنب أي `direction:ltr`. الإثبات كان بقياس مرساة النص (Range rect ضد rect الخلية) مش بالعين — العين على thumbnail بتغلط (قريت مكان الأيقونات معكوس والهندسة صححتني).

31. **الضغط البرمجي أعمى عن طبقات الرسم.** زرار طيّ السايدبار عدّى من الاختبار (`offsetParent` موجود + `tg.click()` بيشتغل) وكان **مدفون بالكامل تحت اللوجو** في الإنتاج — `.tlogo` عندها `position:relative` وجاية بعده في الـDOM فبترسم فوقه. `el.click()` الجافاسكربتية بتشتغل حتى لو العنصر مغطي 100%. الدليل الحقيقي إن العنصر شايفه حد: `elementFromPoint` في نص العنصر لازم ترجّعه هو نفسه + ضغطة Playwright حقيقية (hit-tested). المالك هو اللي مسكها من سكرين شوت.

32. **`select('*')` بيشيل حِمل مالوش لازمة — وفي جدول فيه JSONB الحِمل بيبقى الأغلبية.** الماليات كانت بتسحب 2,973 kB لـ2,609 أوردر، **1,605 منها `status_log` لوحده** — عمود الفرونت مابيقراهوش من `all` أصلاً. القاعدة: أي `select('*')` على جدول فيه JSONB = مرشّح مباشر للتضييق. وقيس الأول (`pg_column_size` جروب بالعمود) قبل ما تخمّن السبب.

33. **تضييق الأعمدة خطر صامت — والاختبار اللي مايقطعش الأعمدة مابيثبتش حاجة.** عمود ناقص من `ALL_COLS` بيرجع `undefined` فالرقم يطلع غلط من غير أي خطأ. الجرد بالقراءة **مايكفيش**. اللي اشتغل: الستب بقى **بيقطع الأعمدة زي PostgREST بالظبط**، والاختبار بيرندر الماليات والإحصائيات (منتجات/منصات/كالندر + الكروت) **مرتين** — بالصف الكامل وبالمقطوع — ويقارن **كل رقم مرندَر**. والهارنس اتعاير بشيل 3 أعمدة واحد ورا التاني (`real_shipping_fee` · `inventory_cost_snapshot` · `platform`) والتلاتة اتمسكوا. **ستب بيرجّع الصف كامل مهما طلب الكود = الاختبار عمره ما هيمسك عمود ناقص.**

34. **فحص النتيجة وفحص الحساب مش نفس الحاجة** (امتداد لدرس 6). في معايرة ضغطة الكالندر حقنّا نطاق تاريخ مفتوح لآخر الزمن — وفحص «الجدول عرض أوردرات اليوم ده» **عدّى**، لأن الداتا صدفةً طلعت صح مع النطاق الغلط. اللي مسكه فحص **طول النطاق** المستقل. لما تختبر فلترة أو مدى، اختبر **الحدود اللي خرجت فعلاً** مش بس الصفوف اللي رجعت.

35. **`el.click()` بينجح على عنصر مدفون — والـhit-test هو الدليل.** (تطبيق عملي لدرس 31 بقى معياراً: أي ميزة ضغط بتتأكد بـ`elementFromPoint` في نص العنصر + ضغطة Playwright حقيقية، على الديسكتوب والموبايل.) والمعايرة بحقن شكل درس 31 بالحرف (عنصر جاي بعده في الـDOM بيرسم فوقه) اتمسكت فوراً. **ملحوظة عملية:** لو Playwright رفض يضغط وقال `intercepts pointer events` — ده مش عطل في الاختبار، ده الـhit-test شغال صح (كارت ترحيب الجولة كان بيحجب، والحل تعليم `sahl_tour_done_<uid>` في الستب).

36. **الوعد اللي التاجر مايستحملوش يترفض حتى لو صح.** إخفاء نسبة التسليم لحد ما اليوم يخلص كان **صح منطقياً** ومنع باج حقيقي (شحنة من 10 = «100%» وتلوين ممتاز)، وبعد ما اتنشر المالك رفضه: يوم فيه 19 من 25 محسومين كان بيظهر «—». الحل مكانش الرجوع الكامل ولا التمسك — **الرقم يظهر والسياق يظهر معاه** (⏳ N لسه ماتقفلتش). لما تخفي معلومة عشان الدقة، اسأل: التاجر هيفهم الإخفاء ولا هيحسّه عطل؟

37. **الـHTML parser بيبلع الغلط ويكمل — فالتخطيط بيتفكّ من غير أي إشارة.** قفلتين `</div>` زيادة في تبويب العمولات قفلوا `#page-finance` و`.appcol` بدري، فصفحات الإحصائيات والمحفظة والإعدادات خرجوا **بره عمود التخطيط** ونزلوا تحت الشاشة على الموبايل. مفيش خطأ في الكونسول، `node --check` مايشوفهاش (مش جافاسكربت)، و`check.sh` كان بيقول «كله تمام». اللي مسكها: هارنس الكالندر فشل على الموبايل بـ`elementFromPoint` رجّعت null — والسبب الحقيقي طلع إن الخلية عند y=1155 والشاشة 844، يعني **تحت الطية مش مدفونة**. الفرق بين التشخيصين اتحسم **بمقارنة الهندسة ضد HEAD** (نفس الاختبار على نسخة الريبو = 17/17)، مش بالتخمين. الفحص الحادي عشر في `check.py` بقى بيمسكها، واتعاير بحقن الباج الأصلي بالحرف + قفلة ناقصة + تشابك غلط + ضابط على الملف السليم.

38. **الفحص القديم بيبقى غلط لما الميزة تكبر — والفشل ده معلومة مش إزعاج.** بعد ما كارت «ملغية» بقى «اتصرف» و«الرصيد المستحق»، `test-commission` فشل في فحصين. الإغراء إنك تعدّل التوقّع بسرعة؛ الصح إنك تسأل الأول **مين مصدر كل رقم**: المعلّق والمستحق من **جدول الحركات**، والمتصرّف والرصيد من **الفيو**. التقسيمة دي اتكتبت في الاختبار كتعليق — عشان اللي بعدنا مايوحّدهاش بالغلط.

39. **قاعدة صح + سياق غلط = عطل إنتاجي.** حطيت `security_invoker = on` على
    `v_my_tenant` وأنا بضيف عمود — تطبيقاً لدرس 13 حرفياً. النتيجة: **مفيش تاجر
    قدر يدخل اللوحة**. السبب إن `authenticated` عنده SELECT **بالأعمدة** على
    `tenants` والأسرار مسحوبة منه، وتحت invoker الصلاحية بتتفحص على **كل عمود
    مذكور في تعريف الفيو** — حتى لو الاستعلام مابيطلبهوش — فأي قراءة بترجع
    `permission denied for table tenants` و`loadTenantAndEnter` بتفشل. والمشروع
    كان **خبط في نفس الحاجة قبل كده وصلّحها**: migration
    `fix_invoker_views_after_tenants_revoke` (26 يوليو) حوّلت 3 فيوهات من invoker
    لـDEFINER بنفس السبب بالحرف. **الدرس مش «اقرا الدروس» — الدرس إن أي قاعدة
    عامة لازم تتأكد على الحالة الحية قبل ما تتطبّق على شكل قايم**، والقايم ده
    كان فيه سببه مكتوب في تاريخ الـmigrations.

    **وأخطر جزء: أداة التحقق نفسها كانت مكسورة وكادت تخبّي العطل.** أول انتحال
    لموظف رجّع «التوكن اتحجب ✅» — وده كان **صح بالصدفة**: القراءة كلها كانت
    بترفض. اللي كشفها **ضابط** (درس 21): `SELECT slug` — عمود ممنوح أكيد —
    وقع هو كمان. من غير الضابط كنت هأقفل الجلسة وأنا مطمّن. **أي انتحال
    لازم يبقى معاه قراءة لازم تنجح، وإلا «اتحجب» معناها «مفيش طريق» مش «فيه حارس».**

40. **`JSON.parse` بيفسد معرّفات ميتا لو جت كأرقام.** معرّفات الإعلانات 17-18 خانة — أكبر من `Number.MAX_SAFE_INTEGER`. في اختبار `referral` بعتنا `...0001` ورجع `...0000`: **الخانة الأخيرة اتغيرت في صمت**. ميتا بتبعت المعرّفات دي كـ strings فمفيش مشكلة دلوقتي، لكن أي معرّف جاي كرقم في أي `webhook` **لازم يتشك عليه**. والدرس الأعم: **جرّب بالنوع الغلط كمان** — لو كنا جرّبنا بـ string بس، الباج ده كان هيعدي من غير ما حد ياخد باله.

41. **`REVOKE` على مستوى العمود بيعدّي في صمت لو المنحة على مستوى الجدول.**
    عشان تقفل عمود على `authenticated` لازم الجدول نفسه **مايكونش** ممنوح له
    الصلاحية دي — غير كده الـ`REVOKE (عمود)` بينجح من غير ما يغيّر حاجة،
    لأن مفيش منحة عمود أصلاً يشيلها. `wa_messages` مقفول صح (`rdm` + منح
    بالعمود) و`wa_conversations` مفتوح (`arwdm` + صفر منح بالعمود) — والفرق
    ده **مش قرار حد**، هو أثر تاريخي. **فقبل ما تقفل عمود: اقرا
    `pg_class.relacl` و`pg_attribute.attacl` الأول**، والإصلاح الصح هو
    REVOKE على الجدول + GRANT بالأعمدة الشرعية. وبعد أي قفل، **أعد القياس
    بانتحال ومعاك ضابط** — «نجح الـREVOKE» مش دليل إن الكتابة اتمنعت.

43. **`GRANT a, b (أعمدة)` بيمنح `a` على الجدول كله — قايمة الأعمدة
    بتلزق بالصلاحية اللي قبلها مباشرةً بس.** كتبت
    `GRANT INSERT, UPDATE (<15 عمود>)` وأنا قافل أعمدة `ctwa_*`، فالنتيجة
    كانت UPDATE بالأعمدة (صح) + **INSERT على مستوى الجدول** (غلط). التزوير
    اتقفل في مسار واحد وفضل مفتوح في التاني. الصح:
    `GRANT INSERT (أعمدة), UPDATE (أعمدة)` — أو أمر لكل صلاحية لوحدها.
    **والأخطر إني كتبت البند «اتقفلت» وهو نصه مقفول**، لأني قست الـUPDATE
    بس ومافكرتش في الـINSERT. درس 41 قال «أعد القياس بانتحال ومعاك ضابط» —
    عملت كده بس **على مسار واحد**. القاعدة الكاملة: **قيس كل مسار كتابة
    يوصل للعمود** (INSERT · UPDATE · وأي RPC بصلاحية definer)، مش المسار
    اللي في بالك.

44. **الحقل اللي اسمه صح مش بالضرورة محتواه صح — وده مايتعرفش غير من داتا
    حقيقية.** بنينا شارة «جه من إعلان» على `referral.headline` — الاسم
    بيقول «عنوان الإعلان». أول التقاط حي كشف إن ميتا بتحط فيه **اسم
    الصفحة**: «3ataba.com — عتبة دوت كوم» على إعلان اسمه KitchenOrganizer،
    يعني نفس النص على كل إعلانات المتجر. الشارة كانت بتترسم صح 100%
    وبتقول صفر معلومة، والمالك هو اللي مسكها («مش عارف من أنهي إعلان»).
    وقت البناء كان فيه **صفر التقاط**، فالسؤال الصح مكانش «نعرض إيه؟» بل
    **«نستنى أول رسالة حقيقية ونشوف الحقول جاية بإيه»** — تأجيل قرار
    العرض كان أرخص من بنائه على اسم حقل. (وده امتداد لـ«ماتفترضش دلالة
    API» — بس على **محتوى** الحقل مش على وجوده.)

45. **`js_strip` في `check.py` كانت بتقرا `return /regex/` كأنها قسمة.**
    الفاحص بيعرف الـregex literals بس بشرط `prev in ("", "op")` — و`return`
    بتخلي `prev="val"` لأن آخر حرف فيها `n`. فـ`return /^https?:\/\//i`
    خلّت `https` تتقري **اسم حر** والفحص طلّع خطأ على كود سليم تماماً.
    الإصلاح: `_regex_after_keyword` بتبص على آخر كلمة وتسمح بالـregex بعد
    `return typeof instanceof in of case do else void delete new yield await throw`.
    **الإنذار الكاذب أخطر من مفيش فاحص** (درس 9): بتتعوّد تتجاهله أو
    — الأسوأ — بتلف حوالين الفاحص بدل ما تصلّحه، فتسيب الفخ للي بعدك.
    واتعاير بأربع حقن: اسم حر حقيقي · اسم بره نطاقه · شيل الإصلاح نفسه
    (الإنذارين الكاذبين رجعوا) + ضابط.

42. **في الـUPDATE: قيمة غايبة مش أمر بالمسح.** لما تبني تحديث من حمولة
    خارجية، أي حقل مش موجود فيها **مايدخلش الجملة أصلاً** — ماتحطّهوش `null`.
    `asText(undefined) → null` خلّت `applyReferral` تمسح `ctwa_clid` كل ما
    يجي referral من غير الحقل ده، وميتا **بتوثّق** الحالة دي (إعلانات
    WhatsApp Status). الفرق بين «الحقل مش موجود» و«الحقل قيمته فاضية» هو
    الفرق بين تحديث جزئي وبين مسح صامت — وفي بيانات بتتبعت **مرة واحدة بس**
    (معرّفات النسبة، التوكنات، الـclids) المسح ده **نهائي**. القاعدة: ابنِ
    الـpatch بالمفاتيح الموجودة، وخلّي المسح **فعل صريح** مش أثر جانبي.

46. **الاستدعاء المتسلسل ≠ التسليم المتسلسل — وميتا موثّقة كده صراحةً.**
    بنيت إرسال الرد الجاهز على إن `await` ورا بعض بيضمن الترتيب عند
    العميل. التوثيق بيقول بالحرف: «When sending a series of messages, the
    order in which messages are delivered is **not guaranteed to match the
    order of your API requests**» — والطريقة الوحيدة المضمونة هي انتظار
    `delivered` في الـwebhook قبل كل رسالة (مرفوضة عندنا: موبايل مقفول =
    الرد ميكملش).
    **والأخطر إن المعايرة عدّت.** كتبت معايرة بتبعت الصور بالتوازي وبتثبت
    إن الترتيب بينقلب — فالفحص كان بيثبت إن **نداءاتي** مرتبة، وده حقيقي
    ومالوش أي علاقة باللي بيوصل للعميل. المالك شاف «صورة ← كلام ← صورة»
    والقياس على `wa_messages` أثبت إن نداءاتنا خرجت مرتبة تماماً
    (19:13:37 · 19:13:39 · 19:13:41).
    **القاعدة:** لما تختبر حاجة بتخرج لطرف تالت، اسأل: الفحص ده بيقيس
    **اللي إحنا بنعمله** ولا **اللي بيوصل**؟ لو الأول بس، فهو درس 26
    متنكّر في شكل اختبار. والحل اللي اتمشى عليه: قلّل عدد الرسايل بدل ما
    تعتمد على ترتيبها (الكلام `caption` على آخر صورة = يستحيل يتوسط).

47. **المعايرة بتبوظ في صمت لما الكود حواليها يتغيّر.**
    معايرة في `test-wa-adfilter` كانت بتشيل سطر `waFetchAdConvos();`
    باستبدال نصي بيشمل السطر اللي بعده. ضفت `waFetchAdNames();` بينهم،
    فالاستبدال مابقاش بيطابق — **المعايرة اشتغلت من غير ما تشيل حاجة**،
    والنتيجة طلعت زي الأصلية فالفحص «وقع» لسبب وهمي. الإنذار الكاذب هنا
    كان مفيد بالصدفة، لكن العكس أخطر: معايرة بتفشل في تعديل الكود بتدّي
    **ثقة كاذبة** إن الفحص بيمسك باج وهو مابيمسكش حاجة.
    **القاعدة:** المعايرة تستهدف **أصغر وحدة ممكنة** (السطر نفسه بـregex
    مش سطرين متجاورين بالنص)، وأي معايرة لازم تفشل لما تتشغّل على الكود
    السليم — لو عدّت على الاتنين، هي مش معايرة.

    🔴 **تنقيح اتدفع تمنه 19 سبتمبر: «أصغر وحدة» ≠ «وحدة فريدة».**
    نفس المعايرة دي بوّظت تاني لما الكتلة اللي جواها اتغيّرت (اتضاف
    `waRenderConvLabels(ac)` جنب `waUpdateCtwa(ac)`)، والإصلاح المتسرّع
    كان regex على **النداء لوحده** `waUpdateCtwa\(ac\);` — وده أصغر
    فعلاً، بس فيه **نداء تاني بنفس الشكل بالظبط** قبله في الملف
    (سطر 215)، فالـregex غير الشاملة مسكت الغلط وشالت نداء في دالة
    مالهاش علاقة. الاختبار وقع لسبب تالت تماماً وضيّع وقت في تشخيص وهمي.
    **الصح: مرساة تجمع بين الصِغر والتفرّد** — هنا
    `waUpdateWindow(ac); waUpdateCtwa(ac);` (اتأكد إنها فريدة بـ`grep -c`
    قبل ما تكتبها)، **ومعاها حارس يرمي لو الاستبدال مالقاش حاجة**:
    ```js
    const before = body;
    body = body.replace(ANCHOR, REPLACEMENT);
    if (body === before) throw new Error('المعايرة مالقتش المرساة');
    ```
    الحارس ده هو اللي بيحوّل «معايرة بايظة في صمت» لفشل بصوت عالي.

48. **تصنيف قالب واتساب بيتحدد بتعريف ميتا الحرفي مش بمنطقك — والـclassifier
    بيقولهولك قبل الإرسال لو سألته.**
    المالك طلب «template utility» لفتح شات. صحّحت إن تحية عامة = `Marketing`،
    وكتبت صياغة ربطتها بطلب العميل استناداً للتوثيق العام («*enable businesses
    to **follow up on user actions or requests***»). **وبرضه اترفضت.** شاشة
    إنشاء القالب بتعرّفها أضيق بكتير: «Utility = *Send messages about an
    **existing order or account***» — لازم **أوردر أو حساب قايم**، ومتابعة
    «طلب» مجرّد مش كفاية.
    **اللي وفّر الوقت:** الرفض جه من **الـclassifier بتاع ميتا وقت التسجيل**
    («Category does not match» + «will be rejected») مش من مراجعة بعد أيام،
    والمالك شافه بنفسه قبل ما يضغط. يعني أرخص تحقق ممكن للتصنيف هو
    **تعبئة الشاشة والبص على ردها** قبل أي بناء.
    **والقاعدة الأعم (امتداد لـ«ماتفترضش دلالة API»):** تعريف السياسة اللي
    بيتنفّذ فعلاً هو اللي في **واجهة التنفيذ**، مش اللي في صفحة التوثيق.
    لما الاتنين يختلفوا، الواجهة هي اللي بتقرر.

49. **السيشن الجديدة بتبدأ من الفرع الافتراضي — فبتقرا `CLAUDE.md` قديم
    وتشتغل على حقيقة فاتت، وهي واثقة.**
    حصل 16 سبتمبر: المالك فتح سيشن جديدة لشغل J&T وقالها تقرا `CLAUDE.md`.
    ردّت رد سليم تماماً ومنظّم — **وكله مبني على نسخة عمرها ساعات**. كانت
    على فرع `claude/admiring-brahmagupta-gqow5d` المتفرّع من الافتراضي،
    و`HEAD` عند `7ae691a` = **14 كوميت ورا** فرع الشغل.
    **والمفارقة القاتلة:** كل تسليم J&T — القرارات ومعادلة سعر الشحن
    والمدخل الناقص (الوزن) ونتيجة التحقق من الـAPI — **اتكتب في
    `CLAUDE.md` مخصوص عشانها**، وهي مقرتش منه ولا حرف. وكمان الـ
    `SHIPPING_COST_DEFAULT = 85` كانت لسه في شجرتها، فكانت ممكن **ترجّعه**
    وهي فاكرة إنها بتصلّح.
    **القاعدة:** التسليم مش «أنا كتبته في `CLAUDE.md`» — التسليم هو **إن
    السيشن اللي هتنفّذ بتقرا النسخة دي فعلاً**. نفس عيلة انحراف
    `AGENTS.md` بالظبط، بس الانحراف هنا **بين الفروع** مش بين الملفات.
    **وأرخص كشف: اسأل عن رقم يعرفه الجديد بس** — «آخر نسخة اتسلّمت كام؟».
    v52 = محدّثة · v48 = قديمة. وسطر رقم النسخة اللي اتكتب أصلاً عشان
    ماخمّنش v45 تاني، بقى **مصافحة** بتكشف القِدَم في سؤال واحد.
    (مؤشرات تانية بانت في ردها: حجم `CLAUDE.md` **158KB** والحقيقي
    **200KB** · و«آخر حاجة اتعملت» بتشاور على شغل الصبح.)
    **الإصلاح:** `git fetch origin <فرع-الشغل> && git merge origin/<فرع-الشغل>`
    — بيبقى **fast-forward نضيف** طالما `HEAD` بتاعها جدّ لفرع الشغل
    (اتأكد بـ`git merge-base --is-ancestor`)، ومفيش حاجة بتضيع.

24. **الوثيقة مش دليل.** بند 🔴 "سياسات wa-media مفتوحة للجميع" فضل في الملف ده بعد ما المشكلة اتقفلت. **قبل أي شغل على بند أمني، اتحقق منه على الحالة الحية الأول** — التحقق أرخص من الإصلاح، والبند الغلط بيسحب وقت من بند حقيقي.

---

## بوابة الواتساب في CentralORDERS (31 يوليو)

**المشكلة اللي اتكشفت بالتجربة:** التاجر قفل "تفعيل تأكيد الطلبات بالواتساب" من اللوحة، وجه أوردر تست، **والرسالة اتبعتت**. السبب: `CentralORDERS` **عمره ما قرا `whatsapp_confirmation_enabled`**. اللوحة بتكتب في عمود ومحدش بيقراه — المفتاح مكانش موصّل بمسار الإرسال أصلاً.

**الشكل الجديد (منشور — `activeVersionId=97a29fba`):**
```
PLATFORM LOG → WA Gate → If2 ─(true)→ SENDING CONF MSG → … → Wait 30د → If → SENDING CONF MSG1
                             └(false)→ يقف
```

- **`WA Gate`** = نود HTTP Request على `POST /rest/v1/rpc/wa_should_send`.
  **مفيش عملية RPC في نود Supabase بتاعة n8n** (الموارد المتاحة: `row` بس) — الاستدعاء لازم يبقى HTTP على مسار PostgREST.
  التوثيق `predefinedCredentialType` → `supabaseApi`. الدالة متاحة لـ**`service_role` بس** (مش `authenticated` ولا `anon`)، فالـcredential لازم تكون service_role.
- **`If2`** = `{{ $('WA Gate').item.json.send }}` boolean is true.
- **النودتين بيقروا الاعتماد من البوابة**: `$('WA Gate').item.json.phone_id` و`.token` — مش من `Get Tenant`. كده الرقم المشترك بيشتغل تلقائي أول ما `platform_settings` تتعبّى، والخصم بيتم جوّه الدالة.

**التذكير محمي من غير بوابة تانية** — مساره مالوش طريق غير من ورا `SENDING CONF MSG`. لو `send=false` الفلو بيقف عند `If2`، فمفيش `Wait` ومفيش تذكير.

**الفاضل:** لو التاجر قفل المفتاح **جوّه** الـ30 دقيقة، التذكير بيخرج برضه (البوابة مابتتسألش تاني). حالة ضيقة. و`p_is_reminder` بقى **بارامتر من غير مستدعي** — الدالة بتدعمه ومفيش حاجة بتناديه بـ`true`.

**⏳ ولا تنفيذ واحد لحد دلوقتي.** البوابة اتنشرت ومفيش أوردر عدّى عليها. **الاختبار الحاسم لسه مطلوب:** اقفل المفتاح → `Execute step` على `WA Gate` لوحدها بـ`order_id` قديم → المتوقع `{"send":false,"reason":"feature_off"}`؛ افتحه → `{"send":true,"reason":"own_token"}`. الفرق بين الردّين هو الدليل الوحيد إن المفتاح بقى موصّل — من غيره احنا واثقين من **الشكل** مش من **السلوك**.

---

25. **مخرجات النود بتفضل متاحة عبر `Wait`.** التذكير محتاج بيانات اعتماد الواتساب بعد انتظار 30 دقيقة، والحل مكانش نود بوابة تانية — `$('WA Gate').item.json.token` بتشتغل في **نفس التنفيذ** حتى بعد الـ`Wait`. فايدة مزدوجة: الرسالتين بيخرجوا من نفس الرقم بالتأكيد، والدالة بتتنادى **مرة واحدة** فالخصم مرة واحدة. (المؤشر إن ده آمن: `SENDING CONF MSG1` كانت بتنادي `$('Webhook').item` من قبل كده وشغّالة في الإنتاج — يعني الـitem pairing عبر المسار ده مثبت عملياً مش مفترض.)

26. **"مفيش تنفيذ" مش "شغّال".** البوابة اتبنت واتنشرت واتراجعت نود بنود — وعدد تنفيذاتها **صفر**. مراجعة الشكل بتثبت إن التوصيل صح، **مابتثبتش إن السلوك صح**. أي تغيير على مسار إنتاجي فاضل عليه اختبار سلوكي لحد ما رد حقيقي يثبت العكس.

## الموظفين + عمولة الـupselling + التسويات (6 أغسطس)

طلب Trendose. اتبنى كله **مركزي** — صف في `user_profiles` مش شغل يدوي لكل تاجر.

### إدارة الموظفين
Edge Function `tenant-staff` (v2) — `list` / `create` / `toggle` / `delete`.
**الثابت الحاكم: `tenant_id` عمره ما بييجي من الـbody** — بيتقرا من بروفايل صاحب
الطلب عبر الـJWT. غير كده أي أدمن يقدر يضيف موظف في متجر غيره بتعديل الطلب.
الحراسات: أدمن نشط بس · الهدف في نفس المتجر · مايلمسش نفسه · مايلمسش
`is_super_admin` · الدور `admin|employee` بس · الباسورد 8 حروف على الأقل ·
ولو إنشاء البروفايل فشل بيمسح مستخدم الـauth اليتيم.
**ليه Edge Function مش توسيع `platform-admin`؟** لأن ده كان هيدّي التاجر مسار
بصلاحية service_role — الفصل مقصود.

### العمولة
- إعدادات لكل موظف على `user_profiles`: `upsell_commission_enabled/type/value`
  (`fixed` مبلغ ثابت أو `percent` نسبة، بـCHECK constraint).
- `save_order_products(...)` **SECURITY DEFINER** — انحراف مقصود ومبرر: لو
  INVOKER كنا هنحتاج سياسة INSERT على `upsell_events`، وساعتها أي موظف يدخل
  صف بعمولة من اختراعه من الكونسول. بدلها الدالة بتعيد حراسات الـRLS بإيدها،
  و**`before` بيتقرا من صف السيرفر** فالفرق مايتزوّدش من المتصفح.
- `upsell_events`: **مفيش سياسة INSERT/UPDATE/DELETE خالص** (اتأكد حيّ —
  الكتابة المباشرة بتعدّل صفر صف). و`user_id` **مش FK** عن قصد: الأدمن يقدر
  يحذف موظف والسجل المالي لازم يفضل، فـ`user_name` و`commission_rate` لقطات
  مجمّدة وقت الحدث.
- الاستحقاق **تريجر** مش نداء من الفرونت (`app.resolve_upsell_on_status`) —
  عشان يشتغل كمان لما n8n (service_role) هو اللي بيغيّر الحالة، وده اللي
  بيحصل في أغلب التسليمات. delivered → مستحقة · cancelled/returned/failed →
  ملغية. والاتجاهين رجّاعين.

### التسويات
- `commission_settlements`: المبلغ موجب = صرف، سالب = قيد عكسي. **إلغاء التسوية
  قيد عكسي مش حذف** — السجل المالي مايتمسحش.
- `v_commission_balances` **`with (security_invoker = on)`** وبأعمدة صريحة
  (درسي 12 و13). `outstanding = earned_total − settled_total` و**بيبقى بالسالب
  عن قصد** لو اتصرفت عمولة والأوردر رجع بعدها — بيتعرض «عليه X ج» مش صفر.
  التقصير على صفر بيخبّي فلوس على التاجر.
- `settle_commission` / `reverse_settlement` — أدمن على متجره بس، وفيه حارس
  `already_reversed`.
- مبلغ التسوية بييجي **مكتوب سلفاً بكل المستحق وقابل للتعديل** (قرار المالك).

### الواجهة
- **الأدمن:** تبويب العمولات في الماليات — 4 كروت (معلّقة · مستحقة الكل ·
  اتصرف · الرصيد) + كارت لكل موظف بزرار «💵 تسوية» + جدول الحركات + سجل
  التسويات. الضغط على أي صف حركة بيفتح الأوردر.
- **الموظف:** صفحة «عمولتي» (رصيده · المعلّق · اللي اتصرفله · نسبته وشرحها ·
  حركاته · التسويات اللي اتعملتله) + شريط مختصر فوق جدول الطلبات — المكان
  اللي هو قاعد فيه أصلاً. **الموظف مالوش زرار إلغاء** على أي تسوية.
- **شارة 🔼** على الأوردرات اللي فيها upselling — من عمود `orders.has_upsell`
  اللي بيتحط جوّه `save_order_products` **على أي زيادة موجبة حتى لو العمولة
  مقفولة**، لأن التاجر عايز يراجع كل الـupsells مش المدفوع عليها بس.

⚠️ **رندرر سجل التسويات متكرر** (الأدمن وصفحة الموظف) — الأدمن بس اللي فيه
زرار الإلغاء. لو وحّدتهم يوم، الشرط لازم يمشي معاهم؛ فيه فحص في
`test-settlement.mjs` بيمسك الحالة دي بالظبط.

---

## زرار الشحن من نافذة التفاصيل (7 أغسطس)

مسارين حسب التاجر — والزرار القديم «📦 شحن» في التفاصيل بقى «اتشحن يدوي»:

- **«🚚 شحن أوتوماتيك»** (للرابط API — من `v_my_tenant.has_shipping_api`،
  boolean مايكشفش المفتاح فشغّال للموظفين): Edge Function **`order-ship`**
  (v1) بفحوص قبلية — نفس ثابت `tenant-staff`: **`tenant_id` من الـJWT مش
  من الـbody** · بوليصة موجودة = 409 · حالة غير pending/confirmed = 422 ·
  عنوان < 10 حروف أو مفيش تليفون = 422 (بيمسك «العنوان القصير» قبل السفر) ·
  مفيش مفتاح = 422 برسالة توجيه · **cooldown 90 ثانية** (سد «ضغطتين =
  شحنتين» — n8n نفسه مالوش حارس) — وبعدين بتعلّم `shipping_requested_at`
  وتنده الويبهوك (العنوان من `platform_settings.ship_webhook_url`).
- 🔴 **مصدر الحقيقة صف الأوردر مش رد الويبهوك**: الويبهوك بيرد فوراً من
  غير نتيجة، فالواجهة بتعمل poll (3ث × 15) لحد ما `tracking_no` يظهر —
  **الحالة عمرها ما بتتكتب من الواجهة ولا من الـEF**، n8n بس بيكتبها بعد
  رد شركة الشحن (اتقاس حي: 12–30 ثانية). فشل في أي حتة = الأوردر فاضل
  زي ما هو + شارة «⚠️ محاولة ماكملتش» بعد 3 دقايق (`ship.js`).
- **«📦 اتشحن يدوي»** (الكل): RPC ذري **`mark_shipped_manual`** —
  SECURITY INVOKER (النطاق وقفل النفاد من الـRLS زي `set_order_status`،
  ونفس شكل سجل الحالة حرفياً) + حارسي `already_has_tracking` و`bad_status`.
  بيسجّل رقم التتبع الاختياري — ده اللي بيفتح التتبع والجرد للتاجر اليدوي.
- أوردر له `tracking_no` = **ولا زرار** — بوليصة واحدة بس. والمهرب للحالات
  الخاصة موجود في قايمة الحالات + «تحديث الحالة المختارة».
- **علامة جوه إطار شارة الحالة في الجدول** (طلب المالك بعد التجربة الحية —
  «الموظف يدوس ويمشي»): بتلف والطلب بيتبعت، وصفرا نابضة لو ماكملش.
  ومعاها **تيكر** كل 30ث بيعيد جلب الأوردرات المعلّقة بس — **صامت**:
  مابيرسمش الجدول غير عند تغيير فعلي أو انقلاب علامة (إعادة الرسم كل
  تيك كانت باينة للمالك «ريفريش على الفاضي» واتشالت). ⚠️ صفوف الجدول في
  `fil` (الصفحة الحالية من السيرفر) مش `all` (مخزن الماليات الكسول —
  بيبقى فاضي) — التيكر اتكتب غلط على `all` الأول والمجس العدّاد هو اللي
  كشفها (تيكات بتحصل واستعلامات صفر). و`shipping_requested_at` لازم
  يفضل في `ORDER_LIST_COLS`.
- **قرارات UX من التجربة الحية التانية والتالتة (7 أغسطس مساءً):** النافذة
  بتتقفل **فوراً** مع تأكيد الشحن · عتبة «ماكملتش» = **60 ثانية** (نزلت من 90 بطلبه) ·
  **صفر toast بعد الضغطة وفي أي فشل وصفر إعادة رسم للجدول** — العلامة جنب
  الحالة هي القناة الوحيدة، بتتحدث بتحديث **جراحي** لخلية الصف
  (`updateRowIndicator`)، وسبب الفشل الحقيقي (عنوان قصير...) متخزن في
  `shipFail` وبيظهر في تلميح العلامة وفي شارة النافذة. الرسم الكامل
  محجوز لتغيير الحالة الفعلي. رسالة نجاح «البوليصة اتعملت» بس اللي فاضلة.
- **اليدوي بيقفل نافذة التفاصيل مع التأكيد (15 أغسطس — بلاغ المالك):** كان
  بيفضل فاتح التفاصيل لأن `applyShipped` بترندر الديتيل تاني لما `sel` لسه
  شايل الأوردر. القفل (`ovl` + `ordersSetSelected(null)` + `detailAbort`)
  **لازم يسبق** `applyShipped` — نفس عقد الأوتوماتيك «الموظف يدوس ويمشي».
  ⚠️ درس من اختباره: فحص «الصف اتحدث» القديم كان بيعدّي **بالصدفة** من تعديل
  `sel` بالمرجع (نفس object بتاع `__ORDERS` في الستب) — لما القفلة شالت المسار
  ده، الفحص وقع وكشف إنه مكانش بيفحص الجدول أصلاً. الستب بقى بيحاكي كتابة
  السيرفر في `__ORDERS`، والفحص بيقرا `fil` (مصدر الجدول) عبر dynamic import.
- **رقم التتبع في مودال اليدوي اختياري فعلاً (8 أغسطس):** المودال العام
  (`core/modal.js`) كان بيرفض أي خانة فاضية ويحمّرها — اتبنى كده لمودالات
  المبلغ والسبب الإجبارية — فالـplaceholder كان بيقول «اختياري» والضغطة
  بتنوّر أحمر. بقى فيه خيار `inputOptional` (اليدوي بس اللي بيستخدمه —
  التسوية والسبب فاضلين إجباريين وفيه ضابط في `test-settlement` بيثبت ده).
  واتصلح معاه تحمير **لازق**: `borderColor` كان بيتحط عند الرفض ومابيترجعش
  أبداً، فأي مودال بإدخال بعد أي رفضة كان بيفتح أحمر من أول لحظة.
- migration: `manual_ship_and_ship_request` (اتجرّبت بترانزاكشن راجعة
  بانتحال موظف حقيقي مش سوبر — أول محاولة انتحال طلعت بحساب السوبر أدمن
  و«العبور» بتاعها كان صلاحية مقصودة مش ثغرة).

---

## سكانر المخزن (Telegram Mini App — ملف خارج الريبو)

صفحة HTML واحدة المالك بيستضيفها بنفسه وبتضرب `POST /webhook/scannerpath`.
**مش في الريبو عمداً** (فيها PINs الموظفين نص صريح). النسخة الحالية **4.3**
(15 أغسطس — الكاميرا بطّلت تشتغل على الآيفون والأندرويد سليم):
- **آيفون = مسارين مقفولين من تليجرام نفسه:** `showScanQrPopup` بيفشل
  **في صمت من غير exception** (مفيش طريقة تكتشفه من الجافاسكربت)،
  و`getUserMedia` جوه الـMini App بيرجّع **ستريم أسود** — باج تليجرام iOS
  موثق ومفتوح (tma.js issue #748). نسخة 4.2 وقعت في التاني: البديل الحي
  فتح بكاميرا سودا. **درس: المسار البديل لازم يتبني على قناة مختلفة جذرياً
  مش نفس الـAPI بشكل تاني.**
- **الحل في 4.3 — التوجيه بالمنصة** (`Telegram.WebApp.platform`):
  أندرويد → البوب أب زي ما هو (وبتحصين try/catch + إقفال وإعادة محاولة →
  ماسح حي getUserMedia+ZXing لو فشل). **آيفون → وضع التصوير**: زرار
  «📸 صوّر البوليصة» بيفتح كاميرا النظام الأصلية عبر
  `<input type="file" capture="environment">` (شغالة دايماً في WKWebView)
  والصورة بتتفك بـZXing (`@zxing/library@0.21.3` مثبّتة) بـTRY_HARDER
  و4 محاولات: مقاسين × اتجاهين — عشان البوليصة المتصورة بالعرض.
- **شارة نسخة ظاهرة على شاشة الدخول** («نسخة 4.3 · ios · tg 8.0») — عشان
  نفرّق فوراً بين «النسخة الجديدة معطلة» و«تليجرام مقدّم الملف القديم من
  الكاش». لو الشارة مش بتظهر بعد الاستبدال: غيّر اسم الملف أو ضيف `?v=43`
  للينك المتسجل في BotFather.
- اتجرب بهارنس 18 فحص (المنصات التلاتة + فشل الفك + التبديل خروج/مرتجع) +
  **معايرة فك حقيقية**: Code128 متولد بـpython-barcode اتفك بنفس سلسلة
  محاولات الصفحة وهو مستقيم ومقلوب 90° ومايل 12° وصغير (4/4)، وQR كذلك
  (درس 21 — الضابط على الطريق المفتوح).

---

## خصائص المنتجات — ألوان/مقاسات (15 أغسطس)

طلب المالك: «عايز أعرف عندي كام من اللون ده، مش كام من المنتج عموماً». الاكتشاف
الحاكم: **`orders.var` متعبّي أصلاً** (1,414 من 1,877 أوردر آخر 60 يوم — «أبيض»،
«مقاس 85 عرض»...) واللاندنج بيبعته من الأول، بس المخزون كان بيطنّشه.

**الموديل: الخاصية صف بنت في `stock_products` نفسه** (`parent_id` + `variant_label`)،
والاسم بيتولّد **على السيرفر** «أم — لابل» (فاصل em dash عمداً — سكانر n8n بيقسم
على `" - "` العادية فقط، فالـem dash بينجّي الاسم من التفكيك). migration: `stock_variants`.

- **كمية الأم نفسها = سلة «غير موزع»** — مش مجموع البنات. إجمالي العيلة =
  الأم + البنات. لما تتحول لمنتج بخصائص، الكمية القديمة بتفضل على الأم لحد
  ما تتوزع بزرار التوزيع.
- **`resolve_variant_movement`** (BEFORE INSERT على `stock_movements`): حركة على
  منتج ليه بنات + معاها `tracking_no` → بيجيب `orders.var`، يطبّعه بـ
  `app.norm_variant` (أرقام هندية→لاتيني · مسافات · bidi · تطويل · lowercase)،
  ويحوّل الحركة للبنت المطابقة. **مفيش مطابقة = الحركة على «غير موزع» —
  صفر تخمين صامت.** المرتجع بنفس المنطق. **صفر تعديل في n8n**، والتريجر نايم
  لأي منتج من غير بنات.
- **`transfer_stock(p_from,p_to,p_qty)`**: توزيع ذري (حركتين out/in) جوه نفس
  العيلة بس، أدمن المتجر بس، بقفل صفوف مرتّب (منع deadlock)، والتريجر القديم
  بيمنع السالب فالتوزيع فوق المتاح بيترفض لوحده.
- **حارس `stock_variant_guard`**: مستوى واحد بس (مفيش حفيد) · نفس المتجر ·
  الاسم بيتجدد دايماً من الأم واللابل · وإعادة تسمية الأم بتسحب البنات
  (`stock_parent_rename_cascade`). لابل مكرر بعد التطبيع = unique violation.
- 🔴 **الـSELECT على `stock_products` ممنوح بالأعمدة** (كله إلا `wholesale_price` —
  ده اللي بيحجبه عن الموظفين). يعني: **أي `SELECT *` أو `%ROWTYPE` جوه دالة/تريجر
  هيقع من مسار الداشبورد** بـpermission denied (اتلسعنا بيها في أول نسخة من
  `transfer_stock`). أعمدة صريحة دايماً، وأي عمود جديد محتاج GRANT صريح
  (`parent_id`/`variant_label` اتمنحوا في الـmigration).
- **الواجهة**: جدول المنتجات بيجمّع العيلة (صف أم بإجمالي العيلة + شارة عدد
  الخصائص، البنات بتتفتح بضغطة، وصف «غير موزع» لو فيه رصيد) · كارت «المنتجات»
  بيعد العيلات · «نواقص» والتنبؤ بيستبعدوا أم ليها بنات (صفرها حالة طبيعية
  مش نفاد — من غير الاستبعاد كل عيلة موزعة كانت تطلع «نفد» كاذبة) · محرر
  المنتج فيه قسم الخصائص (إضافة/توزيع/استرجاع inline) · محرر الخاصية بيعدّل
  اللابل بس (**الاسم عمره ما بيتبعت من الفرونت**) · محرر الحركة بيعرض العيلة
  متجمعة والأم «(غير موزع)». حراسات الحذف: أم ليها بنات أو بنت فيها كمية =
  رفض برسالة (والداتابيز بترفض برضه بـFK RESTRICT).
- **ملاحظات مفتوحة بعلم المالك**: أوردر متعدد المنتجات ليه `var` **واحد**
  للأوردر كله — الـupsell المضاف بيتوجه لو اتفق مع نفس الـvar، غير كده «غير
  موزع» (والموظف يقدر يختار صف الخاصية مباشرة من محرر المنتجات) · «Pink»
  الإنجليزي مش هيطابق «بينك» — لابلات مختلفة والتطبيع مش ترجمة.

---

## props الويبهوك: `manufacturer_note` و`manufacturer_cost` (30 أغسطس)

المالك عدّل نود `Insert Order to Supabase` في `CentralORDERS` (منشور —
`activeVersionId=a67513ae`) وبقى بيكتب خصائص المنتج النازلة من الويبهوك:
- **`var`** = `cart_items[0].variant.variation_props[0]` — أول prop بس. ده
  اللي ميزة خصائص المخزون بتطابق عليه (`resolve_variant_movement`).
- **`manufacturer_note`** = أول اتنين props مفصولين بمسافة («أبيض 4 أدوار»).
- **`manufacturer_cost`** = `body.expense`.

**ليه ماكانتش بتظهر:** الكتابة كانت شغالة 100% (71 من 73 أوردر) — الداشبورد
هو اللي **معندوش ولا سطر واحد بيقرا العمودين دول**. `manufacturer_note`
مكانش في `ORDER_LIST_COLS` ولا في أي كود رسم (نافذة التفاصيل بتسحب `select('*')`
فالداتا كانت **بتوصل المتصفح وتترمي**). نفس عيلة باج مفتاح الواتساب بالمقلوب:
وقتها اللوحة بتكتب ومحدش بيقرا، وهنا n8n بيكتب ومحدش بيقرا.

**القرار (المالك): عرض بس.**
- سطر واحد «خصائص المنتج» في التفاصيل + شارة الجدول بقت تعرض الـprops كاملة.
  الأولوية لـ`manufacturer_note` و`var` **fallback** (آلاف الأوردرات القديمة
  قبل 29 أغسطس مالهاش `manufacturer_note`). الدالة `orderProps` في
  `core/format.js`، وسطر «اللون / المقاس» القديم اتشال — كان هيكرر «أبيض»
  جنب «أبيض 4 أدوار».
- **زرار «نسخ كل المنتجات» (3 سبتمبر — بلاغ المالك):** فات على v33 وفضل بيقرا
  `sel['var']`، فالمنسوخ كان بيتقطع عند أول prop («تيربو بريمو 5 دور (عدد 1) -
  4 أدوار» بدل «… - 4 أدوار مدور»). بقى على `orderProps` زي الجدول والتفاصيل.
  **الدرس: لما توحّد مصدر عرض، اعمل مسح آلي لكل المستهلكين مش اللي في بالك بس**
  — مسار النسخ مكانش في البال. المسح دلوقتي نضيف: الموضعين الوحيدين اللي
  بيقروا `['var']` هما كومنت والـfallback جوه `orderProps` نفسها.
- ⚠️ **الفصل بين الـprops مسافة ومش قابل للتفكيك في الفرونت**: في «مقاس 85 عرض»
  المسافات جوّه prop واحد. لو حبينا نفصلهم يوم، الفاصل لازم يتغير في n8n
  (مثلاً ` · `) — مش شغل فرونت.

🔴 **`manufacturer_cost` مالهاش أي أثر على الأرباح** — بتتعرض للأدمن بس بتسمية
صريحة «(مش داخلة في الأرباح)». التكلفة الحقيقية لسه من snapshot الشحن أو سعر
جملة المخزون. **وكان فيه مرشّح ميت `manufacturer_cost_snapshot` في
`orderCostSnapshotValue` — عمود مش موجود في الجدول خالص** واتشال عشان محدش
«يصلّحه» لـ`manufacturer_cost` بالغلط. فيه فحص في `test-props.mjs` بيثبت إن
تكلفة البضاعة ثابتة مهما اتغيرت تكلفة المصنّع، ومعايرة بتحقن ضمّها وتتأكد إن
الحارس بيقع.

**حدود التعبيرة الحالية (اتقاست حي، بعلم المالك):** `cart_items[0]` بس — الأوردر
متعدد المنتجات بيضيّع props الباقي، **ولو أول منتج مالوش variant الاتنين بيطلعوا
null** (حصل في أوردرين: 16147 و16135) · و`props[0]`/`[1]` بس فأي prop تالت
بيقع في صمت.

---

## لينك مستقل لكل قسم — التوجيه بالمسار (3 سبتمبر)

طلب المالك: `app.sahlgedan.com/orders` بدل صفحة واحدة بتباديل داخلي.

**الخريطة** (slug في اللينك ← الصفحة الداخلية): `orders` · **`inventory`←stock** ·
**`chats`←inbox** · `finance` · `analytics` · `billing` · `settings` · `mycommission`.
الأسماء الإنجليزية اختيار المالك ومش لازم تطابق أسماء الصفحات الداخلية.

- **`core/router.js`** — الجذر (`BASE`) بيتحسب **مرة واحدة** وقت التحميل من أول
  URL، وبعدها إحنا بنتحكم في كل تنقّل فمفيش انحراف. بيشتغل على الجذر وعلى أي
  مجلد فرعي (المعاينة) بنفس المنطق.
- **`showPage` هي نقطة الاختناق الوحيدة** — كل زرار وكل CTA بيعدّي عليها، فتحديث
  الـURL اتحط جوّاها بس. أي تنقّل جديد يتضاف كمان لازم يعدّي عليها.
- 🔴 **الـURL بيتحدّث بعد حراسات الأدمن مش قبلها.** موظف بيفتح `/finance`
  بيتحوّل للأوردرات **واللينك بيتصحّح لـ/orders** — لينك بيقول finance والصفحة
  أوردرات = لينك بيكدب. والتصحيح بـ`replaceState` مش `pushState` عشان زرار
  الرجوع مايرجّعهوش لمسار مرفوض في حلقة. فيه معايرة في `test-routing` بتشيل
  التصحيح وتتأكد إن الفحص بيمسكه.
- **الجولة مستثناة تماماً** (`tourActive`) — 21 خطوة كانت هتحط 21 مدخل في تاريخ
  المتصفح وتخرّب زرار الرجوع.
- الإقلاع: `applyInitialRoute()` بتتنادى من `loadTenantAndEnter` **بعد** ما
  اللوحة تبان — فالدخول على لينك مباشر وانت مش داخل بيوصّلك لمكانه بعد اللوجين
  لوحده (الـURL مابيتغيرش أثناء اللوجين لأننا SPA).
- 🔴 **الآلية: صفحة حقيقية لكل قسم — مش SPA fallback.** في `app/` فيه
  `orders.html` · `inventory.html` · `chats.html` · `finance.html` ·
  `analytics.html` · `billing.html` · `settings.html` · `mycommission.html` —
  **نسخ بالبايت من `index.html`**، بيتولّدوا بـ`python3 tools/make-route-pages.py`
  (بيقرا الـslugs من `router.js` نفسه، مصدر واحد للحقيقة).
  **الاستضافة بتشيل امتداد `.html` تلقائياً** — اتقاس حيّ 6 سبتمبر بملف `probe.html`
  على `app.sahlgedan.com`:
  ```
  /probe       → 200 text/html   (من غير أي تحويل)
  /probe.html  → 307 → /probe
  /probe/      → 307 → /probe    ← بيشيل السلاش مش بيضيفها
  ```
  السطر التالت هو اللي بيخلي الخطة سليمة: **عنوان المستند بيفضل من غير سلاش
  دايماً**، فالمسارات النسبية جوه الصفحة (`js/main.js`) بتتحل من الجذر صح.
  لو كان بيضيف سلاش كانت الصفحة هتدوّر على ملفاتها في `/orders/js/…` وتطلع مكسورة.
- 🔴 **ليه ده أحسن من `assets.not_found_handling: "single-page-application"`؟**
  1. **رفعة الداشبورد العادية بتكفي** — الإعداد ده حقل في metadata النسخة مش ملف،
     فالرافع (اللي بيرفع ملفات بس) عمره ما هيقدر يظبطه، ووحده wrangler يقدر.
  2. 🔴 **شبكة الأمان «كله أو مفيش» بتفضل حية.** مع الـSPA fallback أي ملف ناقص
     بيرجع `index.html` بـ**200** من غير أي فحص للامتداد (مثبت من كود Cloudflare:
     `notFound()` في `asset-worker/src/handler.ts`) — يعني رفعة ناقص منها موديول
     JS بتبان كلها خضرا. بالنسخ، الملف الناقص بيفضل **404 بصوت عالي**.
  3. مفيش إعداد ممكن يتمسح مع أي رفعة جاية.
  التكلفة: 8 نسخ من `index.html` (~76KB للواحدة) في الرفعة، وخطر إنها تنحرف عن
  الأصل — واتقفل بفحص.
- 🔴 **«كله أو مفيش» في الأقسام كمان.** `probeDeepLinks` بتفتح البوابة أول ما
  `/orders` ترد 200، وبعدها الراوتر بيكتب لينك **لكل** قسم. فقسم واحد من غير ملف
  = لينك بيتكتب وبيدي 404 عند الريفريش. عشان كده:
  - **فحص في `check.py`** (الفحص الـ12): كل slug في `ROUTES` لازم يكون ليه ملف
    **مطابق بالبايت** لـ`index.html`. اتعاير بتلات حقن (ملف ناقص · `index.html`
    اتعدّلت والنسخ لأ · قسم جديد في الراوتر من غير ملف) + ضابط.
  - **`verify-deploy.sh`** بيمر على **كل** الـslugs مش على `/orders` بس، ومعاه
    ضابط: مسار مجهول لازم يفضل **404** — لو رد 200 يبقى فيه fallback شغال
    وشبكة الأمان مش مضمونة.
- ⚠️ **`check_carrier_naming` بتتخطى صفحات الأقسام** — نسخ بالبايت، وفحصها كان
  بيكرر كل استثناء مسموح 8 مرات.
- 🔴 **ممنوع يبقى فيه ملف `_redirects` في `app/` — بيكسر النشر.**
  **اتثبت بتجربة A/B من المالك 3 سبتمبر:** نفس الرفعة بالظبط، مرة بالملف ومرة
  من غيره. **بالملف = رفض صامت** (زرار Deploy يرمد ويرجع أزرق، صفر رسايل،
  والمالك جرّب 4 مرات وغيّر شبكة النت). **من غيره = نجحت فوراً.**
  والملف كان **ASCII صافي 24 بايت** في المحاولة التانية — يعني **الملف نفسه
  هو المرفوض مش محتواه**. ولاحظ التنافر: `_headers` **مقبول وشغال** على نفس
  الرافع (الـCSP نازلة فعلاً).
  وقاعدة `/* /index.html 200` نفسها **مرفوضة بالاسم في parser بتاع Cloudflare**
  (`hasWildcardToIndex` في `parseRedirects.ts` → «Infinite loop detected»)، وحتى
  القواعد الصريحة لازم وجهتها `/` مش `/index.html`. يعني السطر اللي اتكتب في v35
  كان غلط من وشين — والمسار كله بقى **ملغي** لصالح النسخ.
⚠️ و`X-Content-Type-Options: nosniff` بند تشغيلي مش أمني بس — هو اللي بيحوّل CSS
ناقص من عطل صامت لخطأ في الكونسول. ماتشيلوش.
⚠️ وماتضيّقش قاعدة `/*` في `_headers` — المطابقة على **مسار الطلب** مش الملف
(اتقاس حي: الـCSP نزلت على 404 بتاع `/orders`)، فأي تضييق = الـCSP تختفي عن
لينكات الأقسام في صمت.
### عنوان التاب + تاب مستقلة للمحادثات (6 سبتمبر — طلب المالك)

- **عنوان التاب لكل قسم** — `PAGE_TITLES` في `router.js` (اسم الصفحة الداخلي →
  التسمية)، والصيغة «القسم · سهل». بيتطبّق من `showPage` **بعد حراسات الأدمن**
  زي الـURL بالظبط: تاب بتقول «الماليات» وفيها أوردرات = عنوان بيكدب. فيه
  معايرة في `test-routing` بتخليه `setPageTitle(requested)` وتتأكد إن الفحص بيقع.
  **مش مستثنى في الجولة** (بيوصف اللي باين، وده صح في الحالتين).
  ⚠️ العنوان الثابت في الـmarkup **واحد في كل الملفات** (نسخ بالبايت)، فقبل
  اللوجين التاب بتقول العنوان العام وبعد ما اللوحة تبان بتتسمّى. أي محاولة
  لتخصيص `<title>` في كل ملف بتكسر فحص التطابق بالبايت.
- 🔴 **المحادثات بتفتح في تاب لوحدها** (`openOwnTab` في `router.js`) — عشان
  تفضل مفتوحة قدام الموظف طول اليوم.
  - **اسم ثابت للتاب** (`OWN_TAB = { inbox: 'sahl-chats' }`) مش `_blank` —
    الاسم هو اللي بيخلي الضغطة العاشرة تركّز نفس التاب بدل ما تفتح عاشرة.
    فيه معايرة بتحطّه `_blank` وتتأكد إن الفحص بيمسك التابات المتراكمة.
  - **`window.open('', name)` بـURL فاضية** — بتركّز التاب الموجودة **من غير
    ما تنقّلها**، فلو الموظف كاتب رد نصّه مايضيعش. بتتنقّل بس لو طلعت
    `about:blank` (يعني اتفتحت لسه).
  - **`claimOwnTab`**: التاب اللي بتعرض المحادثات بتاخد الاسم لنفسها وبتسيبه
    لما تخرج — من غير ده تاب اتفتحت على `/chats` مباشرةً مالهاش اسم، فتاب
    تانية تدوس «المحادثات» تفتح **تالتة**. والمسح بيحصل **بس لو الاسم بتاعنا**.
    مستثنى في الجولة عشان مرورها على المحادثات مايخطفش الدور من تاب شغالة.
  - **بترجع للتنقّل العادي في 4 حالات** (والزرار لازم يعمل حاجة دايماً — درس 16):
    إحنا في التاب دي أصلاً · الاستضافة مش بتخدم اللينكات (`deepLinksOk`) ·
    **موبايل ≤768** (تاب جديدة هناك = الموظف يتوه ومايعرفش يرجع) · حاجب
    النوافذ رجّع null.
  - ⚠️ **إشعار المتصفح للرسالة الجديدة سايب في نفس التاب عمداً**
    (`inbox.js` — `window.focus()` + `showPage('inbox')` + فتح المحادثة):
    بيوديك للمحادثة المحددة على طول وده أهم من التاب.

- ⚠️ **`tools/spa-server.py`** بيحاكي الاستضافة: `<مسار>` → `<مسار>.html` لو موجود،
  والملفات اللي ليها امتداد بتطلّع 404 عمداً. `--strict` بيشيل الـfallback على
  `index.html` فيبقى **مطابق للاستضافة الحقيقية** — بيتستخدم في معايرة
  `verify-deploy.sh`، ومن غيره معايرة «قسم ناقص» بتعدّي كذب (اتلسعنا فيها).

---

## اسم الموظف تحت كل رد في الشات (6 سبتمبر)

طلب المالك: «لما الموظف يرد على شات العميل يتكتب تحت كل رسالة مين الموظف اللي
باعتها». **الحالة الحية وقت البناء:** 548 رسالة صادرة، **كلها ردود بشرية من
اللوحة** (اتفحصت — حتى الـ5 اللي شكلهم آلي طلعوا بشر)، يعني `wa-send` هو
الكاتب الوحيد للصادر ومفيش رسايل n8n هتاخد اسم موظف بالغلط.

- **`wa_messages.sent_by` (uuid) + `sent_by_name` (text)** — migration
  `wa_message_sender_attribution`. `sent_by` **مش FK عمداً** (نفس
  `upsell_events.user_id`): الأدمن يحذف موظف والسجل لازم يفضل. و`sent_by_name`
  **لقطة مجمّدة** فتغيير الاسم بعدين مايعيدش كتابة التاريخ.
- 🔴 **العمودين ممنوعين على المتصفح بصلاحيات الأعمدة.** السبب مقاس مش مفترض:
  سياسة `wa_msg_insert` بتسمح **لأي موظف نشط** يدخل صف لمتجره، والصلاحيات كانت
  على مستوى الجدول — يعني الموظف كان يقدر ينسب رسالة **لزميله** من الكونسول،
  وده بيفرّغ الميزة من معناها لأن التاجر بيقرا الشات ده عشان **يحاسب**. نفس
  أسلوب `stock_products.wholesale_price` ونفس منطق `upsell_events`.
  اتجرّب بترانزاكشن راجعة بانتحال موظف حقيقي: **تزوير النسبة اتمنع
  (insufficient_privilege) · الكتابة العادية فضلت شغالة · القراءة شغالة ·
  `is_read` شغال · وتعديل النسبة على صف موجود اتمنع كمان**.
  ⚠️ **التمن: أي عمود جديد على `wa_messages` محتاج GRANT صريح بعد كده.**
- **`wa-send` v6**: الهوية من `userClient.auth.getUser()` (الـEF عليها
  `verify_jwt:true` أصلاً وبتستخدم توكن المستخدم) والاسم من `user_profiles`
  عبر service. **الاسم عمره ما بييجي من الـbody.** وفيه حارس: الاسم يتسجّل بس
  لو الموظف من **نفس متجر المحادثة**. وفشل قراءة الاسم **مايوقفش الإرسال** —
  رسالة بلا نسبة أحسن من رسالة ماتبعتتش.
- **الواجهة**: `waSenderTag` في `inbox/message-view.js` — الاسم في نفس سطر
  الوقت (`اسم · 10:32 ✓✓`) عشان مايزوّدش سطر تحت كل فقاعة. **للصادر بس.**
  والفقاعة الفورية بتعرض `currentUser.name` من أول لحظة عشان الاسم مايتنطّش،
  وبعد رد السيرفر **اسم السيرفر هو اللي يكسب** (مصدر الحقيقة).
- 🔴 **الرسايل القديمة (548) بتتعرض من غير أي اسم** — مفيش «موظف» ولا اسم
  المتجر ولا فاصل يتيم. **نسبة مخترعة أسوأ من مفيش نسبة.** فيه فحص حاكم في
  `test-wa-sender.mjs` ومعايرة بتحط fallback وتتأكد إنه بيقع.
- ⚠️ **الستب اتوسّع**: `__WA_CONVOS` و`__WA_MSGS` (بيتقروا وقت الاستعلام)،
  و`sb.functions.invoke` بقت **بتحترم `__FN` وبتسجّل في `__FNCALLS`** — قبل
  كده كانت بترجّع `{ok:false}` ثابت، فاختبار أي مسار بيعدّي على Edge Function
  من الواجهة كان مستحيل. (من غير `__FN` بترجع لسلوكها القديم بالحرف.)

---

## المحادثة تظهر مع الأوردر + «رد على رسالة» (6 سبتمبر)

**قياس حي قبل البناء:** 383 من 960 أوردر في 30 يوم (**40%**) مالهمش محادثة،
و**كل** الـ1,356 محادثة موجودة عندها `last_inbound_at` — يعني المحادثة كانت
بتتخلق **بس** لما العميل يبعت حاجة (من `wa-inbox-ingest`).

### 1) تريجر `app.wa_conv_on_order` على `orders` AFTER INSERT
- **مش تعديل n8n** عن قصد: التريجر مركزي فأي تاجر وأي مسار بيدخل أوردر
  بيتغطى تلقائي. (والرسايل الواردة أصلاً بتتسجّل من Edge Function بتاعتنا
  `wa-inbox-ingest` مش من n8n — اتأكد بقراءة `Whatsapp_WEBHOOK`: **مفيش ولا
  نود بتكتب في `wa_messages`**.)
- 🔴 **تحويل الرقم لازم يطابق `wa-inbox-ingest` بالحرف** وإلا لما العميل يرد
  بتتعمل **محادثة تانية جنب دي**. اتأكد على الداتا الحية قبل التطبيق:
  **1337 من 1337** مطابقة تامة، صفر غلط وصفر فاضي. الشكل:
  `20xxxxxxxxxx` زي ما هو · `0…` (11 رقم) → `20`+الباقي · 10 أرقام → `20`+.
  **رقم مش مفهوم = مانعملش محادثة** بدل ما نخمّن (محادثة برقم غلط = الموظف
  بيكلم حد تاني).
- **بوابة**: التاجر لازم يكون مركّب واتساب (`whatsapp_phone_id`) — غير كده
  صندوقه بيتملى بمحادثات مايقدرش يرد عليها.
- **`ON CONFLICT DO NOTHING`** على `(tenant_id, wa_id)`: محادثة موجودة
  **مانلمسهاش** — تنبيهها لفوق مع كل أوردر بينزّل الشاتات اللي فيها رسايل
  مش مقروءة تحت.
- **`last_message_text` و`last_direction` بيفضلوا NULL عن قصد** — الأعمدة دي
  بيملاها `wa_after_message_insert` من رسالة **حقيقية**. الواجهة بتعرف «لسه
  مفيش رسايل» من `last_direction IS NULL`. و`last_message_at` بياخد وقت
  الأوردر عشان الترتيب يبقى له معنى.
- اتجرّب بترانزاكشن راجعة: **بيعمل واحدة · مايكررش مع أوردر تاني · وبيتخطى
  التاجر اللي مالوش واتساب**.
- 🔴 **الواجهة لازم تفرّق بين حالتين**: `waUpdateWindow` كانت بتقول «النافذة
  قفلت — العميل لازم يبعتلك رسالة **جديدة**»، وده **بيقرا غلط** لمحادثة
  عمرها ما اتفتحت. بقت بتقول «العميل لسه مبعتش أي رسالة — واتساب مابيسمحش
  نبدأ إحنا» لما `last_direction` فاضي. وخانة الكتابة مقفولة في الحالتين —
  **دي قاعدة واتساب مش قرارنا**.
- **الـ383 أوردر القدام مالهمش محادثة** — إصلاح للأمام بس (قاعدة المشروع).
  ❌ **والـbackfill اترفض بقرار صريح من المالك (6 سبتمبر)** — متتناقشش تاني.
  الأوردرات اللي قبل التريجر مالهاش محادثة والمالك مش عايزها تظهر.

### 2) `wa_messages.reply_to_wa_id` + `wa-inbox-ingest` v4
- واتساب بيبعت `context.id` = معرّف الرسالة المقتبسة، والـEF كانت بترميه.
- 🔴 **واتساب مابيبعتش نص الرسالة المقتبسة** — المعرّف بس. فالواجهة بتحلّه من
  الرسايل المحمّلة (أحدث 500 للمحادثة) عبر خريطة `wa_message_id → رسالة`.
- 🔴 **ولو مالقتهاش بتقول «↩︎ رد على رسالة أقدم» من غير أي نص مخترع.**
  ده **مش استثناء نادر**: رسايل التأكيد الآلية اللي n8n بيبعتها **مش متسجّلة
  في `wa_messages` أصلاً** (اتأكد: كل الصادر المتسجّل ردود بشرية من اللوحة)،
  فالرد على رسالة التأكيد — وهي أكتر حاجة العميل بيرد عليها — عمره ما هيتحل.
  **الحل لو اتقرر يوم: n8n يسجّل رسايل التأكيد في `wa_messages`** — خطوات
  يدوية للمالك، ومش هتتعمل من غير قراره (مسار إنتاجي).
- ⚠️ **العمود الجديد محتاج GRANT صريح** — الفخ اللي اتوثّق مع
  `wa_message_sender_attribution`. `reply_to_wa_id` **قراءة بس** من المتصفح،
  الكتابة من الـEF بصلاحية service. (اتأكد حيّ: صفر عمود من التلاتة قابل
  للكتابة من `authenticated`.)
- 🔴 **مصغّرة الصورة المردود عليها (بلاغ المالك بعد تجربة v41):** «📷 صورة»
  لوحدها كانت بتقول إنه بيرد على صورة **من غير ما تقول أنهي صورة** — والموظف
  اللي بيرد على عميل بيسأل عن سعر منتج محتاج يشوف المنتج. بقى فيه مصغّرة
  38px + الكابشن، والأيقونة بتتشال لما المصغّرة تظهر.
  ⚠️ ومعاها فخ اتصلح: **المسار التدريجي كان بيحل ميديا الرسايل الجديدة بس**،
  فرد جديد على صورة **قديمة** كان بيدي وسم `<img>` مكسور — وده السيناريو
  الأشيع (الشات مفتوح والعميل بيرد على صورة اتبعتت من شوية). دلوقتي بيحل
  ميديا الرسالة المقتبسة كمان.
- ⚠️ **الستب كان بيرجّع روابط تخزين فاضية** — فأي فحص «المصغّرة موجودة» كان
  هيعدّي على وسم مصدره مكسور. `createSignedUrls` بقت بتخدم من `window.__MEDIA`
  (والافتراضي فاضي زي ما كان بالحرف عشان الاختبارات القديمة)، والفحص بيتأكد
  من **`naturalWidth > 0`** مش من وجود الوسم.
- `test-wa-sender.mjs` بقى **32 فحص + 4 معايرات** (منهم واحدة بتحط نص مخترع
  للمقتبسة المجهولة، وواحدة بتشيل المصغّرة وتتأكد إن الفحص بيقع).

---

## الموظف يرد على رسالة معينة (7 سبتمبر)

**التحقق من الـAPI قبل البناء** (قاعدة «ماتفترضش دلالة API»): توثيق ميتا الرسمي
بيحط `context` في **جذر** الحمولة وجواه `message_id`، وهو من
`BaseMessageProperties` يعني بيشتغل مع **النص والصورة والملف** سوا.
⚠️ **والتوثيق ساكت** عن تلات حاجات — مسجّلة كمجهول مش افتراض:
هل ينفع تقتبس رسالة **صادرة** إحنا بعتناها · حد أقصى لعمر الرسالة المقتبسة ·
أكواد الأخطاء الخاصة بالـcontext. **فمااتفترضش رقم**: أي فشل والاقتباس موجود
بيرجع `reply_failed` والواجهة بتقول «واتساب رفض الرد على الرسالة دي (غالباً
قديمة) — ابعتها كرسالة عادية» بدل «حصلت مشكلة».
🔴 **ولسه محتاج تأكيد سلوكي حي** (درس 26): الشكل اتراجع والسلوك لأ.

### `wa-send` v7
- بياخد `reply_to` (wamid) من الـbody، وبيحط `payload.context = {message_id}`.
- 🔴 **الحارس**: المعرّف جاي من المتصفح **فمايتصدّقش**. التحقق **بالتلاتة مع
  بعض**: `tenant_id` + `conversation_id` + `wa_message_id`. من غيره موظف يبعت
  wamid من **محادثة تانية**، وإحنا نخزنه في `reply_to_wa_id`، فالواجهة ترسم
  اقتباس لمحتوى **مش من المحادثة دي** = تسريب بين المحادثات.
- **الرفض صريح** (`bad_reply_target` 400) **مش تجاهل صامت** — التجاهل معناه
  الموظف فاكر إنه رد والعميل بيوصله نص سايح بلا سياق.
- التحقق بـservice مش بتوكن المستخدم **عن قصد**: المحادثة نفسها اتصرّح عليها
  فوق عبر RLS بتوكنه، فالسؤال هنا «الرسالة دي في المحادثة دي؟» مش «مسموحله؟».

### الواجهة
- **زرار الرد جوّه سطر الوقت** جنب الساعة والتيكات — **مش عايم فوق الفقاعة**.
  🔴 النسخة الأولى كانت `position:absolute` في ركن الفقاعة، والفحص كشف إنها
  غلط: على الديسكتوب مخفي لحد الهوفر فمفيش مشكلة، لكن **اللمس مفيهوش hover**
  فكان لازم يفضل ظاهر — وساعتها **بيغطي أول سطر من نص الرسالة**. ونقله برّه
  الفقاعة مكانش خيار: `.wa-msgs` عندها `overflow-y:auto` فأي حاجة خارجة
  بتتقص (نفس فخ زرار طيّ السايدبار — درس 31).
  وبكده بقى **سلوك واحد** على الماوس واللمس (ظاهر بشفافية .45 وبيوضح
  بالهوفر) بدل سلوكين مختلفين.
  ⚠️ فيه فحص بيقيس **التقاطع الهندسي** بين الزرار ونص الرسالة — لو حد رجّعه
  يعوم تاني الفحص بيقع.
- **رسالة من غير `wa_message_id` → مفيش زرار خالص** (درس 16) — واتساب بيطلب
  المعرّف، فزرار بيبان وهو مش شغال أسوأ من زرار مش موجود.
- **معاينة فوق خانة الكتابة** بترسم بـ`waQuoteBlock` نفسها — يعني اللي في
  المعاينة هو اللي هيبان في الشات **بالظبط**، مش تمثيل تاني ممكن ينحرف.
  وإلغاء بالزرار أو بـ**Esc**.
- **الفقاعة الفورية بتعرض الاقتباس** قبل رد السيرفر (زي اسم الموظف).
- 🔴 **التصفير في 3 مواضع**: نجاح الإرسال · الإلغاء · **تبديل المحادثة**.
  التالت هو الخطر الحقيقي: تختار رسالة، تفتح محادثة تانية، تكتب — فيلزق
  اقتباس من محادثة قديمة (والسيرفر هيرفضه بس الواجهة كانت هتكدب لحد ما
  يرفض). فيه معايرة بتشيل التصفير ده وتتأكد إن الفحص بيقع.
  ومعاهم **قفل النافذة**: لو الكتابة اتقفلت المعاينة بتختفي معاها بدل ما
  تفضل معلّقة فوق بانر «مش هتقدر ترد».
- ⚠️ **`waLastMsgs`** = آخر قايمة رسايل اترسمت. `waSetReplyTo` بتجيب منها
  الرسالة الكاملة (`waRenderedState` فيها الملخص بس: id/status/direction).

**درس من الاختبار:** فحص الـhit-test وقع أول مرة بسبب **عيب في الاختبار مش في
الكود** — `elementFromPoint` بتشتغل بإحداثيات **الشاشة**، والفقاعة اللي فوق
الطية بتدي نقطة واقعة على لوحة الأوردرات فوق الشات. الموظف نفسه بيسكرول
للرسالة قبل ما يرد عليها، فالفحص لازم يعمل `scrollIntoView` الأول. **بس نفس
الفشل هو اللي خلّى التصميم يتراجع وتطلع مشكلة التغطية الحقيقية** — الفشل
معلومة مش إزعاج (درس 38).

`test-wa-sender.mjs` بقى **48 فحص + 5 معايرات**.

---

## `AGENTS.md` — وكلاء غير Claude (7 سبتمبر)

المالك بيشتغل على نفس المشروع مع **Codex** كمان، وCodex بيقرا `AGENTS.md`
مش `CLAUDE.md`.

🔴 **الحل الساذج (نسخة يدوية تانية) هو أخطر حل**: نسختين بينحرفوا حتماً،
وبعد شهر كل وكيل بيشتغل على حقيقة مختلفة — وده أسوأ من إن الملف مش موجود
أصلاً (الوكيل ساعتها بيسأل بدل ما يفترض).

**الشكل المطبّق — نفس منطق صفحات الأقسام بالظبط:**
- **`CLAUDE.md` هو المصدر الوحيد للحقيقة.**
- `python3 tools/make-agents-md.py` بيولّد `AGENTS.md` = مقدمة متولّدة +
  **`CLAUDE.md` كامل حرفياً**.
- **فحص 13 في `check.py`** بيشغّل المولّد بـ`--check` ويرفض النشر لو اتغير
  أي حرف. اتعاير بأربع حقن: `CLAUDE.md` اتعدّلت والتوليد لأ · حد عدّل
  `AGENTS.md` بإيده · الملف اتمسح · وقرار محسوم جديد بيوصل للمتولّد فعلاً
  (ظهر مرتين: في الخلاصة وفي الملف الكامل) + ضابط.
- **المقدمة نفسها مستخرجة من `CLAUDE.md` مش مكتوبة في السكربت** — لو
  اتكتبت في السكربت كانت هتنحرف، وهي بالظبط الحتة اللي المفروض تمنع
  الانحراف. الأقسام المسحوبة: اللغة · المبدأ المعماري · قواعد الأمان ·
  أسلوب الشغل · القرارات المحسومة.
- وفوقهم قسم **«اقرا ده قبل ما تقترح أي حاجة»**: أكتر حاجة ضيّعت وقت في
  مراجعات Codex السابقة إنه بيقترح حاجة **اتقررت خلاص** أو **اتفحصت على
  الحالة الحية وطلعت غلط**. القسم بيوجّهه للقرارات المحسومة والمخاطر
  المفتوحة و`check.sh` قبل ما يبدأ.

⚠️ **بعد أي تعديل على `CLAUDE.md`: `python3 tools/make-agents-md.py`** —
زي `make-route-pages.py` بالظبط.

---

## قالب متابعة الأوردر بالواتساب (7 سبتمبر)

طلب المالك: الزرار «إرسال رسالة واتساب للعميل» في نافذة التفاصيل كان بيفتح
`web.whatsapp.com` **على جهاز الموظف** — الرسالة بتخرج من رقمه الشخصي، وده اللي
بياخد Ban كل شوية والتاجر يقعد 24 ساعة مش عارف يرد على حد.

**القالب المعتمد:** `order_shipping_confirm_ar` · Category **Utility** ·
Language **Arabic (EGY) = `ar_EG`** (مش `ar` — حرف غلط = الإرسال بيفشل) ·
من غير أزرار بقرار المالك («العميل هيرد علينا في الشات لوحده بعد المكالمة»).

- **`Edge Function` منفصلة `wa-followup` (v2) مش توسيع لـ`wa-send`.** عقد
  `wa-send` هو **الرد جوّه نافذة الـ24 ساعة** وفيها حارس صريح بيرفض أي إرسال
  بره النافذة. والقالب موجود **عشان** يتجاوزها — العميل اللي مردش على التليفون
  غالباً مبعتش حاجة أصلاً فنافذته مقفولة. خلطهم في دالة واحدة = فلاق بيلغي أهم
  حارس في `wa-send`، وأي غلطة بعدين بتبقى **رسايل مدفوعة بتتبعت من غير قصد**.
- 🔴 **الحمولة `order_id` بس.** الاسم ورقم الطلب واسم المنتج بيتقروا من صف
  الأوردر **على السيرفر**. لو جم من المتصفح، أي موظف يقدر يبعت أي نص لأي عميل
  **بره نافذة الـ24 ساعة** تحت غطا قالب موافق عليه — وده بيحرق الـWABA بتاع
  التاجر. نفس ثابت `wa-send` (`sent_by_name`) و`tenant-staff` (`tenant_id`).
- 🔴 **نص القالب في `tenants.wa_followup_body` مش محفور في الكود** — منه بتتبني
  الرسالة اللي بتتخزن في `wa_messages` والمعاينة اللي الموظف بيوافق عليها. لو
  اتكتب في الـEF: التاجر يعدّل قالبه عند ميتا والمتسجّل عندنا يفضل **يكدب**،
  وكل تاجر بقالب مختلف يحتاج نشر كود (كسر الـcentral). والـEF بترفض الإرسال لو
  النص مش متسجّل (`no_template_body`) — **خطأ إعداد أحسن من صندوق بيكدب**.
- **التطبيع دالة واحدة**: `app.wa_id_from_phone` + غلاف `public.wa_id_from_phone`
  (PostgREST بيكشف `public` بس) **مصرّح لـ`service_role` وحده**. نسخة تانية من
  المنطق في TypeScript = محادثة مكررة أو رسالة لرقم تاني.
- **الزرار بيستبدل اللينك القديم مايتعرضش جنبه** — وجود الاتنين معناه الموظف
  هيدوس على اللي اتعوّد عليه ويرجع يبعت من موبايله. التاجر اللي مالوش قالب
  (Trendose والـ5 التانيين) بيفضل على اللينك زي ما هو.
- **مودال تأكيد بالنص المرسوم كامل** — الرسالة بفلوس وبتوصل عميل حقيقي، فالموظف
  بيوافق على **اللي العميل هيقراه** مش على «رسالة متابعة». (`#cmodal-sub` اتضاف
  ليها `white-space:pre-line` — كانت بتلمّ سطور مودالات الشحن من زمان.)
- **`orders.wa_followup_sent_at`**: علامة باقية بعد ما النافذة تتقفل + حارس
  ضغطة مزدوجة 90 ثانية في الـEF (نفس مدة `order-ship`). ⚠️ **حارس تكلفة مش
  حارس أمان** — الموظف يقدر يكتب في العمود من الكونسول زي أي عمود في `orders`.
  اللي بيمنع الإرسال لمتجر تاني هو قراءة الأوردر **بتوكن المستخدم عبر RLS**
  جوّه الـEF (وبتطبّق قفل النفاد كمان).
- ⏳ **القالب لسه «In review» عند ميتا وقت البناء** — الإرسال هيفشل لحد
  الموافقة، والواجهة بتعرض **سبب ميتا الحقيقي** مش «حصلت مشكلة».
- ❌ **مفيش حارس حالة على الزرار — قرار المالك (7 سبتمبر).** الزرار شغّال على
  أي أوردر مهما كانت حالته، حتى المتشحن والمتسلّم. اتعرض عليه إننا نضيف حارس
  زي `order-ship` (pending/confirmed بس) و**رفض**: المودال بيعرض السياق
  والموظف هو اللي يقرر (درس 36). أي مراجعة تقترح الحارس ده غلطانة.

---

## إعلانات Click-to-WhatsApp — التقاط الـreferral (12 سبتمبر)

اتعملت في **سيشن تانية** على فرع `claude/whatsapp-api-facebook-ads-0yqfsq`،
واتدمجت على الإنتاج 14 سبتمبر.

العميل اللي بيدوس على إعلان CTWA رسالته بتيجي ومعاها object اسمه `referral`
جنب نص الرسالة — فيه `source_id` (معرّف الإعلان) و`headline` و`ctwa_clid`.
**الكود القديم كان بيرميه كله** (كان بيقرا `m.text.body` وبس).

- **`wa_messages.referral` (jsonb) خام** + 6 أعمدة مسطّحة على `wa_conversations`
  (`ctwa_clid` · `ctwa_ad_id` · `ctwa_headline` · `ctwa_source_type` ·
  `ctwa_first_at` · `ctwa_last_at`) + فهرس جزئي على `(tenant_id, ctwa_ad_id)`.
  الخام مقصود: لو ميتا ضافت حقول مانحتاجش migration تاني.
- **`ctwa_first_at` بتتكتب مرة واحدة** بحارس `.is('ctwa_first_at', null)` —
  العميل اللي رجع من إعلان تاني تاريخ أول دخول بتاعه مايتمسحش.
- 🔴 **`ctwa_clid` بييجي مع أول رسالة بعد الضغط بس** — لو ضاع مفيش طريقة
  نجيبه تاني، وهو اللي Conversions API بيحتاجه عشان ينسب الأوردر للإعلان.
- **`asText()` على الأعمدة النصية** — ميتا ممكن تبعت `source_id` كرقم
  وPostgREST بيرفض رقم على عمود `text`.
- **فشل تسطيح البيانات مايضيّعش الـreferral الخام** — الرسالة بتتسجّل الأول.
- `wa-inbox-ingest` بقت **v5**. اتأكد وقت الدمج إنها **لسه بتلتقط
  `reply_to_wa_id`** (ميزة 6 سبتمبر) — المرآة اتكتبت من v4 الحية مش من الصفر.
- 🔴 **باج اتكشف واتصلح بعد الدمج (14 سبتمبر — `wa-inbox-ingest` v6/v7):**
  `applyReferral` كانت بتبعت **كل** أعمدة الـ`ctwa_*` في UPDATE واحد، و
  `asText(undefined)` بترجع `null` — يعني **حقل غايب كان بيدعس القيمة المحفوظة**.
  والحقل اللي بيتدعس هو أغلى حاجة في الميزة: `ctwa_clid`.

  **ليه ده مش نظري:** توثيق ميتا بيقول حرفياً عن الحقل ده
  «*The `ctwa_clid` property is **omitted entirely** for messages originating
  from an ad in WhatsApp Status*» — وStatus placement بيتحط تلقائي في
  Advantage+. فالسيناريو: العميل يدوس إعلان في الفيد (الـclid اتسجّل) →
  بعدين يدوس إعلان في الـStatus (referral من غير clid) → **الـclid يتمسح** →
  Conversions API مايقدرش ينسب الأوردر للإعلان. وميتا بتبعت الـclid **مرة
  واحدة بس** بعد الضغط فمفيش طريقة نجيبه تاني.

  **الإصلاح:** الـUPDATE بقى بيتبني من المفاتيح **الموجودة بس** — قيمة موجودة
  بتستبدل، وغايبة بتسيب القديم. (`ctwa_first_at` كان محمي أصلاً بحارس
  `.is(..., null)` — هو الوحيد اللي كان مظبوط.)

  **اتأكد بطبقتين حتميتين** (من غير كتابة في الإنتاج): ترانزاكشن راجعة أثبتت
  إن الشكل القديم بيحوّل الـclid من قيمة لـNULL وإن الجديد بيحافظ عليه ويحدّث
  `ad_id` عادي · وفحص Node على منطق `keep()` **المنقول حرفياً من المنشور**
  بحمولة ميتا الحقيقية + **معايرة** بتثبت إن الشكل القديم بيحط `null` صريحة.
  ⏳ والتأكيد السلوكي الحي لسه مستني أول رسالة CTWA حقيقية (درس 26).
  ⚠️ **v7 = v6 + تصحيح تعليق اتشوّه في الترميز وقت النشر** — الكود واحد.

### ⚠️ حاجات مفتوحة بعلم المالك

- ✅ **~~`ctwa_*` قابلة للكتابة من المتصفح~~ — اتقفلت 14 سبتمبر**
  (migration `lock_ctwa_columns_from_browser`). كانت أي موظف نشط يقدر يزوّر
  «العميل ده جه من إعلان كذا» من الكونسول — و`inbox.js` بيعمل `update({labels})`
  و`update({note})` بتوكن المستخدم أصلاً فمحتاج devtools بس. والرقم متوجّه
  لـConversions API يعني تزوير بياخد قرارات صرف عند ميتا.

  **السبب كان معماري مش قرار:**

  | الجدول | منحة الجدول لـ`authenticated` | أعمدة ليها منحة خاصة |
  |---|---|---|
  | `wa_messages` | `rdm` (مفيش `a`/`w`) | 14 من 18 |
  | `wa_conversations` | `arwdm` (فيها `a` و`w`) | **0** من 21 |

  فـ`referral` طلع محمي **بالصدفة** (الجدول مالوش `a`/`w`)، و`ctwa_*` مفتوحين
  لنفس السبب بالعكس. و🔴 **`REVOKE UPDATE (ctwa_*)` كان هيعدّي في صمت** —
  مفيش منحة عمود يشيلها (درس 41). الشكل اللي اتطبّق: REVOKE على الجدول +
  GRANT بالـ15 عمود الشرعيين (= كل الأعمدة إلا الـ`ctwa_*`).

  اتجرّب في ترانزاكشن راجعة **وبعدين اتقاس حيّ بعد التطبيق** بأربع ضوابط:
  الواجهة لسه بتكتب (`unread_count` · `labels` · `note` — الـ3 الوحيدين اللي
  بتكتبهم فعلاً) · `SELECT *` شغّال · التزوير **اتمنع** · و`service_role`
  (اللي `wa-inbox-ingest` شغّال بيه) لسه بيكتب عادي.
  ⚠️ **أي عمود جديد على الجدول ده بقى مايخدش INSERT/UPDATE تلقائي** — لازم
  يتضاف للقايمة في الـmigration صراحةً. الافتراضي بقى **مقفول**، وده المقصود.
- ⏳ **الميزة اتنشرت ولسه ماشتغلتش ولا مرة** — صفر محادثة فيها `ctwa_ad_id`
  وصفر رسالة فيها `referral` (بيانات الاختبار اتمسحت). درس 26: «مفيش تنفيذ»
  مش «شغّال».
- ✅ **~~مفيش ولا سطر في الداشبورد بيقرا الأعمدة دي~~ — اتعرضت 14 سبتمبر**
  (قسم «شارة جه من إعلان» تحت). الداتا كانت بتوصل المتصفح وتترمى —
  نفس عيلة `manufacturer_note` بالظبط.
- ✅ **~~المرآة ناقصة~~ — اكتملت 14 سبتمبر** (9 من 9) ومعاها فحص 14
  في `check.sh`. تفاصيل وحد التحقق في قسم هيكل الملفات فوق.

### شارة «جه من إعلان» في الصندوق (14 سبتمبر)

طلب المالك: **عنوان الإعلان بس، والشارة تظهر في الاتنين** — المحادثة
المفتوحة وقايمة المحادثات.

- **`waCtwaText(c)`** في `inbox/inbox.js` هي المصدر الوحيد لنص الشارة — القايمة
  والهيدر بيقروا منها سوا (درس توحيد المصدر في `orderProps`).
- 🔴 **معرّف الإعلان (`ctwa_ad_id`) عمره ما بيتعرض** — الموظف بيرد على عميل،
  مش بيحلل حملات. فيه فحص بيمسح الصفحة كلها ومعايرة بتحقن عرضه.
- **إعلان من غير عنوان → «جه من إعلان» وخلاص.** ميتا مش دايماً بتبعت
  `headline`، وعنوان مخترع من المعرّف أسوأ من مفيش عنوان (نفس قاعدة
  «↖︎ رد على رسالة أقدم» ونسبة الموظف).
- 🔴 **`waUpdateCtwa` بتتنادى من موضعين وكل واحد بيغطي مسار لوحده** — مش تكرار:
  - من `renderConvos` → العميل بيدوس الإعلان **والشات مفتوح قدام الموظف**،
    فالشارة لازم تظهر مع أول جلب مش عند إعادة الفتح.
  - من `openConversation` → **إشعار المتصفح** (`inbox.js:611`) بيفتح محادثة بالـid
    وهي ممكن تكون لسه مش في القايمة، وساعتها `renderConvos` مابتلقاهاش
    فمابتصفّرش الشارة — وشارة عميل تاني بتفضل معلقة فوق اسم ده.
    ⚠️ **المعايرة هي اللي كشفت ده**: أول معايرة شالت نداء `openConversation`
    و**ماوقعش أي فحص** — وقتها بان إن الفحص ناقص، مش إن النداء زايد.
- **العنوان بيتقص بـ`…` مش بيمدّ التخطيط** — `.wa-chat-id{min-width:0}` هي اللي
  بتخلي القص يشتغل جوّه هيدر flex (فيه فحص هندسي على 1440 و390).
- ⚠️ **صندوق المحادثات بقى مفتوح في المعاينة**: `preview-stub.js` كان بيرجّع
  `wa_inbox_status = {verified:false}` و`wa_conversations: []`، فالصفحة كانت بتقفل
  على بانر «ركّب واتساب» و**مفيش أي ميزة شات كانت قابلة للتجربة فيها**.
  دلوقتي فيه 4 محادثات وهمية: إعلان بعنوان · عادية · إعلان بلا عنوان ·
  محادثة اتعملت مع الأوردر ولسه مفيهاش رسايل. **ومعرّف الإعلان محطوط
  في داتا المعاينة عمداً** — لو بان على الشاشة يبقى فيه حاجة غلط.
  ومعاه اتصلحت ثغرة في الستب: `eq('conversation_id')` مكانش بيتطبّق، فكل
  محادثة كانت هتعرض رسايل كل المحادثات.
- ✅ **~~لسه مفيش ولا التقاط حي~~ — أول التقاط حقيقي 16 سبتمبر 3:01م.**
  الالتقاط اشتغل 100%: `referral` خام بـ9 حقول · `ctwa_clid` **موجود** ·
  `ctwa_ad_id` · `ctwa_first_at`=`ctwa_last_at`. **الباج اللي صلحناه
  (مسح الـclid) كان هيضيّع الـclid ده لو ماكانش اتصلح.**

### 🔴 والالتقاط الحي كشف إن الحقل اللي بنعرضه غلط (16 سبتمبر)

المالك: «جت رسالة من عميلة بس مش عارف من أنهي إعلان بالظبط؟» — والشارة
كانت شغالة وبتعرض **«3ataba.com — عتبة دوت كوم»**.

**`referral.headline` اللي ميتا بتبعتها = اسم الصفحة، مش اسم الإعلان.**
اتقاس: الإعلان اسمه `KitchenOrganizer` (اتأكد من Meta Ads API —
`120235247618060034`، حملة KitchenOrganizer، ACTIVE) والـheadline
«3ataba.com — عتبة دوت كوم». يعني **نفس النص على كل إعلانات المتجر** —
شارة موجودة وبتقول صفر معلومة.

**اللي بيعرّف الإعلان فعلاً موجود عندنا أصلاً**: `referral.body` = كوبي
الإعلان («🙄 مطبخك زحمة والرُخامة مليانة مواعين وأدوات؟ … منظم المطبخ
المتكامل») — سطر واحد منه والموظف عرف. الخام كان متخزّن في
`wa_messages.referral` من الأول، فمفيش داتا ضاعت.

**الإصلاح:**
- عمود `wa_conversations.ctwa_ad_body` (migration
  `fix_ctwa_insert_grant_and_add_ad_body`) + تعبئته من الـreferral الخام
  المتخزّن (مش backfill لداتا مش موجودة — نفس الداتا بتتنقل لعمود).
- `wa-inbox-ingest` **v8**: `keep("ctwa_ad_body", last?.body)`.
- `waCtwaText` بقت سلسلة: **body → headline → «جه من إعلان»**.
  ⚠️ الـheadline **ماتشالتش**: الالتقاطات اللي قبل 16 سبتمبر مالهاش body،
  و**عينة واحدة** هي اللي أثبتت إنه اسم الصفحة — ماينفعش نحكم إنه غلط دايماً.
- التلميح بقى فيه نص الإعلان **كامل**، والشارة **أول سطر بس**.

### لينك الإعلان في الشارة (16 سبتمبر — طلب المالك)

طلبه الحرفي كان «حط اللينك في التلميح»، و**التلميح (`title`) مايقبلش لينك
يتضغط** — خاصية نص مش HTML. فالشكل اللي اتنفّذ: **الشارة نفسها بقت لينك**،
والتلميح بيعرض العنوان كنص عشان الموظف يشوف رايح فين قبل ما يدوس.

- عمود `ctwa_source_url` (migration `ctwa_source_url`) + `wa-inbox-ingest` **v9**.
  ✅ **العمود اتقفل تلقائي** على المتصفح من غير أي سطر في الـmigration —
  ده أثر إصلاح الـGRANT، واتقاس في ترانزاكشن راجعة قبل التطبيق.
- 🔴 **`waCtwaUrl` بتفلتر على `^https?://` قبل ما ترسم الـhref.** العنوان جاي
  من حمولة خارجية، و`javascript:` في `href` **بيتنفّذ عند الضغط** — يعني XSS
  بضغطة. فيه فحص ومعايرة بتشيل الحارس وتتأكد إن الـhref بقى `javascript:`.
  (⚠️ `new URL()` لوحدها **مش حارس** — بتقبل `javascript:` عادي.)
- **`rel="noopener noreferrer"` إلزامي** مع `target="_blank"` — من غيره الصفحة
  المفتوحة بتوصل لـ`window.opener` وتقدر تنقّل لوحة التاجر لأي مكان.
- 🔴 **`stopPropagation` على لينك الصف**: اللينك جوّه صف قابل للضغط، فمن غيره
  الضغطة بتفتح الإعلان **وتبدّل المحادثة** في نفس الوقت — الموظف بيروح لشات
  تاني وهو بصّ على تاب جديدة فمايلاحظش. فيه معايرة بتشيله وتتأكد إن الفحص بيقع.
- **الشارة بتفضل ظاهرة حتى لو مفيش لينك صالح** — النص أهم من اللينك.
- ⚠️ الهيدر بقى `<a>` مش `<div>`. من غير `href` مايتضغطش ومايخدش focus،
  فده الوضع الطبيعي للمحادثة اللي مالهاش لينك.

⚠️ **باقي حقول `referral` لسه في الخام ومش مسطّحة**: `thumbnail_url` ·
`video_url` · `media_type` · `welcome_message`. مش ضايعة — `wa_messages.referral`
بيحفظ الـobject كامل، وأي عمود جديد يتعبّى منه في أي وقت (زي ما حصل مع
`ctwa_ad_body` و`ctwa_source_url`).

**درس 26 بالحرف:** الشكل اتراجع (الشارة بتترسم صح) والسلوك لأ (الحقل غلط).
وقت البناء كان فيه **صفر التقاط** فماكانش فيه أي طريقة نعرف — والسؤال
اللي كان المفروض أسأله للمالك مش «عنوان ولا عنوان+معرّف؟» بل **«تعالى
نشوف أول رسالة حقيقية الأول»**.

### 🔴 وثغرة تانية اتكشفت في نفس الشغل — INSERT كان لسه مفتوح

migration `lock_ctwa_columns_from_browser` (14 سبتمبر) اتكتب فيه:
`GRANT INSERT, UPDATE (<15 عمود>)`. **وفي SQL قايمة الأعمدة بتلزق
بالصلاحية اللي قبلها مباشرةً بس** — فالنتيجة كانت UPDATE بالأعمدة (صح)
و**INSERT على مستوى الجدول** (غلط). يعني التزوير اتقفل في مسار الـUPDATE
وفضل مفتوح في مسار الـINSERT، وسياسة `wa_conv_insert` بتسمح لأي موظف نشط
يدخل صف لمتجره — فكان يقدر **يخلق محادثة بنسبة إعلان مخترعة**.

اتقاس على الحي: `relacl` كانت `authenticated=ardm` (فيها `a`)،
و`has_column_privilege(...,'ctwa_clid','INSERT')` = **true** بينما
`UPDATE` = false. الإصلاح: `GRANT INSERT (عمود)` لكل عمود لوحده،
والنتيجة `authenticated=rdm` — بالظبط زي `wa_messages` المقفول صح.

اتأكد بانتحال موظف حقيقي بستة فحوص **كلها بضوابط**: القراءة شغالة (1773
محادثة) · `ctwa_ad_body` مقروء · الواجهة لسه بتكتب `note` · **التزوير
بـINSERT اتمنع** · **INSERT شرعي من غير ctwa نجح** (يثبت إن المنع من
العمود مش من المسار) · التزوير بـUPDATE اتمنع.

⚠️ **وأنا كتبت البند ده «اتقفلت» وهو نصه مقفول.** درس 41 كان عن
column-vs-table في الـREVOKE، ودي نفس العيلة في الـGRANT — وفاتت مني
لأني قست الـUPDATE بس.

### ملحوظة على الدمج
الفرع كان متفرّع من `master` بتاع **30 يوليو** — ورا الإنتاج بـ103 كوميت.
الدمج التلاتي جاب **الفرق الحقيقي بتاعه بس** (3 ملفات) ومالمسش `app/` خالص.
⚠️ **بس الـ`app/` اللي جوّه الفرع ده نسخة 6 أسابيع قديمة** — رفعها على
Cloudflare كان هيرجّع اللوحة ورا. أي فرع جديد يتفرّع من فرع الإنتاج مش من `master`.

---

## أجنت الإعلانات — الدفعة الأولى (16 سبتمبر)

تلات طلبات من المالك في جلسة واحدة. كلها في صندوق المحادثات.

### 1) فلتر «📣 جه من إعلان»

chip في شريط الفلاتر بيجمّع كل المحادثات الجاية من إعلان CTWA.

- **`waIsFromAd(c)`** هي المصدر الوحيد للشرط، و**`WA_AD_OR`** هي نفسه بلغة
  PostgREST. **لازم يفضلوا متطابقين** — لو اتفرّقوا هتلاقي محادثة عليها
  شارة 📣 ومش بتظهر في الفلتر (فلتر بيكدب). فيه فحص بيتأكد إن كل صف
  معروض في الفلتر عليه الشارة فعلاً.
- 🔴 **التصنيف ده مش `label`.** الـlabels بيكتبها الموظف في `labels[]`،
  وأعمدة `ctwa_*` **مقفولة عن المتصفح عمداً** (14 سبتمبر). فهو chip نظام
  زي «غير مقروءة»، مالوش مدخل في `WA_LABELS` ولا في `waToggleLabel`.
- 🔴 **بيستعلم من السيرفر مش من المحمّل.** `waFetchConvos` بتجيب **أحدث
  200 محادثة بس** و`waConvMatches` بتفلتر في الذاكرة — فلتر يقرا من
  `waConvos` لوحدها بيقول «عندك محادثتين من إعلان» والداتابيز فيها 9،
  والباقي بيختفي في صمت (درس 6). فالفلتر بيضرب استعلام بشرطه.
- ⚠️ **والنتيجة في مصفوفة منفصلة (`waAdExtra`) مش مدموجة في `waConvos`.**
  الدمج اتجرّب وطلع إن **ترتيب وصول الاستعلامين** هو اللي بيحدد النتيجة:
  الإعلانات لو نزلت الأول بتتعلّم «إضافية»، وبعدين الجلب العادي بيجيب
  نفس الصفوف ضمن الـ200 فتتشال منه — واختفت محادثتين من القايمة العادية
  في الاختبار. بالفصل، `waConvos` بتفضل **نسخة السيرفر زي ما هي** فعدّادات
  غير المقروء والتصنيفات وشارة القايمة الجانبية مالهاش أي علاقة بالفلتر.
- **`waConvById`** بتبص على الاتنين — من غيرها الضغط على صف من الفلتر
  بيفتح الشات وهيدره فاضل بتاع المحادثة اللي قبلها.
- العدّاد على الـchip بيتحسب **من أول رسمة** (جلب الإعلانات بيحصل مع
  `loadInbox`) — رقم بيتغير لما تدوس عليه = رقم بيكدب قبل الضغطة.
- رسالة الفراغ فيها **سياق**: «المحادثات اللي قبل تفعيل تتبع الإعلانات
  مالهاش بيانات إعلان حتى لو جت من إعلان». من غير السطر ده التاجر بيقرا
  الصفر على إنه عطل (درس 36) — وده واقع فعلاً: **3 محادثات بس** فيها
  `ctwa` من 1,783 وقت البناء (أول التقاط 16 سبتمبر 3:01م).
- ملاحظة السقف مابتقولش «الأقدم مش بيظهر» وقت الفلتر ده — الفلتر شافه.
- 🔴 **ونص الشارة بقى اسم الإعلان** (بلاغ حي 16 سبتمبر مساءً): إعلانين
  مختلفين طلع ليهم **نفس الكوبي بالحرف** (`FOMO HOOK` و`realone`)، فالشارة
  بتقول نفس الكلام على الاتنين. نفس درس 44 على مستوى أعمق — `headline`
  مكانش مميّز و`body` كمان مش مميّز. الاسم **مش بييجي في الـwebhook**
  فجه جدول `ctwa_ads(tenant_id, ad_id, ad_name)`، والكتابة **لأدمن المتجر
  بس** (الاسم بيتعرض كمصدر العميل، فتزويره زي تزوير نسبة الإعلان).
  السلسلة: **الاسم → الكوبي → الـheadline → «جه من إعلان»**.
  ⚠️ الاسم محتاج تحديث يدوي مع أي إعلان جديد — ربط token مؤجّل.
- ⚠️ **ومحادثة من غير أي شارة مش عطل**: الـ`referral` بيتبعت مع **رسالة
  الضغط على الإعلان بس**. العميل اللي فتح الشات من قايمة محادثاته أو جابله
  الرقم من اللاندنج مالوش بيانات إعلان **ومفيش طريقة نجيبها بعدين**.
  اتقاس 16 سبتمبر: 6 إعلانات شغالة بـ1,497 ضغطة و**3 referral بس** —
  يعني أغلب الإعلانات مش Click-to-WhatsApp أصلاً.

### 2) ردود جاهزة بصور

`wa_quick_replies.media jsonb` = `[{path,mime,name}]` · الملفات في
`wa-media` تحت `<tenant_id>/quick-replies/` فسياسة الـStorage الموجودة
بتعزلها من غير سياسة جديدة. سقف **5 صور** بـCHECK، وCHECK تاني بيمنع رد
من غير نص ومن غير صور (وده اللي بيخلي `body` يقدر يبقى فاضي).

- ~~🔴 مفيش إرسال بضغطة واحدة (الرد بيتحضّر فوق الخانة)~~ — **اتلغى بقرار
  المالك 24 سبتمبر**: بقى زرار باسم مختصر بيبعت على طول. شوف قسم «الردود
  المحفوظة زراير بضغطة» تحت. (بنود «التصفير» و«مرفق بالإيد» تحت اتشالت معاه —
  مفيش حاجة محضّرة تتصفّر.)
- 🔴 **الكلام `caption` على آخر صورة** (قرار المالك بعد التجربة الحية —
  درس 46). النسخة الأولى بعتت الصور بالتسلسل وبعدها الكلام رسالة مستقلة،
  والعميل شاف «صورة ← كلام ← صورة»: نداءاتنا خرجت مرتبة (اتقاس) والترتيب
  اتقلب **عند ميتا**، اللي موثّق إنها مابتضمنش الترتيب. بالـcaption عدد
  الرسايل N مش N+1 والكلام **يستحيل** يتوسط. ⚠️ وحد الـcaption **1024
  حرف** (موثّق) — الأطول بيرجع رسالة مستقلة.
- صورة في النص وقعت = **بنقف**. العميل استلم صورتين من تلاتة وكلام
  بيتكلم عن التالتة = رسالة بتلخبط أكتر من رسالة ماوصلتش.
- الصور بتترفع **مرة واحدة وقت الحفظ**، وكل إرسال بيستخدم نفس المسار.

🔴 **وثغرة كانت موجودة قبل الشغل ده — `wa-send` v8:**
`image_path` جاي من المتصفح وبيتحوّل لـsigned URL بـ**service role**، يعني
بيتخطى سياسة الـStorage. الحارس الموجود على `reply_to` (التحقق بالتلاتة)
**مكانش موجود على المسار خالص** — فموظف كان يقدر يبعت مسار بتاع **متجر
تاني** ويستقبل صورته على رقم هو بيتحكم فيه. بقى فيه `bad_media_path`:
المسار لازم يبدأ بـ`<conv.tenant_id>/` ومفيهوش `..`. اتنشر **من المرآة
نفسها** فالنسختين مطابقتين بالبناء، وفيه فحص في `check-functions.py`
(بقى 16 عقد) + فحص منطق خالص في `test-wa-qr.mjs` بالحمولات الحقيقية.

### 3) «إنشاء طلب» يدوي من الشات

RPC **`create_manual_order`** SECURITY DEFINER — انحراف مقصود ومبرر، نفس
منطق `save_order_products`. لو INVOKER كان الموظف هيدخل الصف بنفسه
(`INSERT` على `orders` مفتوح لـauthenticated بالـRLS أصلاً) ويحط أي حاجة.

- 🔴 **الحالة بتتقرر على السيرفر — والمتصفح عمره ما بيبعتها.**
  **⚠️ اتغيّرت 19 سبتمبر (طلب المالك): الأوردر بينزل `confirmed` مش
  `pending`** — «العميل أصلاً طالبه واتساب بنفسه وأكّد معانا». والقياس
  بيأيّده: كل الـ9 أوردرات اليدوية أول سطر في `status_log` بتاعها
  `pending → confirmed` بإيد موظف **خلال 5–13 ثانية**.
  🔴 **بس مش بـ`INSERT` مباشر بـ'confirmed'** — `charge_order_on_status_change`
  أول بند فيها إن أوردر بيدخل **لأول مرة** بحالة محاسَبة = **صفر خصم**
  (استيراد بوسطة)، و`confirmed` حالة محاسَبة. يعني الإدخال المباشر =
  أوردر **مجاني للأبد** (البند التالت بيعدّي أي تحويل بعد كده كمان).
  الشكل المنفّذ: `INSERT pending` ثم `UPDATE` لـ`confirmed` **في نفس
  الترانزاكشن**، فالخصم بيولّع على التحويل بالظبط زي ما كان بيحصل لما
  الموظف يدوس بإيده. اتقاس في ترانزاكشن راجعة **بضابط**:

  | المسار | `billed_at` | العدّاد | محفظة |
  |---|---|---|---|
  | `INSERT` مباشر `confirmed` | false | +0 | 0 |
  | …وبعد التحويل لـ`bosta_assigned` | لسه false | +0 | 0 |
  | `INSERT pending` ثم `UPDATE` | **true** | **+1** | **1** |

  🔴 **والمحاسبة اتلغت بعدها بساعات (19 سبتمبر) — والخطوة دي فضلت.**
  ده مش سهو: السبب الأصلي (الخصم) مات، بس **فيه سببين لسه حيّين**
  ومكفيين لوحدهم:
  1. **سطر `status_log`.** `INSERT` مباشر بـ`'confirmed'` بيدي أوردر
     **سجل حالته فاضي تماماً** — نافذة التفاصيل بترسم التايم لاين من
     السجل ده، فالأوردر بيبقى بلا تاريخ. بالـ`UPDATE` بيتكتب
     `قيد الانتظار ← مؤكد · 👤 <الموظف>` زي أي أوردر تاني.
  2. **التلجرام.** `tg_log_status_change` بتقرا آخر مدخل في السجل، فبشكل
     مطابق الإشعار اللي بيوصل المالك **نفسه** زي ما كان.
  ⚠️ **فأي سيشن جاية تقرا «المحاسبة اتلغت» وتشيل الـ`UPDATE` هتضيّع
  الاتنين في صمت** — والأوردر هيبقى بلا سجل ومن غير إشعار، من غير أي
  خطأ في أي مكان. (نفس عيلة درس 24: السبب المكتوب اتغيّر والقرار فضل صح.)
  والتمن الوحيد (خصم 0.75 من رصيد لا نهائي) بقى **بلا معنى** أصلاً.
- 🔴 **`order_uid` في نطاق `W-n` منفصل** (قرار المالك). الترقيم الحالي
  بييجي من اللاندنج (`body.short_id` في نود `Insert Order to Supabase`)
  وعليه `UNIQUE(tenant_id, order_uid)`. لو الأوردر اليدوي أخد `max+1`،
  أول أوردر جاي من الويبهوك بنفس الرقم **يقع في n8n ويضيع في صمت**
  (النود عليها `onError:continueErrorOutput`). التخصيص بقفل advisory.
- `tenant_id` من `app.current_tenant_id()` — نفس الثابت الحاكم.
- التليفون بيتطبّع بـ**نفس** دالة تريجر المحادثات (`app.wa_id_from_phone`)
  وبيتخزن محلي `01xxxxxxxxx` = شكل **4,051 من 4,076** صف (اتقاس). رقم
  الدالة مش فاهماه = **رفض**: رقم مش مفهوم يبقى أوردر مالوش محادثة
  ومايتشحنش.
- عنوان < 10 حروف مرفوض — نفس عتبة `order-ship` بالحرف. والرفض هنا أرخص:
  هناك الأوردر بيتسجّل ومايتشحنش والموظف يكتشف بعدين.
- cooldown 90 ثانية على نفس التليفون (نفس `order-ship` و`wa-followup`).
- **منصة `whatsapp`** اتضافت للـCHECK ولـ`normalize_order_platform`
  ولخريطة `PLAT` في الإحصائيات. 🔴 من غير **التريجر** القيمة بتتحوّل
  `other` في صمت — اتقاس في ترانزاكشن راجعة: أول محاولة طلعت `other`
  بالظبط لأني نسيت التريجر. البوابة هي التريجر مش الـCHECK.
- **`orders.created_by`** — مين عمل الأوردر. مش FK (زي `upsell_events.user_id`)
  و⚠️ **مش محصّن ضد التزوير**: `orders` منحته على مستوى الجدول ومفيش أي
  منحة بالعمود، فأي موظف يقدر يعدّله من الكونسول. **للمتابعة مش للمحاسبة**
  — نفس تصنيف `wa_followup_sent_at`. قفله يحتاج REVOKE على الجدول + GRANT
  بالـ56 عمود، وده خطر حقيقي على أكتر جدول بيتكتب فيه (درس 43).
- الفورم بتتعبّى من المحادثة (الاسم والتليفون) — ده الفرق بين «فورم»
  و«فورم من الشات». والتليفون بيتحوّل من `wa_id` (`20…`) للشكل المحلي.
- اسم المنتج بصيغة n8n بالحرف **«الاسم (عدد N)»** — أي شكل تاني بيكسر
  `parseProductItems` ومحرر المنتجات. وقايمة المنتجات من `v_stock_products`
  عشان الاسم يطابق المخزون (اسم مش موجود = التكلفة مش هتتحسب).
- كل رفض بيتقال بسببه الحقيقي والفورم بتفضل مفتوحة بالبيانات.
- 🔴 تبديل المحادثة بيقفل الفورم — الحقول متعبّية من محادثة قديمة.
- ⚠️ **مفيش رسالة تأكيد آلية** للأوردر ده: `CentralORDERS` بيتنده من
  ويبهوك اللاندنج مش من `INSERT` على الجدول. وده **صح هنا** (العميل قاعد
  في الشات)، ومكتوب صراحةً تحت الفورم عشان محدش يستنى رسالة مش جاية.

### 🔴 لقطة من الصلاحيات — نفس عيلة درس 41

`REVOKE ALL … FROM PUBLIC` على الدالة **مشالش منحة `anon`**: Supabase
بيمنحها **صراحةً** (`anon=X/postgres` في الـproacl)، والـREVOKE على PUBLIC
مالوش علاقة بمنحة الدور. مش قابلة للاستغلال (الدالة بتقرا `auth.uid()`
فـanon بياخد `not_allowed`) بس دالة definer بتدخل أوردرات ومتاحة لـanon
هي فخ مؤجّل. اتشالت في migration تاني واتقاست **بانتحال anon حقيقي**
(`insufficient_privilege`) ومعاها ضابط إن الموظف لسه بينشئ عادي.

**القاعدة:** بعد أي `REVOKE` على دالة، اقرا `proacl` واتأكد من الأدوار
واحد واحد — «الـREVOKE نجح» مش دليل إن الصلاحية اتشالت.

### ⏳ مفتوح بعلم المالك

- ❌ **الأوردر اليدوي مش بيحمل نسبة الإعلان** (`ctwa_ad_id`/`ctwa_clid`) —
  **قرار المالك 16 سبتمبر: مؤجّل لمرحلة تانية.** الأوردرات اللي هتتعمل
  قبل ما يتنفّذ مالهاش نسبة، والـ`clid` بييجي **مرة واحدة ومش بيترجع**.
  ده كان اقتراحي الأساسي لأجنت الإعلانات (تعرف الإعلان جاب كام أوردر
  بكام) ومقدمة `Conversions API`.
- ✅ **~~الأوردر اليدوي محصلش عليه تنفيذ حي~~ — اتأكد 20 سبتمبر**
  (`W-10` و`W-11`). القياس في «الأوردر اليدوي بينزل مؤكد» تحت.
- ✅ **~~الرد الجاهز بصور محصلش عليه تنفيذ حي~~ — شغّال في الإنتاج
  بحجم حقيقي.** اتقاس 20 سبتمبر على `wa_messages` (آخر 7 أيام):
  **50 إرسالة بصورتين**، **45 منها الكابشن على الصورة الأخيرة**
  والأولى `body` فاضي — بالظبط الشكل اللي اتقرر بعد درس 46. ومعاهم
  91 إرسالة بصورة واحدة. مثال حي: `16:57:38` صورة بلا نص ثم
  `16:57:40` صورة بكابشن 766 حرف، والاتنين `delivered`.
  ⚠️ **والتصحيح ده مهم**: البند كان مكتوب «لسه مفيش تنفيذ» وهو **غلط
  من أساسه** — التجربة الحية دي هي **اللي طلع منها درس 46 نفسه**
  (المالك شاف «صورة ← كلام ← صورة» فاتغيّر الشكل للكابشن). أنا كتبت
  «مفيش تنفيذ» والمالك صحّحني.
- ⚠️ **مرآة `supabase/migrations/` ناقصة migration اتنين من جلسة 16
  سبتمبر الصبح**: `fix_ctwa_insert_grant_and_add_ad_body` و
  `ctwa_source_url` — مطبّقين على الحي ومش متسجّلين في الريبو.

---

## «بدء شات مع رقم جديد» — قالب لعميل عمره ما كلّمنا (16 سبتمبر)

طلب المالك: «ساعات العميل بيكلمنا على الموبايل ويقولنا ابعتولي رسالة على
واتساب واحنا مش بنعرف نبعتله من الـAPI». السبب إن نافذة الـ24 ساعة **مافتحتش
أصلاً** — العميل عمره ما بعت حاجة، والقالب هو الطريق الوحيد.

### 🔴 والقالب طلع `Marketing` مش `Utility` — وده غيّر حساب المخاطرة

طلب المالك كان حرفياً «template utility». اتصحّح قبل البناء، وبعدين
**الـclassifier بتاع ميتا نفسه رفض التسجيل قبل الإرسال**: الدايالوج بيقول
«Category does not match» ومعاه «This message template **will be rejected**».

تعريفهم في الشاشة نفسها: **Utility = *Send messages about an existing order
or account*.** يعني الشرط مش «متابعة لطلب العميل» زي ما التوثيق العام بيوحي
(«follow up on user actions or requests») — لازم يكون فيه **أوردر أو حساب
قايم**. وصياغتي («بخصوص طلب حضرتك إننا نكلمك على واتساب») مافيهاش أوردر.

اتسجّل `Marketing` بقرار المالك. المقابل المقبول بعلمه: سعر أعلى · سقف
ميتا لرسايل الـMarketing لكل مستخدم · وتعرّض أكبر للبان — والرقم أصلاً
«بياخد بان كل شوية». اللي بيوازن ده: العميل **طلب الرسالة بنفسه من دقايق**
(أقوى `opt-in` ممكن — سياسة واتساب بتطلبه حرفياً)، والحجم رسالة أو اتنين
في اليوم، والحراسات تحت.
⏳ **والطريق لـ`Utility` معروف ومؤجل**: الموظف يعمل «إنشاء طلب» الأول
وبعدين القالب يتكلم عن رقم الأوردر — ساعتها `Utility` عن جدارة. بس ده
بيغيّر الترتيب فمابيخدمش حالة الاستفسار اللي لسه مش أوردر.

### الشكل

**جدول `wa_start_templates`** (migration `wa_start_templates`):
`(tenant_id, template_name, lang, label, body, params, category, enabled)`.

- 🔴 **قراءة بس من المتصفح — مفيش سياسة كتابة خالص.** تسجيل قالب مش فعل
  داشبورد: النص هنا لازم يطابق المسجّل عند ميتا **بالحرف**، وخانة في
  اللوحة بتسيب الأدمن يكتب نص مختلف معناها الصندوق بيعرض كلام العميل
  ماستلمهوش (نفس سبب `tenants.wa_followup_body`). التسجيل بـSQL وقت موافقة
  ميتا. اتقاس بانتحال موظف حقيقي **ومعاه ضابط**: القراءة نجحت (صف واحد) ·
  INSERT و UPDATE و DELETE اترفضوا بـ`insufficient_privilege`.
- 🔴 **`params` قيم ثابتة في الصف** — عمرها ما بتيجي من المتصفح. ده مش
  تشدّد: لو النص أو المتغيرات جم من الفرونت، أي موظف يبعت أي كلام لأي رقم
  **برّه نافذة الـ24 ساعة** تحت غطا قالب موافق عليه. نفس ثابت `wa-followup`.

**`Edge Function` `wa-start` (v1)** — تالتة مستقلة عن `wa-send` و`wa-followup`
لنفس السبب اللي فصلهم: عقد `wa-send` هو الرد جوّه النافذة (وفيه حارس صريح
بيرفض برّاها)، وعقد `wa-followup` قالب **مربوط بأوردر** (`order_id` بس).
الحالة دي مالهاش أوردر، فخلطها في أي منهم = فلاق بيلغي حارس أساسي.

الحمولة **`phone` + `template_id` + `name` بس** (`name` للصندوق مش لميتا).
الحراسات بالترتيب:
1. **التصريح = قراية القالب بتوكن المستخدم عبر RLS**، و`tenant_id` بييجي
   **من صف القالب** مش من الـbody.
2. **قفل النفاد صريح** — `wa-followup` بياخده ببلاش لأنه بيقرا `orders`
   (سياساتها فيها `wallet_depleted()`)، وإحنا مش بنقرا أوردر.
3. **`bad_template_params`**: عدد القيم المسجّلة لازم = أعلى `{{n}}` في
   النص، وإلا العميل بيستلم `{{2}}` حرفياً.
4. **الرقم بـ`app.wa_id_from_phone`** — نفس دالة تريجر المحادثات. نسخة
   تانية من المنطق = الرد بيروح لخيط تاني.
5. 🔴 **`window_open`**: العميل كلّمنا في آخر 24 ساعة → **رفض** وبنرجّع
   `conversation_id` والواجهة بتفتح الشات. قالب مدفوع والنافذة مفتوحة =
   فلوس بتتحرق على رسالة كانت ببلاش.
6. **`too_soon`**: قالب واحد لكل رقم كل 24 ساعة، **بيشمل قوالب
   `wa-followup`** عن قصد — الاتنين مدفوعين وعدّاد ميتا مابيفرقش.
7. **المحادثة بتتعمل بعد نجاح ميتا** — قبله كانت محاولة فاشلة بتسيب
   محادثة فاضية لعميل عمرنا ما كلمناه (نفس منطق `wa_followup_sent_at`).

**الواجهة**: زرار «➕ شات جديد» في هيدر قايمة المحادثات + فورم (رقم · اسم ·
قالب · معاينة).
- 🔴 **مفيش قالب مسجّل = الزرار مايبانش خالص** (درس 16) — زرار بيفتح فورم
  مالهاش قوالب وعد كاذب. التاجر اللي مالوش قالب عمره ما هيشوفه.
- 🔴 **مودال تأكيد بالنص المرسوم كامل** قبل أي إرسال — نفس عقد مودال
  المتابعة: الموظف بيوافق على **اللي العميل هيقراه**.
- ⚠️ **`waStartRender` في الفرونت نسخة طبق الأصل من `renderTemplate` في
  الـEF ولازم يفضلوا متطابقين.** التكرار هنا **مجبور** (المعاينة قبل
  الإرسال مالهاش سيرفر تسألها)، فالهارنس بيثبت التطابق بقالب حالات حدّية:
  قيمة فيها `$&` (مع `replaceAll` كانت هتتفك لنص المطابقة) وقيمة جوّاها
  `{{1}}` (مع التعويض على مراحل كانت هتتعوّض تاني).
- كل رفض بسببه الحقيقي، و`template_failed` بيعرض **تفصيلة ميتا**.

**الفحوص**: 8 عقود جديدة في `check-functions.py` (بقى 24) متعايرة بتلات
حقن + ضابط · و`test-wa-start.mjs` (37 فحص + 4 معايرات).

### ✅ اشتغلت حيّ من أول مرة (16 سبتمبر 20:37) — درس 26 اتقفل للميزة دي

المالك بعت لرقم `2011472089xx` والمسار كامل اشتغل، والدليل من `wa_messages`
مش من كلام:
- الرسالة اتبعتت بـ`wamid` و`sent_by_name = shekoz` (النسبة شغّالة).
- **`status = read`** — العميلة فتحت الرسالة.
- 🔴 **و`last_inbound_at` بعدها بدقيقتين (20:39:36)** — يعني **ردّت**،
  ونافذة الـ24 ساعة اتفتحت، والموظف بقى يقدر يرد ببلاش. ده **كل الهدف من
  الميزة** ومقيس مش مفترض.
- الاسم اللي الموظف كتبه («اية») نزل على المحادثة، فالقايمة مابتعرضش رقم أصمّ.

**وده أثبت بنية القالب كمان:** ميتا بترفض الإرسال لو عدد الباراميترات مش
مطابق للقالب المسجّل عندها (`132000`). الإرسال نجح بباراميتر واحد → القالب
فيه `{{1}}` واحد بالظبط زي المتسجّل عندنا.

### ⏳ مفتوح
- **صياغة نص القالب متسجّلة من معاينة ميتا في سكرين شوت المالك مش من الـAPI.**
  توكن الواتساب مالوش `business_management` فمش بيوصل لـ`WABA` node،
  و`assigned_whatsapp_business_accounts` رجع فاضي، و`owned_whatsapp_business_accounts`
  على البيزنس (`393281817211452`) رجع 403. **البنية اتأكدت** (فوق)،
  والفاضل الحروف نفسها — أثرها على **عرض الصندوق بس** مش على الإرسال.
  الحسم: نسخ من المالك، أو `waba_id` عشان
  `GET /{waba}/message_templates?name=chat_start_ar`.
- ⚠️ **سباق الضغطة المزدوجة**: حارس الـ24 ساعة بيقرا من `wa_messages`،
  فضغطتين في نفس اللحظة قبل ما الصف الأول ينزل ممكن يبعتوا رسالتين.
  الواجهة بتعطّل الزرار أثناء الطلب. نفس الفجوة الموجودة في `order-ship`
  بالحرف — مقبولة بنفس المنطق (أسوأ حالة رسالتين متطابقتين).

## تصنيفات المحادثات — 4 جداد + إصلاح فلتر كان بيكدب (19 سبتمبر)

طلب المالك: يضيف «استبدال» · «استرجاع» · «طلب واتساب» · «مكتمل».

### 🔴 والإضافة كشفت إن الفلتر نفسه أعمى

`waFetchConvos` بتجيب **أحدث 200 محادثة بس** و`waConvMatches` بتفلتر على
المحمّل في الذاكرة — يعني فلتر التصنيف عمره ما شاف حاجة أقدم من الـ200.
**اتقاس على الحي (19 سبتمبر):** 2,037 محادثة · المحادثة رقم 200 عمرها
**يومين** · 18 محادثة عليها تصنيف · **6 منها (الثلث) بره النافذة**.
يعني الشاشة بتقول «مفيش محادثات بالتصنيف ده» وهي موجودة — درس 6 بالحرف.
والتصنيفات الجديدة **طبيعتها** إنها على محادثات أقدم (استرجاع من أسبوع)،
فكانوا هيتولدوا مكسورين.

- **`waFetchLabelConvos`** بتستعلم `.not('labels','is',null).limit(500)`
  والنتيجة في **`waLabelExtra` منفصلة** — نفس شكل `waFetchAdConvos`/
  `waAdExtra` بالحرف. الدمج في `waConvos` مرفوض: اتجرّب في فلتر الإعلانات
  وطلع إن **ترتيب وصول الاستعلامين** بيحدد النتيجة واختفت محادثتين.
- **استعلام واحد لكل الـ9 تصنيفات** مش استعلام لكل chip — المحادثات
  المصنّفة مجموعة صغيرة بطبيعتها (18 من 2,037). وبيتنادى من `loadInbox`
  عشان العدّادات تبقى صح **من أول رسمة** (رقم بيتغير لما تدوس عليه = رقم
  بيكدب)، ومن `waSetFilter`، ومن `waToggleLabel` بعد أي تعديل.
- `waConvById` بتبص على `waLabelExtra` كمان — من غيرها الضغط على صف من
  الفلتر بيفتح الشات وهيدره فاضل بتاع المحادثة اللي قبلها.
- ملاحظة «الأقدم مش بيظهر» **مستثناة** وقت فلتر التصنيف (زي الإعلانات) —
  الفلتر ده شايف الأقدم فعلاً.

### الألوان — اتحسبت مش اتخمّنت

أول اختيار لـ«مكتمل» كان `#7c3aed` وطلع **تاني أسوأ مرشّح** (ΔE 19.2 من
`VIP`). الحساب بالـCIELAB على 16 مرشّح اختار **`#78350f`** (ΔE 44.9).
الأربعة الجداد كلهم ≥ 23.9 من أي لون تاني **ومن `#64748b`** — ده لون
الـfallback بتاع تصنيف **مجهول**، ولو تصنيف حقيقي أخده التاجر مش هيفرّق
بينه وبين «تصنيف مش في القايمة».
⚠️ أقرب زوج في اللوحة كلها `VIP ↔ متابعة` (ΔE 23.1) — **موجود من قبل**
ومالمستوش.

### 🔴 الشريط طلع بره عمود المحادثات خالص — على مرحلتين

**المرحلة الأولى (صباح 19 سبتمبر):** 9 تصنيفات في `flex-wrap:wrap` جوّه
العمود = **4 صفوف و135px**، وعمود المحادثات نزل من 4.7 لـ3.7 محادثة على
1280×720. الحل وقتها: `.wa-f-labels` صف واحد `overflow-x:auto` → 75px.

🔴 **والتجربة الحية رفضته** (بلاغ المالك بسكرين شوت): «الـtags دي كدا
صعبة أوي، هنفضل نسحب يمين وشمال هتكون رخمة». والقياس أيّده: التصنيفات
محتاجة **689px** والعمود بيدّي **315px**، يعني **374px مخفي — أكتر من
النص**.

**المرحلة التانية (قرار المالك):** الشريط اتنقل **بره العمود** وبقى ابن
مباشر لـ`.wa-wrap` بعرض النافذة كلها. المنطق بسيط: النافذة عنده
**1505px** والتصنيفات محتاجة 689px — كانوا محبوسين في 340px من غير سبب.

- `.wa-wrap` بقت `flex-direction:column`، والجزئين اتلموا في `.wa-panes`
  (`display:flex;flex:1;min-height:0` — **الـ`min-height:0` إلزامية**،
  من غيرها العمود بيرفض ينكمش والقايمة بتطلع بره الكارت).
- ومعاها **عنوان الصفحة اتشال** (`h1` + الوصف = 71px). 🔴 والاكتشاف
  اللي برّر ده: صفحة المحادثات كانت **الوحيدة** في اللوحة اللي ليها
  عنوان — الأوردرات والمخزون والماليات والإحصائيات والإعدادات كلهم من
  غير. فشيله **وحّدها** مش بس كسّب مساحة. و`.wa-wrap` بقت
  `calc(100vh - 144px)` بدل `- 215px`.

**المقيس بعد التغيير:**

| الشاشة | الفلاتر | زحلقة | محادثات (كانت) |
| --- | --- | --- | --- |
| **1793×860 (المالك)** | **صف واحد · 44px** | لأ | **7.3** (6.0) |
| 1440×900 | صف واحد · 44px | لأ | 7.8 (6.5) |
| 1280×720 | صفّين · 75px | لأ | 5.1 (4.2) |
| موبايل 390 | صفّين · 69px | **أيوه — بقصد** | 6.6 |

🔴 **والموبايل معكوس عمداً:** على 390px الـ12 chip محتاجين 948px = 4 صفوف
(سُدس الشاشة)، فالزحلقة بترجع **هناك بس** (`@media max-width:768`)، والشريط
بيختفي خالص جوّه `.show-chat` (إنت بتقرا شات، الفلاتر مالهاش لازمة).
⚠️ وارتفاع الموبايل اتظبط على `calc(100vh - 154px)` — أول محاولة (`- 130px`)
خلّت النافذة **تفيض 8px** تحت الشاشة، وفيه فحص بيقيس ده دلوقتي.

### ✅ اتنشر واتأكد حيّ (19 سبتمبر) — درس 26 اتقفل للتخطيط ده

المالك رفع v55 وجرّب: «جربت الشريط وشغال تمام». **والرفعة اتأكدت بالبايت
من الحي** مش بكلام:

| الملف | النتيجة |
|---|---|
| `css/21-inbox.css` | مطابق بالبايت |
| `js/inbox/inbox.js` | مطابق بالبايت |
| `/chats` | مطابق ما عدا **938 بايت** بيحقنهم Cloudflare وقت التقديم (`__CF$cv$params`) |
| `/billing` | **404** |

🔴 **الـ404 دي أهم سطر في الجدول:** `billing.html` اتمسح من الريبو، وإنه
اختفى من الحي كمان معناه الرفعة **استبدلت الفولدر** مش ضافت عليه — وده
بالظبط اللي بيفرّق بين رفعة كاملة ورفعة ناقصة بتفشل في صمت («كله أو
مفيش» — قسم النشر). أي تحقق من نشر بعد كده ياخد نفس الشكل: **ملف اتمسح
لازم يرد 404**، مش بس ملف اتغيّر لازم يبان جديد.

⚠️ **ودرس قياس:** أول عدّة للصفوف كانت `new Set(offsetTop)` وقالت «صفّين»
والحقيقة **صف واحد** — `VIP` كانت على `top=107` والباقي `106`، فرق **1px**
من تقريب الخط اللاتيني. أي عدّ صفوف لازم يبقى **بتسامح ≥3px**، وإلا
القياس نفسه بيكدب.

### ⚠️ الستب كان بيقبل `.not()` ومابيطبّقهاش

`tools/stub.js` و`preview-stub.js` الاتنين كانوا حاطين `not` في قايمة
الميثودز من غير أي تطبيق — يعني استعلام مضيّق بيرجّع **كل** الصفوف وأي
فحص عليه بيعدّي أعمى (درس 33). اتصلحوا، والفيكستشر في الهارنس اتعمل
**600 محادثة والمصنّفة القديمة عند 550** مخصوص عشان الـ`limit(500)`
تعضّ — كده تطبيق `.not()` بقى **حامل حمل فعلاً** ومعايرة (د) بتثبته.

`tools/test-wa-labels.mjs` — **40 فحص + 6 معايرات** (اتقاس بتشغيله مش
بالعدّ): 10 منهم على التخطيط — الشريط بره العمود · بعرض النافذة · صف واحد ·
صفر زحلقة · مفيش `h1` · الموبايل بزحلقة وبلا فيضان · وقفل «ركّب واتساب»
بيغطي الشريط (بـ`elementFromPoint` مش بالهندسة).

### ⏳ مفتوح بعلم المالك (مش من شغل النهاردة — موجود من قبله)

- **`waToggleLabel` read-modify-write**: موظفين اتنين بيصنّفوا نفس
  المحادثة جوّه نافذة الـpoll (20ث) = تصنيف بيضيع في صمت. الإصلاح الصح
  RPC بـ`array_append`/`array_remove` زي ما حصل مع `status_log`.
- **لوحة الاختيار بتبوظ بعد الـpoll**: زميل ضاف تصنيف والـpicker لسه
  فاتح من غير علامة — الضغطة بتعمل **عكس** اللي الموظف قاصده (بتشيل بدل
  ما تحط). الإصلاح سطرين في `renderConvos`.
- **«تم الحل» و«مكتمل» حالتين نهائيتين من غير قاعدة بينهم** — ممكن
  موظفين يستخدموا الاتنين لنفس الحاجة فكل فلتر يعرض نص الشغل.

---

## الأوردر اليدوي بينزل «مؤكد» (19 سبتمبر — طلب المالك)

التفاصيل الكاملة والجدول المقيس في قسم «إنشاء طلب يدوي من الشات» فوق.
الخلاصة: `INSERT pending` ثم `UPDATE confirmed` في نفس الترانزاكشن.

**اللي اتغيّر في الفرونت كمان:**
- نص الفورم بقى «بيتسجّل **مؤكد** … وبيتخصم من الباقة فوراً» — كان بيقول
  «قيد الانتظار» وده بقى **كذب مباشر على الشاشة**.
- **`loadWalletState()` بعد النجاح** — الإنشاء بقى بيخصم، فشريط الباقة
  كان هيفضل على رقم قديم، والأخطر إن `is_depleted` مايتحدّثش فالواجهة
  مش هتقفل لو الأوردر ده هو اللي وصّل المحفظة للنفاد.
- 🔴 **زرار «✓ تأكيد» اتخفى على الأوردر المؤكد أصلاً** — مفيش حارس
  نفس-الحالة في `set_order_status`، فالضغطة كانت بتكتب سطر «مؤكدة ←
  مؤكدة» في السجل. كان نادر لما التأكيد كان بإيد الموظف؛ وبعد ما كل
  أوردر يدوي بقى بينزل مؤكد بقى الموظف يفتح ويدوس بالعادة فيلوّث
  التايم لاين اللي المالك بيقرا منه (درس 16). الرجوع لـ«مؤكد» من حالة
  تانية لسه شغّال.
- المعاينة بقت بتخلق الأوردر `confirmed` بسجل حالة حقيقي — كانت `pending`
  بسجل فاضي، يعني المالك بيراجع شاشة مش زي الإنتاج.

### ✅ اتأكد حيّ (20 سبتمبر) — درس 26 اتقفل للميزة دي

المالك عمل أوردرات يدوية حقيقية، و**الفرق بين المسار القديم والجديد
بيتقرا من فجوة التوقيت لوحدها** — مش من كلام:

| الأوردر | الإدخال (UTC) | سطر التأكيد | الفجوة | المسار |
|---|---|---|---|---|
| **W-11** | 12:10:29.518418 | 12:10:29.518 | **≈ 0** | الـRPC |
| **W-10** | 22:51:02.226474 | 22:51:02.226 | **≈ 0** | الـRPC |
| W-9 | 11:49:54.394 | 11:50:00.308 | 5.9 ث | ضغطة موظف |
| W-8 | 11:34:10.924 | 11:34:20.395 | 9.5 ث | ضغطة موظف |
| W-7 | 11:32:45.019 | 11:32:51.638 | 6.6 ث | ضغطة موظف |

الفجوة **صفر بالميكروثانية** هي بصمة الـ`UPDATE` جوّه نفس الترانزاكشن —
مستحيل تتعمل بإيد. والسالب الصغير (−0.0004) لأن `status_log` بيخزّن
**ملّي** و`created_at` **ميكرو**، فالتقريب هو الفرق كله.

**والتلات أسباب المكتوبة فوق اتأكدوا واحد واحد:**
1. **الحالة**: الاتنين نزلوا `confirmed` و`platform='whatsapp'`.
2. **سجل الحالة**: `pending → confirmed · 👤 shekoz` — فالتايم لاين
   في نافذة التفاصيل بيترسم زي أي أوردر.
3. **التلجرام**: اتقرا من `net._http_response` — رد `200` في **نفس
   الثانية** (15:10:29) ونصه «👤 shekoz أكّد الأوردر ✅ 🛍️ …»، يعني
   **نفس** الإشعار اللي كان بيوصل لما الموظف يدوس بإيده. صفر تغيير.

**وكمّلوا المسار**: الاتنين اتحولوا `bosta_assigned` بعدها (بإيد
`ebrahim`) — يعني الأوردر اليدوي بيتشحن عادي مش بس بيتسجّل.

⚠️ و`billed_at = true` على الاتنين: ماكينة المحاسبة لسه بتولّع زي ما
هي مقصود — بتخصم 0.75 من رصيد لا نهائي، بلا أثر.

### ⚠️ أرقام بتتأثر — بعلم المالك

- **«قيد الانتظار» (`s1`)**: الأوردر اليدوي مابيعدّش فيه خالص. ده **صح**
  (هو مش pending)، بس معناه إنه مابيظهرش كـ«حاجة لسه محتاجة شغل».
- **«نسبة التأكيد» (`s6`)**: الأوردر اليدوي بياخد نقطة موجبة في البسط
  والمقام لحظة الإنشاء. ⚠️ **ده كان بيحصل أصلاً** (الموظف بيأكد خلال
  5–13 ثانية) فمفيش فرق دايم — بس صف «واتساب» في تبويب المنصات بيبقى
  **100% بالبناء**، وده رقم بيقول صفر معلومة. مؤجّل: تعديله يحتاج تغيير
  في الـRPC والحساب المحلي **مع بعض** وإلا ينحرفوا.
- **متوقع التحصيل**: مش متأثر — `confirmed` مش في `BOSTA_EXPECTED_STATUSES`.
- **مؤقّت مهلة المكالمة** (`getCallDeadline`): مربوط بـ`pending` فالأوردر
  اليدوي مالوش عدّاد. **كان كده أصلاً** (النافذة كانت 5–13 ثانية).

---

## 🔴 الانتقال من بوسطة لـJ&T (16 سبتمبر — قرار المالك)

**المالك لغى التعاقد مع بوسطة ونقل لـJ&T** (أرخص بفرق كبير، وتسليم الفلوس
أسرع). الطلب: ربط `APIs` بتاعة J&T بالسيستم و**«كل حاجة مظبوطة بالشعرة —
في الحسابات والمنتجات وسعر الشحن»**.

⚠️ **الشغل ده لسه ماتعملش — اتأجل لسيشن مخصّصة بقرار المالك.** اللي تحت
**قياسات حية اتعملت 16 سبتمبر** + مخاطر مرصودة، **مش خطة متكاملة**. أي
سيشن جاية تبدأ من هنا وتكمّل التشخيص قبل ما تبني.

### اللي اتقاس فعلاً

| البند | القيمة الحية |
|---|---|
| `tenants.shipping_provider` | **`bosta`** لكل الـ7 تجار |
| `shipping_api_key` | موجود لـ`3ataba` بس |
| أوردرات عليها `real_shipping_fee` | **1,744** بمتوسط **132.16 ج** |
| أوردرات `Delivered` | 1,758 (1,748 منها ببوليصة) |
| `Returned to business` | 568 · `bosta_assigned` 267 · `BOSTA AUTO` 263 |

### 🔴 أخطر حاجة: حالات الأوردر نفسها **مفردات بوسطة**

الحالات المخزّنة مش أكواد عندنا — دي أسماء بوسطة بالحرف: `Delivered` ·
`Returned to business` · `Out for delivery` · `Received at warehouse` ·
`Route Assigned` · `In transit between Hubs` · `Picking up from consignee`.
وهي متسجّلة في `BOSTA_INVENTORY_STATUSES` و`BOSTA_OPERATION_STATUSES` في
`core/constants.js`، وبيقرا منهم: `orders/cards.js` · `orders/costs.js` ·
`orders/orders.js` · `issues/issues.js` · `main.js` · `tour/demo-data.js`.

**J&T ليها مفردات مختلفة تماماً.** يعني الخطر مش «الربط مش هيشتغل» — الخطر
إن **الربط يشتغل والحسابات تبوظ في صمت**: حالة J&T مش موجودة في القوايم دي
= الأوردر مايتحسبش في كارت البضاعة ولا في متوقع التحصيل ولا في نسبة التسليم،
**من غير أي خطأ في أي مكان** (نفس شكل درس 6). ده بالظبط اللي المالك قال عنه
«بالشعرة».

### 🔴 والتقاطع مع قاعدة الأمان 1 — لازم يتحسم قبل أي بناء

مسار الشحن الحالي: `order-ship` (EF) → ويبهوك `ordercreate` → **n8n** →
بوسطة → n8n بيكتب `tracking_no` والحالة. يعني **قلب التكامل في n8n**،
و**ممنوع عليّ تعديل وركفلوهات n8n الإنتاجية** (قاعدة أمان 1).

✅ **قرار المالك (16 سبتمبر): يفضل في n8n.** يعني شغلي = **تشخيص + خطوات
مكتوبة بالحرف**، والتنفيذ في الوركفلو بإيد المالك. التمن المقبول بعلمه:
الاختبار السلوكي بيفضل يدوي (درس 26)، والمسار بيخلق شحنات بفلوس حقيقية
فكل خطوة تتراجع قبل التنفيذ.

### 💰 `real_shipping_fee` — المعادلة والمدخل الناقص

**تسعير J&T زي ما وصفه المالك:** بيحاسبوا **بالوزن** (منتج كيلو ومنتج 3 كيلو)،
**زائد 1% من الـCOD بحد أدنى 5 ج على الأوردر**. يعني:

```
real_shipping_fee = تعريفة(المحافظة, الوزن) + max(total_cost × 1% , 5)
```

🔴 **والمدخل الناقص: الوزن مش موجود في السيستم خالص.** اتقاس — مفيش ولا
عمود وزن على `stock_products` ولا على `orders` ولا في أي حتة. فالمطلوب:
- عمود وزن على `stock_products` — و**على صف الخاصية (البنت) كمان** لو
  المقاسات بتفرّق في الوزن، لأن الأم والبنت ممكن يبقى ليهم وزن مختلف.
- وزن الأوردر = Σ (وزن المنتج × الكمية) عبر `parseProductItems`.
  ⚠️ **الفخ**: الأوردر الملزوق/متعدد المنتجات — أي صف اسمه مش متطابق مع
  المخزون وزنه **مجهول**، ولازم يتقال بصوت عالي مش يتحسب صفر (نفس قاعدة
  «صفر تخمين صامت» في `resolve_variant_movement`).

**ومصدر الرقم نفسه فيه احتمالين — والترتيب مهم:**
1. **من J&T** لو الرد (إنشاء الشحنة / التتبع / تقرير الفواتير) فيه المبلغ
   المحصّل فعلاً → ده **مصدر الحقيقة** وبنخزّنه زي ما هو. **يتحقق من
   التوثيق، مايتفترضش.**
2. **نحسبها إحنا** بالمعادلة فوق — محتاج **جدول تعريفة J&T** (محافظة ×
   شريحة وزن). اتقاس: **27 محافظة متميزة** في آخر 30 يوم (1,227 أوردر)
   و**كلها متعبّية**، فخريطة الـzone قابلة للتنفيذ.
   ⚠️ والمحسوب **تقدير** لحد ما فاتورة J&T تأكده — يتخزن متعلّم إنه تقدير.

### 🔎 اللي اتأكد من توثيق J&T (16 سبتمبر — قراءة المصادر العامة)

المالك قال «أكيد الـAPI بتبعت البيانات دي». **اتشيك — والمتاح علناً بيقول
العكس**، والفرق ده بيغيّر التصميم كله فمسجّل هنا:

- **`order/addOrder` بترجّع رقم البوليصة — مش التكلفة.** توثيق J&T الرسمي
  (إندونيسيا) بيقول حرفياً: «J&T will generate **the waybill number** as
  feedback». والـwrapper المصري (`jt-express-laravel`) بيوصف الرد بإنه
  object من غير أي حقل سعر.
- ✅ **فيه endpoint مستقل للتسعير — «Tariff Check»** ضمن endpoints المنصة
  (Order · Track · **Tariff Check** · Cancel Order). ده على الأغلب مصدر
  التعريفة.
- 🔴 **والتكلفة النهائية تسوية شهرية مش رقم API**: «each month both client
  and J&T Express finance will do **reconciliation** for shipment fee based
  on agreement».
- **الوزن باراميتر إجباري في الطلب** (`length, width, height, weight`) —
  ده بيأكد إن الوزن مدخل لازم يتسجّل عندنا، مش اختياري.
- **المصادقة**: `apiAccount` + `privateKey` + `digest` (Base64 من MD5) +
  `bizContent`، وطلبات الأوردر عليها digest تاني بـ`customerCode`.
- الـendpoints المذكورة: `order/addOrder` · `order/cancelOrder` ·
  `order/printOrder` (بوليصة PDF بالـBase64) · `logistics/trace` ·
  `location/getLocation`.

⚠️ **حد التحقق — يتقال صراحةً:** توثيق **مصر** على
`open.jtjms-eg.com/#/apiDoc/index` و**محتاج تسجيل دخول** — مقدرتش أقراه.
اللي فوق من توثيق إندونيسيا الرسمي + wrapper مصري عام، فهو **مؤشر قوي مش
دليل على مصر**. أول حاجة في السيشن الجاية: فتح التوثيق المصري والتأكد من
(أ) هل رد `addOrder` فيه مبلغ · (ب) شكل `Tariff Check` ومدخلاته · (ج) هل
فيه endpoint للفواتير/التسوية.

### ✅ `SHIPPING_COST_DEFAULT = 85` اتشال خالص (16 سبتمبر — طلب المالك)

كان في `finance/finance.js`، وأي أوردر مالوش `real_shipping_fee` بيتحسب
بيه. **رقم بوسطة محفور في الفرونت** — ومع J&T بقى كذب مركّب.

🔴 **وشيله لوحده خطر بالمقلوب**: الأوردر المجهول بيبقى صفر، فتكلفة الشحن
تنقص و**الأرباح تطلع أعلى من الحقيقة في صمت**. فالشكل اللي اتنفّذ:
`orderShippingCost` بترجّع المسجّل أو **صفر**، و`unknownShippingCount`
بتعد **المسلّم + المرتجع** بس (دول اللي الشحن بيتخصم عليهم) وبانر فوق
تفصيلة التكاليف بيقول العدد صراحةً وإن **الربح المعروض أعلى من الحقيقي
بمقدارها**. الرقم يظهر والسياق يظهر معاه (درس 36).

**الأثر المقيس على الداتا الحية وقت الشيل:**

| الفئة | العدد | من غير تكلفة | كان بيتحسب بالـ85 |
|---|---|---|---|
| مسلّم | 1,766 | **21** | 1,785 ج |
| مرتجع | 590 | **589** | 50,065 ج |
| ملغي / في السكة | 1,731 | 1,731 | **صفر — مش داخلين الحساب أصلاً** |

يعني **610 أوردر × 85 = 51,850 ج** كانت بتتحسب تكلفة شحن **مخترعة** واختفت
من الحساب، **97% منها مرتجعات**. ⚠️ والمرتجع **بيتكلّف فعلاً** (متوسط
المرتجع الوحيد المسجّل كان 78.66 ج) — فالرقم الجديد **ناقص بس صادق**،
والبانر هو اللي بيمنعه يكدب.

`test-finance-shipping.mjs` — 10 فحص + معايرتين (رجوع الافتراضي 85 ·
شيل البانر). معايرة الـ85 طلعت 467 بدل 297 = **297 + (2 × 85)** بالظبط.

### 🔴 الفخ الأصلي — للتأريخ

`orderShippingCost` في `finance/finance.js`:
```js
export var SHIPPING_COST_DEFAULT = 85;
var f = parseFloat(o && o.real_shipping_fee);
return (isFinite(f) && f > 0) ? f : SHIPPING_COST_DEFAULT;
```
يعني **أي أوردر مالوش `real_shipping_fee` بيتحسب بـ85 ج** — وده رقم بوسطة
القديم محفور في الفرونت. مع J&T الأرخص، أي أوردر فاته تسجيل التكلفة هيتحسب
بتكلفة أعلى من الحقيقة **والأرباح تطلع أقل من الواقع في صمت**.

**وقياس مهم:** `orders.shipping_cost` = **85.00 في كل الـ4,087 صف**،
و**الداشبورد مابيقراش العمود ده خالص** (نفس عيلة `manufacturer_note`) —
الـ85 اللي بتدخل الحساب جاية من **ثابت جافاسكربت** مش من الداتابيز.
بينما متوسط التكلفة الحقيقية من بوسطة **132.14 ج**. الفرق ده (~47 ج للأوردر)
حقيقي ومقيس، ولازم التعريفة الجديدة تتسجّل صح عشان يبان.

### ✅ الأوردرات القديمة — محسومة

**قرار المالك: مفيش حساب بأثر رجعي خالص.** J&T بتتحسب **من أول يوم تشغيلها**
(اليوم اللي التحديثات هتخلص فيه). أوردرات بوسطة بتفضل بأرقامها زي ما هي،
ومفيش backfill ولا إعادة حساب — نفس قاعدة «إصلاح للأمام بس».

### ⏳ أسئلة مفتوحة لازم تتجاوب على الحالة الحية قبل البناء

- توثيق J&T نفسه: **ماتفترضش دلالة أي endpoint** — اتلسعنا قبل كده لما
  افترضنا إن نداء POST بيعمل شحنة وطلع بحث.
- خريطة الحالات J&T → حالاتنا، و**إزاي نفرّق «اتسلّم» و«رجع» و«لسه في السكة»**.
- ✅ ~~سعر الشحن بييجي منين~~ — المعادلة اتحسمت فوق. الفاضل: **جدول تعريفة
  J&T** (محافظة × وزن) + **وزن كل منتج** + التأكد هل الـAPI بترجّع المبلغ
  المحصّل فعلاً.
- المرتجع والرفض جزئياً · الاستبدال · تحصيل الـCOD وتوقيت التسوية.
- طباعة البوليصة — فيه `bosta-print-awb` (v2) مربوطة ببوسطة.
- **الـ101 أوردر الواقفين** (قسم المخاطر) — دول بوسطة، ولازم يتقفلوا
  **قبل** أو **بمعزل عن** الانتقال، مش يتخلطوا بيه.

### 🔴 قواعد ملزمة للانتقال

1. **البيانات التاريخية بتاعة بوسطة ماتتلمسش** — «إصلاح للأمام بس». الـ1,744
   `real_shipping_fee` دول تكلفة بوسطة الحقيقية وقتها، وأي إعادة حساب بأسعار
   J&T بتزوّر أرباح شهور فاتت.
2. **قيم الحالات القديمة معرّفات مش نصوص عرض** — `BOSTA2`/`bosta_assigned`
   فضلوا زي ما هم عمداً (قرار محسوم)، والانتقال **بيضيف** مش بيستبدل.
3. **نصوص الواجهة اتحضّرت للانتقال من 6 أغسطس** — «بوسطة» اتشالت من كل نص
   معروض وبقت «شحن»/«مع شركة الشحن» **عشان J&T تدخل من غير إعادة تسمية**،
   وفيه فحص في `check.py` بيمنع رجوعها. ده الجزء الجاهز بالفعل.
4. **`if5` في وركفلو الشحن كان مرصود إنه هيبقى SWITCH لما J&T تدخل** — الوقت
   ده جه. ✅ **المالك عمله فعلاً** (`Switch1` بمخرجين `bosta`/`jt` — شوف تحت).

### 🔬 سيشن J&T الأولى (20 سبتمبر) — اللي اتقاس واللي اتبنى

**حالة الحساب (من المالك — كله بره الريبو):** كود العميل `J0086011282` ·
شهادات Developer وCorporate معتمدة · الـIT فعّلوا كل الـendpoints · البوليصة
المخصصة **اتعتمدت** (النموذج المعتمد: زيب `3ataba-JT-waybill-source` +
`3ataba-JT-waybill-sample.pdf` — 100×150 مم، باركود أفقي 70×10 + رأسي، كود
فرز 35×10 Bold 20pt، الملاحظات = المنتجات، **مفيش رقم أوردر سهل عليها**).
Sandbox: مرجع `SAHL-SBX-20260917-01` / بوليصة `UEG088902573105` (**تجريبي
بس**). اللي نجح: Query Order بـ`command:2` + `serialNumber[]` + `customerCode`
+ business digest · التتبع (تفاصيل فاضية للتجريبي) · الاشتراك في التحديثات
(**ولا تحديث حقيقي وصل**). ⚠️ **صلاحية Create Order على الإنتاج ماتحققتش**
وممنوع نتحقق بإنشاء شحنة. ⚠️ كلمة سر التكامل (VIP الحالية ولا الأصلية؟) مش
محسومة — **ماتتغيرش**. Postman والبوابة المصرية **مش متاحين من هنا**.

**مسار الشحن الحي — مقاس من n8n مش من الذاكرة:**
- `CentralORDERS` **مافيهوش أي شحن** (الكومنت في `order-ship` قديم). المسار
  كله في **`Whatsapp_WEBHOOK`** (`9XzDXtvG64WkVoO4`) بمدخلين:
  ويبهوك `POST /webhook/ordercreate` (= `platform_settings.ship_webhook_url`،
  اللي `order-ship` بينده) → `get_order_details` → `get_tenant_details`؛
  و**تأكيد الواتساب بالزرار**: `Update Order → confirmed` → `ConfirmReply` →
  نفس المسار. يعني **كل تأكيد بالزرار بيعمل شحنة أوتوماتيك**، وزرار اللوحة
  شبه مش مستخدم (آخر `shipping_requested_at` 3 سبتمبر).
- `SHIPT CTX` → `If5` (`tenant.shipping_api_key` مش فاضي) → **`Switch1`**
  بمخرجين `bosta`/`jt` على `tenant.shipping_provider`.
- ✅ **`Switch1` في النسخة المنشورة مالوش أي مخرج موصّل — مقصود (قرار المالك
  20 سبتمبر: بوسطة اتلغت).** اتعدّل 11:33 UTC وآخر `BOSTA AUTO` 11:04. يعني
  دلوقتي أي تأكيد واتساب أو ضغطة «شحن أوتوماتيك» بتقف عند `Switch1` والأوردر
  يبقى `confirmed` بلا بوليصة (اللوحة تعرض «محاولة ماكملتش») — والشحن اليدوي
  شغّال عادي. **وقرار المالك: تأكيد الواتساب يرجع يشحن أوتوماتيك مع J&T** —
  يعني مخرج `jt` هو اللي هيتوصّل (بـ`jt-ship`) وفرع بوسطة يفضل مفصول.
- فرع بوسطة (المفصول دلوقتي): `Ta7leel el Address` (Gemini → VALID/INVALID +
  city/zone/district) → `Code in JavaScript` → `If4` → `CityNameData`
  (Google Sheet `Zoning-dataset-EG` تاب `Bosta Egypt Zoning`: `Zone_Id` /
  `District_Id`) → `AI Agent`/`AI Agent4` مطابقة → `BOSTA API1`
  (`POST app.bosta.co/api/v2/deliveries`: `cod=total_cost` · `size=MEDIUM` ·
  `notes=manufacturer_note` · `description=product_name+manufacturer_note`) →
  `Wait3` → `RANKING1` → `Update a row2` (`status='BOSTA AUTO'` + `tracking_no`
  + `customer_ranking`). الفشل → تلجرام `Send a text message4/5/8` بـchat
  متسمّر `-5130323197`.
- **تحديث الحالات (بوسطة):** `BOSTA_WEBHOOK` (`28dQ0sYGclq4Kyeg`) بيكتب
  `status = body.description` بالحرف بـ`by:'Bosta API'` من غير أي حارس تكرار
  أو رجوع — **وهو شغّال لحد النهاردة** (23,317 سطر آخرهم 16 سبتمبر، وهو
  كاتب الـ490 `Delivered` من أغسطس). ⚠️ **غلطت الأول وقلت «واقف من 29 يونيو»**:
  النود بتكتب `status_log` بـ`JSON.stringify(...)` في عمود jsonb، فالسجل بيتخزن
  **كـstring مش array** (double-encoded) من يوم ما اتعدّلت، واستعلامي كان بيعامل
  الـstring كسجل فاضي. `parseStatusLog` في الواجهة بتفكّه فمحدش حاسس — بس
  أي استعلام SQL على `status_log` لازم يفكّ الـstring الأول (`(status_log #>> '{}')::jsonb`).
  والحالات اللي بيكتبها آخر 30 يوم: `Received at warehouse` · `Out for delivery` ·
  `Delivered` · `In transit between Hubs` · `Route Assigned` · `Returned to business`
  · `Exception` · `Out for exchange` · `Picking up from consignee`.
- **`Mora2eb Bosta`** (`H2ev6JVUBH1LoA38`): poller كل دقيقة بيدوّر لأوردرات
  `bosta_assigned` من غير `tracking_no` على شحنتها عند بوسطة **بالتليفون**
  ويكتب `tracking_no` + `customer_ranking` — و`Monazem-mora2eb` بيفعّله 10:00
  ويوقّفه 17:00 يومياً. 🔴 مفتاح بوسطة **نص صريح** جوّه النود. مع J&T ده بيبقى
  بلا معنى (رقم البوليصة بييجي من رد `addOrder`).
- ⚠️ لسه **مش مقروء عبر MCP** رغم إن المالك فعّل «الكل»: `Scanner` ·
  `My workflow 6` (اتعمل 19 سبتمبر) · `Central_Shipping_Webhook`. و`real_shipping_fee_at`
  آخره 12 سبتمبر مع إن `Bosta - Capture Real Shipping Fee` معطّل من 24 يوليو —
  كاتبه لسه مجهول (واحد من التلاتة دول غالباً).

**الداتابيز — الموجود فعلاً:** على `orders`: `tracking_no` (index عادي،
**مش unique** — 2,833 صف / 2,801 متميز) · `bosta_delivery_id` (صفر مستخدم) ·
`awb_printed_at`/`awb_print_count` (7 بس) · `real_shipping_fee`/`_at`/`bosta_size` ·
`shipping_cost` (default 85 ومحدش بيقراه) · `shipping_requested_at` · `city` ·
`alt_phone`. **مفيش**: شركة شحن على الأوردر · مرجع الطلب عند الشركة · وزن ·
كود فرز · حالة الشركة الخام. على `tenants`: `shipping_provider` (default
`bosta`) + `shipping_api_key` نص واحد — **مفيش عنوان ولا تليفون مرسل** (الـFROM
في البوليصة). كل أرقام التتبع الحالية **أرقام بس 8–10 خانات**، فـ`UEG…` بيفرّق
J&T عن بوسطة في الصفوف القديمة من غير عمود. و`city` من اللاندنج **بإملاء
مختلط** (`القاهره` 1,523 · `Cairo` 52 · `القاهرة` 44 · `الجيزه`/`Giza`/`الجيزة`…)
— خريطة تطبيع لازمة قبل أي كود محافظة لـJ&T.

**الدفعة الأولى (اتبنت واتعايرت — من غير نشر ولا push):**
- `supabase/functions/_shared/jt.ts` + `md5.ts` — عميل J&T **نقي من غير أي
  import** فبيشتغل في Deno (EF) وNode (الهارنس) بنفس الملف بالحرف: التوقيع
  التلاتي (`processPassword` = UPPER HEX MD5 بالـsalt `jadada236t2` ·
  `businessDigest` جوّه bizContent مع `customerCode` · `headerDigest` على
  النص بالحرف) · `buildRequest` بيكوّن الـJSON **مرة واحدة** ويوقّع عليه
  ويبعته هو (x-www-form-urlencoded) · `jtCall` بحارس 🔴
  `jt_create_blocked_in_production` (boolean صريح من الكود — سترينج `"true"`
  من حمولة مرفوض) · `redactRequest` فالأسرار عمرها ما تتطبع ·
  `verifyCallbackDigest` (اتضافت 20 سبتمبر مساءً) للـcallbacks التلاتة:
  `Base64(MD5(bizContent + privateKey))` بمفتاحنا، مقارنة بطول ثابت — أي
  نداء مش موقّع بيه يترفض. **دالة بس — مفيش EF بتستقبل لسه.**
- `tools/test-jt-sign.mjs` — 57 فحص منهم 6 معايرات: md5 ضد `node:crypto` (عربي +
  حدود padding + 200 عشوائي) · سلسلة التوقيع ضد **تطبيق بايثون مستقل** من
  نفس المواصفة · الغلاف · الحارس · التعتيم · تحقق الـcallback (مفتاح غلط ·
  حرف اتغيّر · هيدر فاضي → مرفوض). ⚠️ **بيثبت إن الكود بيطبّق
  المواصفة الموصوفة — مش إن المواصفة هي اللي J&T عايزاها.** ده بيتأكد بس
  بـ`--compare-digest` ضد الطلب الناجح في Postman.
- `tools/jt/jt-cli.mjs` — `query` · `trace` · `waybill-info` · `pca` · `freight` ·
  `raw` مع `--dry-run` · `--env-file` (بره الريبو — `.env.*` متجاهَل) ·
  `--timestamp` · `--compare-digest`. `addOrder`/`addLooseOrder`/`cancelOrder`
  مرفوضين في الإنتاج من العميل نفسه، وفي الـSandbox محتاجين `--allow-create`.
- `tools/jt/sign-check.mjs` + `sign-vectors.json` — إثبات تطابق التوقيع على
  متجهات **وهمية مشتركة** (ASCII · عربي · كود عميل صغير · مسافات) — PASS/FAIL
  ضد بايثون مستقل وضد `expected_*`، من غير أي شبكة. اتعاير بقيمة expected غلط.
  ⚠️ للمقارنة مع Postman: `raw <path> '<bizContent بالحرف من Postman>'
  --no-biz-digest --compare-digest <digest الهيدر>` — الترتيب والمسافات
  بيغيّروا التوقيع، فلازم **نفس السترينج**.
- `tools/check-functions.py` بقى بيفحص `_shared/*.ts` (ترميز · أقواس · أسرار)
  + 5 عقود لـ`_shared/jt` — اتعاير بحقن سر · شيل الحارس · stringify مرتين
  + ضابط.

**الدفعات الجاية (بالترتيب — كل واحدة تتجرّب قبل اللي بعدها):**
2. migration `jt_shipments` (تتجرّب بترانزاكشن راجعة الأول): على `orders`:
   `shipping_carrier` (`bosta`/`jt`؛ NULL للقديم = بوسطة) · `carrier_ref`
   (مرجعنا عندهم = `orders.id`) · `jt_sorting_code` · `carrier_status_raw` +
   `carrier_status_at` · `shipping_weight_kg` · `shipping_fee_estimated`
   (**تقدير** منفصل عن `real_shipping_fee` المؤكد). على `tenants`:
   `sender_name/phone/address/city`. أسرار J&T في **EF secrets** مش في الجدول.
   `UNIQUE(tenant_id, tracking_no) WHERE tracking_no IS NOT NULL` **بعد**
   قياس الـ32 تكرار الموجودين.
3. EF `jt-ship`: الحمولة `order_id` بس (ثابت `order-ship`) · idempotency:
   `carrier_ref = orders.id` وقبل أي `addOrder` لو فيه محاولة حديثة/timeout
   يستعلم `getOrders command:1` بالمرجع — موجود = يكتب البوليصة بدل ما يكرر ·
   الحالة تتكتب **بعد** رد J&T بس. + EF `jt-status` (ويبهوك التحديثات):
   idempotent · ترتيب زمني (الأقدم يترفض) · حالة مجهولة تتخزن خام وتتعلّم
   للمراجعة **مش تتفرم**.
4. البوليصة: القالب المعتمد في `app/` (رندر في المتصفح + `window.print`
   100×150، الخطوط مضمّنة، نفس سلّم التصغير) ودفعة = صفحة لكل أوردر. الباركود
   يتفك في الهارنس بـ`zxing-cpp`. `bosta-print-awb` تفضل للقديم.
5. الحالات: قايمة J&T **من البوابة** → خريطة لحالاتنا + القيم الجديدة في **كل**
   قوايم `constants.js` (تتسمّى محايدة). حالة مش في الخريطة = مش بتختفي.
6. التكاليف: تقدير من تعريفة J&T (محافظة × وزن) + 1% COD بحد أدنى 5 ج
   **بعد تأكيد العقد** — وتمييز صريح تقدير/مؤكد. ⚠️ `getWaybillInfo`/
   Delivery Time Inquiry رجّع `data` فاضية — **مش دليل تكلفة**.
7. n8n (خطوات مكتوبة للمالك): `Switch1.jt` → HTTP لـ`jt-ship`، وفرع بوسطة
   يفضل للتتبع القديم بس.

**✅ توثيق J&T المصري اتقرا كامل (20 سبتمبر مساءً) — من غير لوجين:**
البوابة SPA وصفحات `#/apiDoc/*` **مضمّنة في `js/chunk-*.js`** (الجداول والأمثلة
وأكواد الأخطاء) والترجمة في `app.js`. اتستخرجت بـparser ومحفوظة في
**`tools/jt/docs/`** (19 ملف) + المثال الرسمي Java (صفحة basic) + مثال PHP
(من `download.jtjms-eg.com`). **الخلاصة العربية للعقود والمجهولات في
`tools/jt/docs/CONTRACTS.md`** — ده المرجع، مش الذاكرة. أهم اللي اتأكد:
- الجذور: `demoopenapi` / `openapi` `.jtjms-eg.com/webopenplatformapi/api` —
  بقت في `JT_BASE_URLS` والـCLI بياخدها افتراضياً.
- التوقيع زي ما اتوصف **بزيادة `strtoupper(customerCode + hex)`** من مثال PHP —
  اتطبّق (`businessDigest`) ومتعاير. الـform + الهيدرات التلاتة مطابقين للمثال Java.
- `addOrder`: `txlogisticId` = رقم أوردرنا (**J&T بترفض التكرار** بـ`145002001`/
  `145003101` — حارس تكرار طبيعي) · `remark` **200 حرف** · `weight` إجباري ·
  `totalQuantity` لازم 1 · `itemsValue` = COD · `sender`/`receiver` لازم
  `prov`/`city`/`area`/`street` **بأسماء J&T** (`145003060–62`). الرد فيه
  **`billCode` + `sortingCode` (`20,J01-01,000`) + `sumFreight`**.
- الحالات: `getOrders.orderStatus` 100–104 (قبل الاستلام بس) · التتبع
  `details[].scanType`/`scanTypeCode` · الاشتراك `traceNode` 1–15 (10 = تسليم ·
  11 = مشكلة · 13/14 = مرتجع). 🔴 **جدول `scanTypeCode` ↔ الحالة مش موثّق** —
  يتخزن خام لحد أول حمولة حقيقية.
- 3 callbacks بتنده URL بتاعنا بنفس الهيدرات: `logistics/statusFeedback`
  (التتبع — بديل `BOSTA_WEBHOOK`) · `orderserve/statusFeedback` (اتوزّع/اتلمّ/
  اتلغى + الوزن) · **`other/settlementReturn`** (الفاتورة بعد المراجعة =
  **مصدر `real_shipping_fee` المؤكد**). والـ`digest` بتاعهم قابل للتحقق بمفتاحنا.
- التكلفة: `spmComCost/getComCost` تقدير قبل الإنشاء · `getWaybillInfo`
  (`packageChargeWeight` · `totalFreight` · `freight` · `isSign`) بعد التسليم ·
  `settlementReturn` مؤكد. ⚠️ الـ1% COD وحد الـ5 ج **مش في التوثيق** — من العقد.
- ⚠️ التوثيق نفسه فيه غلطات (قيمة المثال مكان اسم الحقل: `10000000001299`
  = `txlogisticId`، `zhangsan` = `name`) — مذكورة في `docs/README.md`.

**⏳ محتاج من المالك قبل الدفعة 2:**
- ✅ ~~حسم `Switch1`~~ — مقصود، وتأكيد الواتساب يشحن أوتوماتيك مع J&T.
- تفعيل «Available in MCP» على `Scanner` · `My workflow 6` · `Central_Shipping_Webhook`
  (لسه بيرفضوا القراءة).
- من الطلب الناجح المحفوظ في Postman (`SAHL-SBX-20260917-01`): القيم اللي نجحت
  لـ`expressType` · `deliveryType` · `goodsType` · `operateType` · `payType` ·
  `serviceType` — **مش موثّقة كقايمة** في البوابة.
- تشغيل `node tools/jt/sign-check.mjs` (PASS/FAIL من غير شبكة — ضد بايثون
  مستقل وضد `expected_*` في `tools/jt/sign-vectors.json`)، وبعدها تشغيل
  Postman pre-request script على **نفس** المتجهات الوهمية ومقارنة الأرقام
  التلاتة. لو مطابقة → `jt-cli query UEG088902573105` على الـSandbox.
- إجرائياً مع J&T: تسجيل URL الـcallbacks التلاتة عندهم (مش API).

### 🚀 سيشن J&T التانية (20 سبتمبر ليلاً) — go-live: اللي اتنشر واللي فاضل

المالك غيّر النطاق: «نكمل ربط J&T ونشغّله على الإنتاج النهارده» ورفع القيود
(نشر · push · داتابيز). **المرجع التشغيلي الكامل: `tools/jt/GO-LIVE.md`.**

**الداتابيز (مطبّق):** على `orders`: `shipping_carrier` (`bosta`/`jt`؛ NULL =
قديم) · `carrier_ref` (= `orders.id` عند J&T) · `jt_sorting_code` ·
`carrier_status_raw/code/at` · `shipping_weight_kg` · `shipping_fee_estimated`
(**تقدير** — مش `real_shipping_fee`) · `ship_prov/city/area` (بأسماء J&T) ·
`jt_ship_error` · `jt_ship_attempted_at`. على `tenants`: `sender_*` (6 أعمدة،
ممنوحة للقراءة وفي `v_my_tenant` — DEFINER زي ما هي). جداول: `jt_events`
(الخام من الـcallbacks، idempotent بالـhash، service فقط) · `jt_status_map`
(**فاضي عمداً** لحد أول حمولة حقيقية) · `jt_pca` (كاش نطاق الخدمة، قراءة
للموظفين) · `jt_pca_alias` (مدينة اللاندنج → عنوان J&T، بيتعبّى من كل شحنة
ناجحة). دوال `app.jt_record_shipment` / `jt_apply_trace` / `jt_apply_settlement`
(DEFINER · service_role بس) + أغلفة `public.*_v1` لـPostgREST.
- 🔴 **الشحنة اللي J&T بتعملها بتتسجّل `status='BOSTA AUTO'`** — القيمة معرّف
  مش نص (الليبل «شحن أوتوماتيك» محايد)، وكل كروت الجدول والماليات بتشتغل
  زي ما هي. التفرقة بالعمود `shipping_carrier`. حالة جديدة كانت هتحتاج CHECK
  + كل قوايم `constants.js` — مؤجّل لحد ما الخريطة تتأكد من حمولات حقيقية.
- `UNIQUE(tenant_id, tracking_no) WHERE shipping_carrier='jt'` — القديم فيه
  31 تكرار من بوسطة فالقيد على J&T بس.

**الـEdge Functions (منشورة، `verify_jwt=false` والتصريح جوّاها):**
- `jt-ship` (v2) `{order_id, receiver?{prov,city,area}, weight_kg?, dry_run?}`: التصريح
  موظف (tenant من JWT) أو service_role (n8n) أو `x-diag-token`
  (`platform_settings.jt_diag_token` — للتشخيص من pg_net من غير أي سر J&T).
  الحراسات بالترتيب في هيدر الملف. **الحقول الخمسة** من
  `platform_settings.jt_addorder_fields` (JSON) — ناقصة = رفض
  `fields_not_configured`؛ `serviceType` اختياري ومابيتبعتش لو فاضي (مكانش في
  طلب Postman الناجح). **عنوان المرسل والمستلم لازم يبقوا صفوف في `jt_pca`**
  لما تبقى متزامنة (`sender_not_in_pca` / `address_not_in_pca`) — عشان
  `145003060–62` ماتطلعش من J&T. الإنتاج مقفول بـ`jt_production_enabled` (423).
  التكرار: `txlogisticId = orders.id` + استعلام `getOrders command:1` قبل أي
  إعادة إرسال وعند timeout وعند رد duplicate.
- `jt-status/{trace|order|settlement}`: تحقق `digest` بمفتاحنا (إنتاج + Sandbox
  لو موجود) → خام في `jt_events` → تطبيق. اتأكد حيّ: توقيع غلط = 401 والخام
  اتسجّل `digest_ok=false`. وقت `scanTime` بيتفسّر توقيت القاهرة (**افتراض** —
  الخام محفوظ).
- `jt-lookup` `{action: config|pca_sync|query|trace|waybill_info|freight|subscribe|raw}`.
  `raw` للتشخيص بس والإنشاء منه في Sandbox بس.
- الملفات المشتركة `_shared/jt-runtime.ts` (أسرار من `Deno.env`: `JT_*` و
  `JT_SBX_*` · التصريح · `egMobile` · `normPlace`).

**الفرونت (v56 — في `app/` ولسه ماترفعش):** `ship.js` — تاجر
`shipping_provider='jt'` بيشوف «🚚 شحن J&T» (من غير `has_shipping_api`)، ونافذة
`#jt-modal` بقوايم المحافظة/المدينة/المنطقة من `jt_pca` (بالصفحات — سقف
PostgREST 1000) + الوزن، والاقتراح من مدينة اللاندنج بالتطبيع · `jt-awb.js` —
القالب المعتمد بالحرف (CSS منقول من `waybill-template.html`) في نافذة
`about:blank` (بترث الـCSP: مفيش inline script، الخطوط من `app/fonts/` واللوجو من
`app/img/` — اتضافوا) + سلّم التصغير + عدّاد الطباعة · `core/code128.js` —
encoder نقي **مطابق بالحرف لـpython-barcode** (اللي النموذج المعتمد اتعمل بيه)
ومتحقق بـzxing-cpp · `awb.js` بيوجّه أوردرات `jt` للقالب وبوسطة للـEF القديمة ·
`detail.js` بيعرض كود الفرز وعنوان J&T وآخر حالة خام · `ORDER_LIST_COLS` اتوسّعت.

**⏳ محتاج من المالك (كله في `GO-LIVE.md`):** ✅ ~~أسرار J&T~~ (في الـVault) ·
✅ ~~القيم من Postman~~ · ✅ ~~عنوان المرسل~~ (كامل من البوابة) · ✅ ~~اختيار الأوردر~~ (`17170`) ·
✅ ~~أسماء J&T لعنوان مستلم `17170`~~ (من قالب البوابة + المنطقة نص حر — 21 سبتمبر) ·
✅ ~~رفع v56~~ · ✅ ~~رفع v57~~ · ✅ ~~تفعيل `order/addOrder`~~ (الـIT فعّله 21 سبتمبر) ·
⏳ **اختيار الأوردر + ضغطة الشحن** · تسجيل الـcallbacks عند J&T · خطوات n8n بعد أول أوردر ناجح.

**🔴 المنطقة نص حر — قرار المالك 21 سبتمبر (متتناقشش تاني):** `jt_pca` قايمة
محافظة/مدينة بس (صفوف `area=''`)، والمنطقة الموظف بيكتبها.
🔴 **وده فضل صح بعد ما قايمة J&T الكاملة وصلت** (6,464 منطقة بأكواد، 21 سبتمبر):
4 من أول 5 شحنات إنتاج ناجحة اتعملت بمناطق **مش في القايمة** وJ&T قبلت الخمسة،
ومحافظة «شمال سيناء» جت من J&T **بلا أي منطقة خالص**. فالقايمة بقت **اقتراحات في
`datalist`** من `app/data/jt-areas.json` — أي اقتراح يحوّلها لقيد غلطان، ومعاه
دليل مضاد مقيس مش رأي.

## قرارات محسومة (متتناقشش تاني)

- **Central pattern** — مفيش نسخ workflows لكل تاجر
- **التوجيه بالـ payload** — أزرار الواتساب بتحمل `<فعل>:<معرّف التاجر>:<معرّف الأوردر>`، فالرسالة بتقول صاحبها بنفسها. ده اللي خلّى الرقم المشترك وTech Provider ممكنين.
- **الإلغاء إشارة مش أمر تنفيذ** — لو الأوردر خرج للشحن، مايتلغيش. علامة تتسجّل والتاجر يقرر. (مسح البوليصة = الشحنة تتوه والبضاعة تضيع.)
- **إقفال التنبيهات يدوي بحت** — عشان نضمن إن التاجر شاف
- **سهل ماتردش على الرسايل النصية** — الرقم رقم التاجر والصندوق صندوقه
- **الأوردر اللي بيتولد مؤكد = مفيش خصم** — التاجر أكّده بره المنصة، إحنا مشتغلناش فيه
- **الخصم على التحويل مش على الإدخال**
- **`BOSTA2`/`bosta2` مش "بضاعة مع بوسطة"** — معناها السكانر بتاعنا ضرب الأوردر في مخزننا وبوسطة لسه ماستلمتش. كارت البضاعة بيستخدم `BOSTA_OPERATION_STATUSES` **عمداً** — أي مراجعة تقترح "تصليحه" لـ`BOSTA_INVENTORY_STATUSES` غلطانة.
- **`orders.manufacturer_cost` عرض بس — مش في الأرباح** (قرار المالك 30 أغسطس). التكلفة من snapshot الشحن أو سعر جملة المخزون. أي مراجعة تقترح ضمّها لـ`orderCostSnapshotValue` غلطانة.
- **`orders.packaging_cost` متطنّش عمداً** (1 أغسطس) — اتفحص على الداتابيز الحية: كل الأوردرات أصفار، ميزة نايمة. التغليف الفعلي = مصروف يدوي فئة "تغليف" بس. لو هيتفعّل يوم، يرجع في `renderFinanceOverview` والرسم البياني مع بعض.
- **مفيش فحص دوري لتوكنات ميتا** — نستنى الشكوى (قرار واعي بقبول المخاطرة)
- **مسار ويبهوك الشحن `ordercreate` مش سري — قرار المالك (7 أغسطس) بقبول المخاطرة.** حجته: محدش هيخمّنه، ومعاه لازم زوج `tenant_id`+`order_id` صحيح (UUIDs). الملاحظة المسجلة بعلمه: الزوج ده بيسافر بره سيستمنا في payload أزرار الواتساب (`<فعل>:<تاجر>:<أوردر>`). الفحوص القبلية في `order-ship` (بوليصة موجودة = رفض · cooldown 90ث) بتحد من أثر أي نداء غريب.
- ❌ **مفيش backfill لمحادثات الأوردرات القديمة** (قرار المالك 6 سبتمبر) — تريجر
  `wa_conv_on_order` بيشتغل للأمام بس، والـ383 أوردر اللي قبله مالهمش محادثة.
- ❌ **مفيش حارس حالة على زرار متابعة الواتساب** (قرار المالك 7 سبتمبر) —
  الموظف بيشوف السياق في مودال التأكيد وهو اللي يقرر.
- ❌ **الردود الصوتية للعملاء — مش هتتعمل نهائياً**
- 🔴 **الرد المحفوظ بيتبعت بضغطة واحدة من زرار باسمه المختصر** (قرار المالك
  24 سبتمبر — **بيلغي** قرار 16 سبتمبر «بيتحضّر مش بيتبعت»). أي اقتراح يرجّع
  «المعاينة قبل الإرسال» غلطان. الحراسات اللي كانت المعاينة بتديها بقت في
  الكود ومتفحوصة: قفل أثناء الإرسال · الزراير بتختفي والنافذة مقفولة · الهدف
  ثابت لحظة الضغط. والنص
  🔴 **`caption` على الصورة الأخيرة** (قرار المالك بعد التجربة الحية —
  درس 46). أي اقتراح يخلّيه **رسالة مستقلة** غلطان: ده كان الشكل الأول
  بالظبط، والعميل شاف «صورة ← كلام ← صورة» لأن ميتا **مابتضمنش** ترتيب
  التسليم. وبالكابشن عدد الرسايل N مش N+1 والكلام يستحيل يتوسط.
  ⚠️ والسطر ده نفسه فضل يقول «رسالة مستقلة في الآخر» لحد 20 سبتمبر —
  سطر في قايمة «متتناقشش تاني» بيناقض المنفّذ = أخطر شكل انحراف.
- **`order_uid` اليدوي في نطاق `W-n`** (16 سبتمبر) — أي اقتراح ياخد
  `max+1` من ترقيم اللاندنج غلطان: بيخلي أوردر الويبهوك الجاي يقع على
  الـUNIQUE ويضيع في صمت.
- 🔴 **بوسطة اتلغت — J&T هي شركة الشحن (16 سبتمبر، قرار المالك).** أي شغل
  جديد يتبني على J&T، وبيانات بوسطة التاريخية **ماتتلمسش**. التفاصيل
  والقياسات في قسم «الانتقال من بوسطة لـJ&T».
- **تكامل J&T يفضل في n8n** (16 سبتمبر، قرار المالك) — مش هيتنقل لـEdge
  Function. شغلي تشخيص وخطوات مكتوبة، والتنفيذ بإيد المالك (قاعدة أمان 1).
- ❌ **مفيش سعر شحن افتراضي في الماليات** (16 سبتمبر، قرار المالك) — الـ85
  اتشالت خالص. التكلفة المسجّلة أو صفر + بانر بيقول كام أوردر ناقص.
  أي اقتراح يرجّع رقم افتراضي (أياً كان) غلطان — بيخبّي النقص.
- ❌ **مفيش حساب J&T بأثر رجعي** (16 سبتمبر، قرار المالك) — التسعير الجديد
  بيبدأ من يوم تشغيله، وأوردرات بوسطة بتفضل بأرقامها. أي اقتراح بإعادة
  حساب تكاليف قديمة غلطان.
- **قوالب `wa_start_templates` قراءة بس من المتصفح** (16 سبتمبر) — أي
  اقتراح يحط محرر قوالب في اللوحة غلطان: النص لازم يطابق المسجّل عند
  ميتا بالحرف، ومحرر بيسيبه ينحرف = صندوق بيعرض كلام العميل ماستلمهوش.
- **الأوردر اليدوي: الحالة من السيرفر وحده** (16 سبتمبر · **اتحدّثت 19
  سبتمبر**) — الحالة عمرها ما بتيجي من المتصفح، والنهائية بقت `confirmed`
  بطلب المالك عبر `INSERT pending` ثم `UPDATE` في نفس الترانزاكشن.
  🔴 **أي اقتراح يخلّي الـ`INSERT` نفسه `'confirmed'` غلطان** — مش عشان
  الخصم (ده اتلغى) لكن عشان الأوردر بيطلع **بسجل حالة فاضي** فنافذة
  التفاصيل تعرض تايم لاين بلا تاريخ، والتلجرام مايخرجش.
- 🔴 **مفيش SaaS ومفيش محاسبة ومفيش تجار تانيين** (19 سبتمبر، قرار
  المالك) — السيستم خاص بـ3ataba وحدها. واجهة المحفظة والباقات اتقفلت،
  والماكينة سابت عمداً لأن `wallet_depleted()` متشابكة في الـRLS. أي
  اقتراح يشيل الماكينة من سياسات الـRLS غلطان (صفر فايدة · أخطر حتة).
  وأي اقتراح يرجّع صفحة/شارة المحفظة غلطان.
- **شريط فلاتر المحادثات بره العمود** (19 سبتمبر، قرار المالك بعد تجربة
  حية) — ابن مباشر لـ`.wa-wrap` بعرض النافذة. أي اقتراح يرجّعه جوّه
  `.wa-list` غلطان: 689px من الـchips في عمود 315px = 374px مخفي ورا
  زحلقة، والمالك رفضها بالنص. والموبايل معكوس (زحلقة) لأن العرض مايكفيش.
- **مفيش عنوان صفحة في المحادثات** (19 سبتمبر) — كانت الصفحة الوحيدة
  في اللوحة اللي ليها `h1`. أي اقتراح يرجّعه غلطان: بياخد 71px وبيخلّيها
  شاذة عن باقي الصفحات.
- ❌ **زرار «🎓 جولة تعريفية» العايم اتشال** (19 سبتمبر، قرار المالك) —
  كان بيفضل ظاهر للأدمن **على طول** حتى بعد ما يخلّص. الجولة نفسها
  شغّالة: كارت الترحيب بيظهر **مرة** لأدمن لسه ماخدهاش.

---

## المخاطر المفتوحة

### 🔴 عاجل
- 🔴 **فورم تسجيل عام مفتوح على `app.sahlgedan.com` — بيخلق متاجر فعلاً.**
  اتكشف 19 سبتمبر وقت تسجيل قرار «مفيش تجار تانيين». على شاشة اللوجين
  زرار **«+ إنشاء حساب جديد»**، وجواه كارت دعاية:
  «**~65 أوردر مجاناً** · رصيد ترحيبي 50ج لكل متجر جديد 🎁 · باقة الدفع
  مقابل الاستخدام». الفورم بتنادي `sb.auth.signUp()` حقيقي
  (`auth.js:371`) و`bootstrapTenantIfNeeded` بتعمل صف في `tenants`.
  🔴 **والدليل إنه اتستخدم فعلاً**: `Atfaalna` (30 مايو) · `breadfast`
  (31 يوليو) · `test-store` (5 أغسطس) — تلاتتهم **مستخدم واحد ورصيد
  50 ج بالظبط**، وده بصمة الرصيد الترحيبي.
  **ده بيناقض قرار المالك بشقّيه**: «مش هيكون فيه أبداً أي تجار تانيين»
  و«مش هنعلن عنه لأي شخص» — أي حد يفتح اللينك يقدر يعمل متجر، والكارت
  بيعلن عرض ترويجي لمنتج مابيقبلش عملاء.
  ✅ **اتعرض على المالك 19 سبتمبر وقرر يسيبه زي ما هو** — قبول مخاطرة
  واعي، زي «مسار ويبهوك الشحن مش سري». مايتقفلش من غير طلب صريح منه.
  ⚠️ ولما يتقرر يوم: **الإخفاء من الواجهة مش كافي أمنياً** —
  `sb.auth.signUp()` نداء مباشر على الـAPI بالـanon key، فأي حد بيعرف
  يستخدم devtools يقدر يسجّل حتى لو الزرار مخفي. القفل الحقيقي من
  إعدادات Supabase (Auth → Disable signups)، والإخفاء تجميل فوقه.
  ⚠️ والمخاطرة الحقيقية **مش** بس متجر غريب: كل متجر جديد بياخد
  **50 ج رصيد ترحيبي** من كارت الدعاية، وبيقدر يستهلك رسايل واتساب
  من الرقم المشترك لو اتفعّل يوم.
- **مفاتيح وتوكنز نص صريح جوّه n8n** — توكن البوت في 6 نودز، توكن الواتساب، مفتاح الشحن
- ✅ ~~`get_order_details` من غير فلتر `tenant_id`~~ — المالك ضاف الفلتر
  (`id` + `tenant_id` مع بعض) ونشر، واتأكد على النسخة الحية 7 أغسطس.

### 🟠 الحاجز أمام العميل التاني
> اتراجعوا على الحالة الحية 31 يوليو. **اتنين منهم مكانوش حواجز أصلاً.**

- ✅ ~~فروع تلجرام مش مقفولة~~ — **البوابة موجودة**: عمود `has_telegram_ops` + تلات نودز IF (`If9`/`If10`/`If11`) في `Whatsapp_WEBHOOK` بتشيك عليه. `3ataba` لوحدها `true`، الباقي `false`. **مش حاجز.** الفاضل: **مكان** الإرسال لسه متسمّر (`chat_id: -1003778185256`)، فالفلاج ده **ماينفعش يتفتح لتاجر تاني** قبل ما الجروب يتقرا من صف التاجر.
- ✅ ~~نودتين بيردوا برقم التاجر الحالي~~ — قرار المالك: `Send message` و`Send message1` **مايجيلهمش payload خالص وميّتين عملياً**. (موصولين في الجراف من `sendConifrmationconfirm1/2` — لو المسار ولّع يوم هيبعتوا من رقم عتبة.)
- 🟠 **جدول محادثات تلجرام بلا `tenant_id` — اتشخّص بالكامل 6 أغسطس على الحالة الحية.**
  **إيه هو:** صندوق المحادثات القديم بيشتغل على topics تلجرام — كل عميل خيط جوّه جروب واحد، والجدول `3ataba_chats` (`dataTableId=oUQ0017BcFv8stjv`، project `heDAIORSNDm8pset`) هو الخريطة. **عمودين بس: `phone_number` + `topic_id`. مفيش `tenant_id` لا كعمود ولا كفلتر.**
  **7 نودز:** 5 `get` في `Whatsapp_WEBHOOK` (أسماؤهم `Get row(s)` و`…1..4`) + في `CentralORDERS`: `TelegramTopicGet` (قراءة) و**`CreateTopicInsertRow` (كتابة — مش قراءة زي ما كان مكتوب هنا غلط)**. تلاتة مالهمش `cachedResultName` فبحث الاسم مابيلقهمش — **امشي بالـID**.
  **التسريب:** عميل عند تاجرين → الفلتر بالتليفون بس بيرجّع صف التاجر الأول → `If1` يشوف `topic_id` مليان فمايعملش خيط جديد → **رسالة أوردر التاجر التاني تنشر في خيط التاجر الأول**. تسريب بيانات بين التجار مش مجرد تصادم.
  **والأهم:** الوركفلو **بيقرا جروب التاجر فعلاً** (`Has telegram group id ?` بتشيك على `$('Get Tenant').item.json.telegram_group_id`) وبعدين بيبعت بـ`chat_id` **متسمّر** في 6 نودز. القيمة الصح مقروءة وبتترمى — فالإصلاح تعبيرة جاهزة مش شغل جديد.
  **الحالة الحية (6 أغسطس):** `3ataba` لوحدها `has_telegram_ops=true` + جروب متسجّل. `Trendose` (التاجر الجديد) و5 تجار تانيين كلهم `false` وبلا جروب → بياخدوا الفرع `false` فمفيش تلجرام خالص، وصندوقهم في اللوحة من `wa_conversations` **معزول بـtenant صح**. **فالتسريب نايم مش ولّع.**
  🔴 **القاعدة: متفتحش `has_telegram_ops` ولا تسجّل `telegram_group_id` لأي تاجر تاني قبل الإصلاح.**
  **الإصلاح (3 خطوات يدوية):** عمود `tenant_id` + backfill بـid عتبة → `tenant_id` في فلتر الـ7 نودز → الـ`chat_id` يتقرا من صف التاجر.
  **حاجات ملحوقة بيه:** توكن بوت التلجرام صريح في URL في **3 نودز في `CentralORDERS` لوحدها** · الـ5 نودز في `Whatsapp_WEBHOOK` عليهم `alwaysOutputData:false` (درس 5 — نود فاضية = وقوف صامت والتنفيذ "ناجح"؛ `TelegramTopicGet` عليه `true` صح) · جدول تاني `Moreish_chats` (`zPnlebcGqOstxsUP`) بنفس الشكل اتعمل 28 أبريل ومااتلمسش — أثر محاولة «جدول لكل تاجر» وبيكسر مبدأ الـcentral workflows، يتشال لو مش مستخدم.
- 🟠 **بيانات الرقم المشترك فاضية** — `sahl_phone_id` و`sahl_wa_token` فاضيين. **مستني Verify البيزنس على فيسبوك** (اعتماد خارجي، مش شغل كود).

### 🟡 قبل التوسع
- 🟡 **مسار الشحن — الفاضل بعد ما الزرار اتبنى (7 أغسطس):**
  1. تعبيرتا الـranking في `Update a row`/`Update a row2` من غير `?.` —
     لو بحث تقييم العميل عند بوسطة هنّج، كتابة الحالة بتفشل **بعد إنشاء
     الشحنة** (نفس عيلة «شحنة من غير حالة»). الإصلاح:
     `{{ $json.data?.deliveries?.[0]?.receiver?.ranking ?? null }}`.
  2. رسايل الفشل `Send a text message4/5/8` — لسه على `$('Update Order →
     confirmed')` وحقول عصر الشيتس وتلجرام متسمّر لعتبة. مؤجلة عمداً:
     مربوطة بمشكلة التلجرام المتسمّر، واللوحة بتعلّم الفشل بنفسها
     (شارة «⚠️ محاولة ماكملتش»).
  3. اختبار سلوكي حي للمسار كامل (زرار ← n8n ← بوليصة) — زي بوابة
     الواتساب، الشكل اتراجع والسلوك لسه (درس 26).
  🔴 **و`if5` هيبقى SWITCH لما J&T يدخل** — **والوقت ده جه (16 سبتمبر):**
  التعاقد مع بوسطة اتلغى. شوف قسم «الانتقال من بوسطة لـJ&T».
- 🔴 **101 أوردر واقفين من غير حالة نهائية** (اتكشفوا 4 أغسطس أثناء تفسير كارت «متوقع التحصيل»): **يونيو 48 بقيمة 53,471ج · يوليو 53 بقيمة 57,961ج** — كلهم على `bosta_assigned`/`BOSTA AUTO`/`bosta2` و**كلهم عندهم رقم تتبع**، يعني الشحنات اتعملت فعلاً وواقعياً اتحسمت من زمان بس الحالة النهائية عمرها ما اتكتبت عندنا. الأثر: **متوقع التحصيل** بينتفخ على مدة «آخر 30 يوم»/«الكل» (163,233ج من 140 أوردر — فلتر «الشهر الحالي» الافتراضي هو اللي حامي) · **تنبيه الدمج** بيقرا نفس الحالات من غير فلتر تاريخ فبيطلّع تنبيهات كاذبة · **نسب يونيو/يوليو ناقصة** وكالندر أيامهم بيقول «⏳ لسه ماتقفلتش» بصدق. الخطة المتفق عليها (مؤجلة بقرار المالك): جرد بالتتبع من بوسطة (نداء **بحث** — يتأكد من دلالة الـAPI الأول) → عرض النتيجة → تحديث جماعي بترانزاكشن تجريبية. **والسؤال الأهم لمراجعة n8n اليدوية: ليه التحديثات وقفت عند الأوردرات دي؟** عشان الفجوة ماتتكررش.
- 🟡 **خصم «استاند امريكانا» واقف من 3 يونيو — فجوة حية اتكشفت أثناء شغل الخصائص (15 أغسطس).** المنتج بيتباع يومياً (542 أوردر var أبيض/أسود في 60 يوم) وآخر حركة مخزون ليه 3 يونيو (29 حركة سكانر قبلها — كان شغال). فيه **وركفلوهين سكانر نشطين**: `Scanner` (مش متاح للقراءة عبر MCP) و`scannerpostbase`. اللي اتقرا فعلاً: كود `scannerpostbase` بيقسم `product_name` على **أي** `" - "` واسم المنتج نفسه فيه واحدة («… 4 دور - جزامة و شماعة …») فالبحث بالاسم مابيلاقيش حاجة **في صمت** (درس 5) — لو ده مسار الخصم الحالي فده السبب. الإصلاح اليدوي المقترح للمالك: نقل قسمة v29 (بعد قوس الكمية بس — نفس `splitProductSegments`) لنودز الـCode بتوع السكانر، ومراجعة `Scanner` من المحرر (مش مقروء آلياً). **ويتعمل قبل ما 3ataba يفعّل خصائص لاستاند** — من غيره الخصم مش هيوصل للعيلة أصلاً. وليه كان شغال قبل 3 يونيو ووقف؟ مش متشخص — يتشاف في تنفيذات n8n وقت المراجعة اليدوية.
- ⏳ **بوابة الواتساب اتركّبت بس مشتغلتش ولا مرة** — `WA Gate` + `If2` اتنشروا في `CentralORDERS` (31 يوليو، `activeVersionId=97a29fba`)، بس **مفيش ولا أوردر عدّى عليها لحد دلوقتي**. الاختبار الحاسم (مفتاح مقفول = مفيش رسالة) لسه مااتعملش. تفاصيل الحالة في قسم البوابة تحت.
- 🟡 **17 من 87 قالب صادر راجع `failed`** (اتقاس 16 سبتمبر على `wa_messages` — 7–16 سبتمبر: 39 `read` · 24 `delivered` · **17 `failed`** · 7 `sent`). كلهم تقريباً `wa-followup`. **السبب مش متخزّن**: ويبهوك الحالة بتاع ميتا بيبعت `errors[]` مع الفشل و`wa-inbox-ingest` بيرميه — فالتاجر بيشوف «فشل» من غير سبب. الخطوة الأولى **مش إصلاح**: تخزين `errors` في عمود/خام عشان نعرف هل ده رقم مش على واتساب ولا حد بلوك الرقم ولا حد سقف. رسايل مدفوعة بتفشل بـ20% حاجة تتشاف.
- النسخة الجديدة من وركفلو الأوردرات مبنية وغير مفعّلة
- طبقة التحقق من توقيع ميتا جاهزة ومستنية المفتاح
- **`wa-media` مافيهاش قيد على أنواع الملفات** (`topup-screenshots` مقيّدة). تاجر يقدر يرفع أي نوع في فولدره. **مقصود مؤقتاً**: n8n بينزّل ميديا واردة من واتساب بأي نوع، والتقييد ممكن يكسر الوارد في صمت. لو اتقفل، لازم جرد أنواع الوارد الأول.
- **كود صفحة issues ميّت** — مفيش `page-issues` ولا `issues-tbody` ولا زرار nav في الـHTML خالص، وكود `issues/issues.js` بيرندر في الفراغ. مستني قرار: تتشال ولا ترجع.
- **مؤجل بقرار من مراجعة Codex التانية (3 أغسطس):** توقيت غير القاهرة (حدود جدول الأوردرات `ordersPeriodRangeISO` وAnalytics والكالندر بتوقيت الجهاز بينما الكروت بالقاهرة — أثره بس لو اللوحة اتفتحت من برة مصر) · keyset pagination ومؤشر سقف الـ200k في `ensureAllLoaded` (بعد النمو) · CHECK constraint للسالب في الداتابيز (الحماية فرونت بس دلوقتي) · tap لتفاصيل اليوم في كالندر الموبايل · تحديث الشارت لحظة تبديل الدارك (بيصلّح نفسه مع أول فتح تاني) · وزن العينات في dayQuality · اختبار popup الـAWB يدوي على Chrome/Safari/موبايل (الـfallback موجود).
- **`admin/_headers` مالهاش `Strict-Transport-Security`** — لوحة التاجر عندها. (التزام شبه دائم — قرار المالك.) `Permissions-Policy` اتضافت 31 يوليو.

### ✅ اتقفلت

**جلسة 11 أغسطس — قسمة المنتجات الملزوقة (v29):**
- ~~أوردرات الويبهوك الملزوقة بـ" - " بتترندر صف واحد~~ — ديف من مراجعة
  خارجية اتطبّق **بتصليحين جوهريين**:
  1. `splitProductSegments` بتقسم **بعد قوس الكمية «(عدد N)» بس** — مش أي
     قوس زي ما الديف كتب. الـregex الفضفاضة كانت بتفكك «ترولي (3 دور) -
     أسود» لمنتجين والكمية بتهاجر للوهمي **وبتتكتب في الداتابيز مع أول
     حذف صف** (مسكتها مراجعة عدائية، والتضييق هو نية كومنت الديف نفسه
     حرفياً). لاحقة الـvariant بترجع تتلزق والكمية بتتنقل لآخر السطر.
  2. **سد upsell وهمي في `saveProducts`**: مقارنة السترينج الحرفية كانت
     هتشوف «تغيير» في كل حفظة لأوردر ملزوق (التمثيل بيتغير) والسيرفر
     هيعلّم upsell لو الإجمالي زاد. `goodsSignature` (أسماء+كميات مرتبة،
     نفس دلالة السيرفر): البضاعة زي ما هي = الاسم المخزن بيتبعت **نفسه**؛
     التمثيل المقسوم بيتكتب بس مع تغيير بضاعة فعلي.
  قياس حي وقت النشر: 307 ملزوقة، 115 بتتقسم فعلاً، **صفر** بالشكل الخطر،
  **صفر** line_prices بأسماء ملزوقة. `test-price.mjs` بقى 27 فحص
  (+4 معايرات بحقن الـregex الفضفاضة وشيل السد وشيل القسمة وشيل اللزق).
  **ملاحظتين مفتوحتين بعلم المالك:** صفوف الملزوق بتتملى بأسعار المخزون
  مش بالمدفوع الفعلي (سقط fallback الإجمالي÷الكمية لما الصف بقى صفوف) —
  upsell على ملزوق ممكن الـdelta بتاعه ينحرف · صف ملزوق منتجه مش في
  المخزون = خانة فاضية والإجمالي مايتبعتش (برسالة).

**جلسة 6 أغسطس — طلبات Trendose** (فرع `claude/trendose-ui-shipping-updates-cvm7kg`):
- ~~أقسام نافذة تفاصيل الأوردر ذايبة في الخلفية~~ — اتنشر واتأكد. السبب اتقاس
  مش اتخمّن: فرق اللون بين `.dpan` و`.dsec` كان **صفر** في النهاري و(1,0,−2)
  في الليلي. بقى لكل قسم إطار + شريط جانبي ملوّن + عنوان بخلفية.
  `!important` على وزن الخط **مقصود** — `02-base.css` فيها
  `div,span,…{font-weight:700 !important}` كانت بتاكل التنسيق في صمت.
- ~~تاريخ حركات المخزون مكسور (اليوم بعيد عن باقي التاريخ)~~ — اتنشر واتأكد.
  السبب الجذري: `fmtStoredDateTime` كانت **الدالة الوحيدة** اللي مابتشيلش
  علامات الـbidi (`\u200e` `\u200f` `\u061c`) اللي `ar-EG` بتحقنها —
  `fmtD`/`fmtDT` بيشيلوها وعليهم كومنت بيشرح الفخ. و`fmtDateOnly` كان فيها
  نفس الباج نايم. (وقريت الـthumbnail غلط أول مرة وقلت فيه قصّ سطر — الزوم 4×
  أثبت إن مفيش.)
- ~~«بوسطة» في نصوص اللوحة~~ — بقت «شحن» و«مع شركة الشحن» عشان J&T وغيرها
  يدخلوا من غير إعادة تسمية. **قيم الداتابيز (`bosta_assigned` · `BOSTA AUTO` ·
  `BOSTA2`) مااتلمستش** — دي معرّفات. وفيه فحص في `check.py` بيمنع رجوعها،
  و`test-carrier-naming.mjs` مسك موضع الفحص الساكن سابه (نص بيترندر من
  الجافاسكربت على صفحة الأوردرات مش الإعدادات).
- ~~مفيش إدارة موظفين~~ — Edge Function `tenant-staff` + كارت في الإعدادات.
- ~~مفيش عمولة upselling~~ — migration `upsell_commission` + `commission_settlements_and_has_upsell`.
- ~~قفلتين `</div>` زيادة فكّكوا التخطيط على الموبايل~~ — اتمسكت بالهارنس
  واتصلحت، وفحص توازن الوسوم اتضاف عشان ماتتكررش (درس 37).
- ~~مفيش تعديل سعر بيع للقطعة في الأوردر الواحد~~ — **8 أغسطس، طلب المالك**،
  واتصلحت جذرياً بعد تجربته الحية (v27): النسخة الأولى كانت بتحسب «فروقات»
  ضد سعر السيستم فالمكتوب كان بيضيع بعد القفل والفتح، وأي زيادة إجمالي
  كانت بتتعلّم upsell حتى تصحيح السعر (3 أحداث عمولة وهمية اتعملت
  واتنضفت). **العقد الحالي:** الرقم المكتوب هو اللي بيتحفظ (إجمالي مطلق
  Σ سعر×كمية) · الأسعار بتتخزن في عمود `orders.line_prices` فبترجع زي
  ما هي · **الفيصل بين upsell وتصحيح سعر على السيرفر** — مقارنة قايمة
  البضاعة (أسماء+كميات) في `save_order_products` (بقت 4 باراميترات،
  migration `line_prices_and_upsell_goods_check`)، الشارة والعمولة
  للبضاعة المضافة بس. سعر المخزون والتكلفة مايتلمسوش. مفيش تغيير =
  مانبعتش حاجة. `test-price.mjs` (14 فحص، 4 معايرات بملف متحقق بالبايت).
- ~~4 ملاحظات من تجربة المالك الحية (7 أغسطس) — v21 اتجرب واتأكد «زي الفل»~~:
  جدول الحركات مفكّك (تصادم كلاس: صفوف الجدول خدت `cm-row` بتاعة سطر
  المحرر اللي عليه `display:flex` — الانحراف اتقاس 624px وبقى صفر بعد
  `cm-ev-row`) · الشريط اتنقل جوّه `#orders-period-bar` (مكانه القديم
  بيتزاحم مع تنبيه طلبات الإلغاء) · زرار «عمولتي» كان بأيقونة عملاقة
  (SVG من غير `.nv-ico`) **وضغطته ميتة** — أزرار القايمة متوصّلة بالـID
  واحد واحد في `orders.js:567` وأي زرار جديد لازم يتضاف هناك ·
  لمسات الصفحة (نقط ألوان `--vc` + الرصيد الموجب أخضر + الحركات بتفتح
  الأوردر). **درس الاختبار:** الفحص القديم عدّى على الزرار الميت لأنه
  كان بينقل بزرار الشريط — أي عنصر ليه أكتر من مدخل، الاختبار لازم
  يدوس على **كل** مدخل بضغطة حقيقية.

> **كل اللي تحت من 3–4 أغسطس اتنشر على الهواء (v15 — 78 ملف، 4 أغسطس).** الفرع `claude/codex-review-v9-second-2pdsuz` بقى = الإنتاج.
- ~~فروق الكروت كانت بتقارن بالشهر اللي فات **كامل**~~ — **اتصلحت 4 أغسطس**. يوم 4 أغسطس الكارت كان بيقارن 4 أيام (94 أوردر) بـ31 يوم (786) ويطلّع «▼ 88%» — رقم بيقيس فرق طول المدة مش الأداء. `prevMonthCairoDates` بقت بترجع **لحد نفس يوم الشهر** (`min(اليوم, آخر يوم في الشهر السابق)` — 31 مارس بيقارن بـ28/29 فبراير)، والـtooltip بقى بيسمّي النافذة صراحةً («مقارنة بـ1–4 يوليو»). التاريخ بتوقيت **القاهرة** لأن الـRPC بيفلتر بـ`(created_at at time zone 'Africa/Cairo')::date`. **قرار المالك:** «حل أ» بس (نفس عدد الأيام) — مش محاذاة أيام الأسبوع، رغم إن تفاوت الأيام عند التاجر كبير (الخميس 391 مقابل الثلاثاء 232 على شهرين). وكروت **تم التسليم/المتحصل** اتسابت بفروقها زي ما هي بقرار صريح — **بس خد بالك إنها مايلة ضدك أول كل شهر** (أوردرات الشهر الجديد لسه في السكة وأوردرات الشهر اللي فات خلصت من زمان)، وده مايتصلحش بمساواة النوافذ، محتاج snapshots تاريخية.
- ~~`clearStatsDeltas` بتمسح تسميات ثابتة في صفحة الماليات~~ — كانت `querySelectorAll('.sdelta')` على الصفحة كلها، فأول تبديل للمدة لغير «الشهر الحالي» كان بيمسح `fin-revenue-delta` («تشمل كل الحالات») و`fin-margin` — ومفيش كود بيعيد كتابتهم فكانوا بيضيعوا لآخر الجلسة. بقت محدودة بكروت `DELTA_METRICS` بس. (باج قديم اتكشف بالصدفة أثناء كتابة اختبار الفروق.)
- ~~الماليات والإحصائيات بطيئة وساعات الأرقام ماتظهرش~~ — **اتصلحت 4 أغسطس** (`28550ae`). المالك بلّغ، والقياس على الداتا الحية (2,609 أوردر) كشف السبب: `ensureAllLoaded` كانت بتسحب `select('*')` = **2,973 kB منها 1,605 kB سجل تغيير الحالة (`status_log`) لوحده** — وهو **مش مقروء من `all` خالص** (بيتقرا من `sel` بعد فتح الأوردر ومن `ORDER_LIST_COLS` في الجدول). والدفعات (3×1000) كانت **متسلسلة**. الإصلاح: `ALL_COLS` = 13 عمود بالظبط (~90% أقل نقل) + الدفعة الأولى بتجيب `count` والباقي **بالتوازي** (جولتين مهما كبر الجدول) + `core/loaderr.js`: فشل السحب بيعرض «مقدرناش نحمّل البيانات» وزرار «حاول تاني» بدل الصفحة الفاضية الصامتة (`if(err){veilDone();return;}` كان بيسيب التاجر مستني حاجة عمرها ما هتيجي).
- ~~الكالندر عرض بس من غير تنقّل~~ — **4 أغسطس** (`61bb89b` + `315c492`): الضغط على أي يوم فيه أوردرات بيحوّل مدة الأوردرات لـ«مخصص» من اليوم ده لليوم ده وينقل للجدول. الأيام الفاضية/الجاية مش قابلة للضغط (وهوفر الرفعة اتقصر عليها عشان مايبقاش وعد كاذب). حدود اليوم **متطابقة عن قصد** بين `aggregateMonth` (تاريخ الجهاز) و`ordersPeriodRangeISO` custom (نص ليل الجهاز) — فاللي بيتعرض هو بالظبط اللي الكالندر عدّه. ومعاه شارة **⏳ N لسه ماتقفلتش** في المربع (= اللي دخل رحلة الشحن ناقص المسلّم والمرتجع، فبيشمل المؤكد اللي لسه ماخرجش) — بتختفي لما اليوم يخلص، ومخفية على الموبايل زي باقي التفاصيل.
- ~~26 باج مؤكد من مراجعة Codex التانية على v9~~ — اتنفذوا في 3 دفعات (3 أغسطس، فرع `claude/codex-review-v9-second-2pdsuz`) **واتنشروا على الهواء 4 أغسطس مع باقي شغل اليوم (v15 — 78 ملف)**:
  **(1) سلامة البيانات:** محرر المنتج بيبعت بس الحقول اللي اتغيرت عن لحظة الفتح (إرسال `current_qty` دايماً كان بيرجّع كمية خصمها السكانر) + منع السالب (الجدول live مفيش عليه ولا constraint) · حرس `sel` الملتقط في التفاصيل ومحرر الموبايل/العنوان و`doUpdate` (منتجات/تعديلات A كانت بتقع على B) · صف «اختار منتج» الفاضي بطّل يمسح المنتج في صمت (والتسمية القديمة «اكتب يدوياً» كانت وعد كاذب — قرار: مفيش حقل يدوي) · الجولة بتطنّش الـRealtime وبتصفّر `allLoaded` في الاسترجاع (أوردر حقيقي كان بيدخل الديمو ويضيع والماليات تفضل على snapshot قديم).
  **(2) القفل والسباقات:** قفل النفاد بيلحق المعروض (بيقفل التفاصيل والشات لحظة الانتقال، وبيوقف poll الإنبوكس اللي كان بيستبدل رسالة القفل بـ"مفيش محادثات" بعد 20 ثانية، ورد تفاصيل واصل بعد القفل مابيرندرش) + بانر على الماليات/الإحصائيات (أصفار الـRLS كانت بتتعرض كأرقام حقيقية) · `loadWalletState` بجيل نداء والتقاط تاجر ومرة واحدة وقت الدخول · `forceSuspendLogout` بقى reload كامل (الرسالة عبر sessionStorage — الكاشات كانت بتعيش لجلسة الحساب التاني) · إغلاق التفاصيل بيبطل `detailReqId` · جيل لكروت الإحصاء زي الجدول · clamp للصفحة لما العدد ينقص + حد أقصى 3 ثواني لتجميع الـRealtime · ↻ بقى بيحدّث Analytics/Inbox.
  **(3) التلميع:** أجيال للإنبوكس (رسائل نفس المحادثة + القايمة) · rebuild الشات بيحافظ على مكان القارئ + مؤشر «أحدث 500 رسالة» · revoke للـobjectURL عند تبديل الشات واختيار doc بعد صورة · dedupe حركات المخزون وall بالـid · كروت أفضل تسليم/أعلى مرتجع بطّلت تخترع فائز لما النسب null · مزامنة UI فلتر طلبات الإلغاء · رسالة بحث المخزون الفاضي · AWB بيطبع مرة واحدة · دارك مود segment الرصيد · الـchip على الموبايل بيشيل segment الباقة تحت 600px بدل القص الصامت · مفتاح الجولة بقى `<uid>` زي العقد (مع fallback للمفتاح القديم) + حفظ/استرجاع الفلاتر والمدة وتبويب الماليات + جيل للـtimeouts.
  **كالندر الأيام — نسبة التسليم:** اتنفذ وعد الواجهة الأول («—» طول ما في اليوم شحنات ماتحسمتش)، وبعد ما المالك شافه حيّ **رجع عنه بقرار صريح (4 أغسطس)**: النسبة تظهر عادي أول ما تبقى فيه شحنة محسومة. السبب: يوم فيه 19 من 25 محسومين كان بيظهر «—»، وشهور يونيو/يوليو كلها كانت بتطلع «—» بسبب الـ101 أوردر الواقفين. المقابل المقبول بعلم المالك: يوم فيه شحنة واحدة اتسلمت من 10 بيقول «100%» ويتلوّن ممتاز — وعدد اللي لسه في الشحن في الـtooltip. **نص الواجهة اتظبط مع السلوك** (وعد مكتوب مش منفّذ = درس 24).
  **سقط بالتحقق:** fallback المحاولات بلا `iso` (فحص live: 423 أوردر فيهم محاولات، صفر بلا iso — مسار ميت) · تصنيف «تسريب» النفاد (سياسات الـRLS بالـ`wallet_depleted()` شغالة فعلاً على `orders`+`wa_conversations`+`wa_messages` — الفجوة كانت UI مش أمن).
- ~~الاشتراك بيتحكم فيه ساعة جهاز التاجر~~ — الحكم بقى من `tenant_subscription_state.computed_status` (بيتحسب بـ`now()` بتاعة الداتابيز) والساعة المحلية fallback بس. **2 أغسطس.**
- ~~كتابة `call_attempts`/`status_log` بـread-modify-write~~ — بقت RPCs ذرية (`append_call_attempt` / `delete_call_attempt` بالهوية iso مش الفهرس / `set_order_status` بتحسب `from` من صف السيرفر). migration: `atomic_call_attempts_and_status_log`، SECURITY INVOKER فالـRLS بتتطبق، والتنفيذ لـauthenticated + service_role بس. **الفاضل**: n8n لسه بيكتب `status_log` بمساره — لو عايزين ذرية كاملة، نودزه تتحول للـRPC (خطوات يدوية للمالك).
- ~~تنبيه الدمج بيعرض أسماء وتليفونات مكشوفة أثناء قفل النفاد~~ — بيختفي مع `is_depleted`.
- ~~قفل النفاد واجهة بس والبيانات بتوصل المتصفح~~ — **بقى enforcement على السيرفر** (2 أغسطس، migration: `depletion_lock_server_enforcement`): `app.wallet_depleted()` (نفس معادلة `wallet_state.is_depleted` حرفياً — لو اتغيرت يتغيروا مع بعض) اتحطت كحارس في سياسات SELECT بتاعة `orders` + `wa_conversations` + `wa_messages` — **في المجموعتين** (`*_same_tenant` و`phase2_*`) لأن الـpermissive بتتجمع بـOR. التاجر المنفّد ياخد صفر صفوف مهما اتحايل، `tenants`/`wallet_state` فاضلين مقروءين عشان يشحن، السوبر أدمن مستثنى، وn8n (service_role) بيتخطى RLS فالأوردرات الواردة بتكمل. الواجهة: رسالة قفل صريحة في الجدول والإنبوكس + رجوع تلقائي (`loadAll`) أول ما الشحن يوصل. اتجرب بترانزاكشن راجعة بانتحال تاجر حقيقي مش سوبر أدمن (1/1 ← 0/0). **ملحوظة**: `lockMaybe` في الواجهة بقت طبقة تانية بس — الحاجز الحقيقي RLS.
- ~~ملف الواجهة الضخم~~ — اتفكّك 29 يوليو (587KB → 58KB + 29 موديول).
- ~~`orders/orders.js` 2,213 سطر~~ — اتقسم 30 يوليو لـ12 موديول، بقى 457 سطر. **اتنشر واتأكد على الهواء.**
- ~~`val` مش مستوردة — كروت الإيرادات في الجولة فاضية~~ — اتصلّحت واتأكدت على الهواء 30 يوليو.
- ~~صفحة الإعدادات مابتعرضش رابط الربط ورمز التحقق~~ — كرت "ربط واتساب بميتا" بيعرض Callback URL و Verify Token من `wa_webhook_secret` (اتضاف للفيو)، ومعاه شارة حالة التوثيق من `wa_inbox_status`. **اتنشر واتأكد على الهواء 31 يوليو.**
- ~~لوحة الأدمن من غير CSP~~ — اتضافت **واتنشرت** 30 يوليو.
- ~~تاج الـCDN عائم من غير SRI~~ — اتثبّت على 2.111.0 + `integrity` + `crossorigin` في اللوحتين، و`script-src` بقت بالمسار الكامل. **اتنشر 30 يوليو.**
- ~~سياسات `wa-media` مفتوحة للجميع~~ — **البند ده كان غلط في الملف ده.** اتحقّقنا 30 يوليو على طبقتين (SQL في ترانزاكشن رجعت + الـStorage API الحي بمفتاح anon): الـbucket `public=false`، الـRLS شغّالة، والعزل على `{tenant_id}/…` بيشتغل. anon مايقراش ومايرفعش، وتاجر مايشوفش ولا ملف من الـ93 بتوع تاجر تاني.

---

## أول حاجة تعملها في أي جلسة

1. اسأل عن الهدف
2. 🔴 **اتأكد إن فرعك محدّث قبل أي حاجة** — سيشن جديدة بتبدأ من الفرع
   الافتراضي وممكن تكون ورا شغل النهاردة بكوميتات (درس 49). القياس
   السريع: رقم «آخر نسخة اتسلّمت» في الملف ده لازم يطابق آخر تسليم
   فعلي. لو ورا: `git fetch origin <فرع-الشغل> && git merge origin/<فرع-الشغل>`.
3. لو محتاج سياق تاريخي — اقرا Notion
4. لو الشغل على الداتابيز أو n8n — **اقرا الحالة الحية الأول**
5. لو الشغل على الفرونت — شغّل `bash check.sh` **الأول** عشان تعرف نقطة البداية نضيفة
6. اشرح اللي هتعمله **قبل** ما تعمله
7. آخر الجلسة — `bash check.sh` + حدّث Notion
