# سهل (Sahl) — ملف ذاكرة المشروع

> اقرا الملف ده بالكامل قبل أي شغل. ده السياق الدائم للمشروع.

---

## اللغة

**التواصل بالعربي المصري العامي.** المصطلحات التقنية والمعرّفات بالإنجليزي زي ما هي.

---

## إيه هو المشروع

منصة SaaS متعددة المستأجرين للتجار المصريين اللي بيبيعوا بالدفع عند الاستلام (COD).

بتأتمت: استقبال الأوردر → تأكيده بالواتساب بأزرار → بوليصة شحن تلقائية → تتبع → مخزون → أرباح → إشعارات تلجرام → صندوق محادثات واتساب.

**الوضع:** live وشغّالة بأوردرات حقيقية. تاجر واحد نشط (متجر حقيقي كإثبات مفهوم). **صفر عملاء مدفوعين لسه.**

---

## 🔴 المبدأ المعماري الحاكم

> **Central workflows — مش نسخ لكل تاجر.**

كل workflow بيقرا هوية التاجر **ديناميكياً** وبيجيب بياناته من الداتابيز وقت التنفيذ.

**تاجر جديد = صف جديد في `tenants`. مش workflow جديد ولا نسخة ولا شغل يدوي.**

أي اقتراح بيكسر ده **مرفوض** — بيحوّل المنتج لخدمة يدوية.

---

## 🔴 قواعد أمان لازمة

1. **ممنوع تعديل آلي على وركفلوهات n8n الإنتاجية.** شخّص، واكتب الخطوات، وسيب التنفيذ اليدوي للمالك. الوركفلوهات فيها بيانات اعتماد حية **وبتخلق شحنات بفلوس حقيقية**.

2. **ممنوع تشغيل أو اختبار وركفلو الأوردرات المركزي** عبر أي أداة — بيخلق أوردرات حقيقية ويبعت رسايل واتساب حقيقية.

3. **مفيش أسرار في الكود ولا في الملفات دي.** المفاتيح مكانها متغيرات البيئة أو جدول إعدادات المنصة أو الـ vault.

4. **أي تعديل على الداتابيز يتجرّب الأول** بترانزاكشن بترجع:
```sql
DO $$ BEGIN
  -- التعديل والاختبار
  RAISE EXCEPTION 'النتيجة: %', var;  -- بيرجّع كل حاجة
END$$;
```
ده بيمنع كمان إرسال رسايل تلجرام حقيقية، لأن طلبات الشبكة بتتحط في طابور جوّه الترانزاكشن.

---

## أسلوب الشغل (مهم جداً)

- **افهم قبل ما تصلح.** التشخيص وشرح السبب الجذري **قبل** أي حل. المالك عايز يفهم مش بس يتصلح.
- **اتأكد من الحالة الحية مش من الذاكرة.** اقرا الداتابيز/الوركفلو/الملف قبل ما تفترض. الورق ممكن يكون قديم.
- **ماتفترضش دلالة API — اتحقق.** (اتلسعنا: افترضنا إن نداء POST بيعمل شحنة والحقيقة إنه بحث. الافتراض ده أدى لقرار تشغيلي غلط.)
- **خطوة خطوة.** كل بند يتنفّذ ويتجرّب ويتأكد قبل اللي بعده. المالك بيجرّب كل تسليم بنفسه.
- **إصلاح للأمام بس.** البيانات التاريخية المصححة يدوي ماتتلمسش.
- **صفر حشو.** مفيش ملخصات ولا مقدمات ولا اعتذارات مطوّلة.
- **لما تغلط، قول غلطت بوضوح** واشرح السبب — من غير جلد ذات.

---

## هيكل الملفات

```
sahl/
├── CLAUDE.md          ← الملف ده
├── app/
│   ├── index.html     ← لوحة التاجر (~7,400 سطر، ملف واحد)
│   └── _headers       ← رؤوس الأمان لـ Cloudflare
└── admin/
    ├── index.html     ← لوحة السوبر أدمن (نطاق منفصل)
    └── _headers
```

**⚠️ الملفين HTML ضخام وفيهم كل حاجة (HTML + CSS + JS).** التعديل عليهم لازم يكون **جراحي**:
- ابحث عن النص بالظبط، وتأكد إنه موجود **مرة واحدة** قبل ما تستبدل
- بعد أي تعديل، **افحص صحة الجافاسكربت**:
```bash
python3 -c "
import re; s=open('app/index.html',encoding='utf-8').read()
b=re.findall(r'<script(?![^>]*src=)[^>]*>(.*?)</script>', s, re.S)
open('/tmp/c.js','w',encoding='utf-8').write('\n;\n'.join(b))"
node --check /tmp/c.js
```
- تأكد إن الـ IDs الجديدة **مش مكررة**

---

## الأدوات (MCP)

| الأداة | الاستخدام |
|---|---|
| **Supabase** | التطوير والتشخيص الأساسي — استعلامات، migrations، لوجات، Edge Functions |
| **n8n** | **تشخيص بس** — اقرا الوركفلو والتنفيذات. ممنوع التعديل. |
| **Notion** | ذاكرة المشروع طويلة المدى — **يتحدّث آخر كل جلسة** |

معرّف مشروع Supabase موجود في ملفات الفرونت (مش سر — الـ anon key عام بطبيعته).

---

## Notion = مصدر الحقيقة

فيه صفحة واحدة فيها تاريخ المشروع كامل بالأقسام المرقّمة.

- **أول كل جلسة:** اقراها لو محتاج سياق تاريخي
- **آخر كل جلسة:** حدّثها بالشغل الجديد والقرارات

**التحديث بيتم بمراسي نصية مطابقة حرف بحرف** — اقرا الصفحة قبل ما تكتب.

---

## النشر

الفرونت على Cloudflare Pages. مشروعين منفصلين (لوحة التاجر / لوحة الأدمن).

**⚠️ الداتابيز والفرونت لازم يتنشروا مع بعض.** حصل قبل كده إن تعديل داتابيز اتنشر والفرونت لأ، فحاجات وقفت تشتغل من غير سبب واضح.

---

## دروس اتدفع تمنها (اقراها قبل ما تشتغل على n8n)

1. **n8n بيحفظ من غير ما ينشر.** وقعنا فيها مرتين في يوم واحد — التعديل يبان شغال في المحرر والعملاء على النسخة القديمة. **الحكم من `activeVersionId` مش من شكل النود.** بعد كل تعديل → Publish فوراً.

2. **إضافة نود في نص مسار شغّال بتكسر كل اللي بعدها** — لأنها بتغيّر `$json`. **الحل: نادي النودز بأسمائها الصريحة** `$('اسم النود').item.json...`

3. **إعادة تسمية نود = كسر صامت.** 14 نود بقت تنادي اسم مش موجود، ومسار التأكيد وقف في الإنتاج من غير ما حد ياخد باله.

4. **أي نود تقع بتقتل كل الفروع التانية من نفس التريجر** — حتى اللي مالهاش علاقة. والترتيب بيتحدد **بموقع النود على الكانفس**. ده أوقف صندوق المحادثات 3.5 ساعة.

5. **نود بترجّع فاضي = الفلو يقف صامت** والتنفيذ يتسجّل "ناجح". فعّل `Always Output Data` على نودز البحث.

6. **الفلترة والترتيب مرتبطين.** فيو بترتيب معكوس عن توقّع الـ `Limit` = النظام يمسك **العنصر الغلط في صمت**. مفيش خطأ، بس النتيجة غلط. **أخطر نوع باج.**

7. **`maybeSingle()` بترمي خطأ لو لقت صفين** والكود اللي بيقرا النتيجة كـ "مفيش" بيخرج ساكت. **الخروج الصامت خبّى مشكلة 3.5 ساعة.** فرّق دايماً بين "مفيش" و"أكتر من واحد".

8. **الباجات النايمة بتصحى من تغييرات إعدادات مش كود.** تغيير باقة تاجر فعّل باج كان نايم من شهور.

9. **أي تنبيه لازم يكون ليه طريقة إقفال** — وإلا بيتعوّد الناس يتجاهلوه ويفقد قيمته.

10. **الاختبار على تاجر واحد مضلل.** حاجة "شغالة" ممكن تكون شغالة بالصدفة لأن غلطتين بيلغوا بعض. **جرّب على تاجرين.**

11. **الـ JSON Body في نود HTTP ماينفعش فيه Enter حقيقي** — لازم `\n`. الخطأ: `Bad control character`.

12. **الفيوهات المعمولة بـ `SELECT *`** بتتفك لأعمدة ثابتة وقت الإنشاء. **أي عمود جديد على الجدول لازم يتبعه إعادة إنشاء الفيو.**

13. **الفيوهات من غير `security_invoker=on`** بتشتغل بصلاحيات المالك وبتتخطى RLS.

---

## قرارات محسومة (متتناقشش تاني)

- **Central pattern** — مفيش نسخ workflows لكل تاجر
- **التوجيه بالـ payload** — أزرار الواتساب بتحمل `<فعل>:<معرّف التاجر>:<معرّف الأوردر>`، فالرسالة بتقول صاحبها بنفسها. ده اللي خلّى الرقم المشترك وTech Provider ممكنين.
- **الإلغاء إشارة مش أمر تنفيذ** — لو الأوردر خرج للشحن، مايتلغيش. علامة تتسجّل والتاجر يقرر. (مسح البوليصة = الشحنة تتوه والبضاعة تضيع.)
- **إقفال التنبيهات يدوي بحت** — عشان نضمن إن التاجر شاف
- **سهل ماتردش على الرسايل النصية** — الرقم رقم التاجر والصندوق صندوقه
- **الأوردر اللي بيتولد مؤكد = مفيش خصم** — التاجر أكّده بره المنصة، إحنا مشتغلناش فيه
- **الخصم على التحويل مش على الإدخال**
- **مفيش فحص دوري لتوكنات ميتا** — نستنى الشكوى (قرار واعي بقبول المخاطرة)
- ❌ **الردود الصوتية للعملاء — مش هتتعمل نهائياً**

---

## المخاطر المفتوحة

### 🔴 عاجل
- **سياسات مخزن ميديا الواتساب مفتوحة للجميع** — قراءة **ورفع** لأي حد من غير تسجيل دخول، ومفيش عزل بين التجار
- **مفاتيح وتوكنز نص صريح جوّه n8n** — توكن البوت في 6 نودز، توكن الواتساب، مفتاح الشحن

### 🟠 الحاجز أمام العميل التاني
- فروع تلجرام الخاصة بالتاجر الحالي مش مقفولة (العمود جاهز، فاضل شروط IF)
- نودتين بتردوا على عميل التاجر برقم التاجر الحالي — لازم يتحولوا HTTP
- صفحة الإعدادات مابتعرضش رابط الربط ورمز التحقق
- جدول محادثات قديم لسه مستخدم في 5 نودز
- بيانات الرقم المشترك فاضية

### 🟡 قبل التوسع
- **طابور إشعارات** — حدود تلجرام: 30 رسالة/ثانية إجمالي، 1/ثانية لنفس الشات. مفيش retry والأخطاء بتتبلع، **فالرسايل بتضيع في صمت**. لازم قبل 15-20 تاجر.
- النسخة الجديدة من وركفلو الأوردرات مبنية وغير مفعّلة
- طبقة التحقق من توقيع ميتا جاهزة ومستنية المفتاح

### الهشاشة الكبرى
**ملف الواجهة الضخم** — دي أول حاجة اتحلت بنقل المشروع لـ git. **استخدم commits صغيرة ومتكررة.**

---

## أول حاجة تعملها في أي جلسة

1. اسأل عن الهدف
2. لو محتاج سياق تاريخي — اقرا Notion
3. لو الشغل على الداتابيز أو n8n — **اقرا الحالة الحية الأول**
4. اشرح اللي هتعمله **قبل** ما تعمله
5. آخر الجلسة — حدّث Notion
